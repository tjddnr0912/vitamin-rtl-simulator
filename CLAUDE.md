# CLAUDE.md - 016_claude_rtl (vitamin)

오픈소스 Rust RTL 시뮬레이터. CLI: `vita`(원샷) / `vcmp`(compile) / `velab`(elaborate) / `vrun`(simulate). SystemVerilog 합성가능 RTL 서브셋(Verilog-2005 RTL 전부 포함)이 Phase-1 MVP.

## 코드 리뷰 방법론 (구현·설계 이후 리뷰 단계)

- **적대적 코드 리뷰** — 라이브 차분(iverilog 등)으로 silent-wrong을 실제 재현해 검증.
- **Fagan Inspection** — 역할(Sub-agent)을 **Author / Moderator / Reviewer / Recorder** 로 분리해 진행.
  - 사전 정의 체크리스트는 **Spec 문서**(`docs/preview/`)로 대체. 별도 체크리스트가 있으면 대체가 아니라 **합산**.
  - 코드의 **논리적 오류**를 검증.
- 리뷰 관점 4축: **Architecture & System Integration** · **Performance & Efficiency** · **Maintainability & Readability** · **Robustness & Testability**.

> **상태:** **전 파이프라인 동작** — one-shot `vita design.sv`, staged `vcmp→velab→vrun` 모두 VCD+stdout 산출. 구현 범위:
> - **Phase-1** 합성 RTL 대부분 — timescale·다차원 unpacked/packed 배열·casex/casez·fork-join·func/task·SV 자료형(enum/typedef/packed struct).
> - **Phase-2** — worklib·package·string·dynamic/queue/assoc 배열·정밀화 IR.
> - **Phase-3+** — SVA 시퀀스·property ops·`cover property`(SVA-REST)·deferred immediate assert·automatic/recursive 콜스택·HIER-REST(계층 참조)·functional coverage·N7 class/OOP(상속+가상 동적 디스패치)·constrained-random verification(rand/constraint/`randomize() with`/dist/randc)·array·string 메서드·program/union/parameterized class/virtual interface·**N6 real-math**(`$ln/$sin/$exp/$sqrt/$pow/$floor/$ceil/…` 21종 §20.8.2 + 비균등 `$dist_normal/exponential/poisson/chi_square/t/erlang`, vendored 순수-Rust libm).
> - **정확성 원칙 = "correct-or-loud"**: silent-wrong은 적대 리뷰(라이브 iverilog 차분)마다 모조리 수정. iverilog 미지원분(SVA·OOP·CRV·param-class·virtual interface)은 hand-IEEE 핀.
>
> **현재:** 2660 테스트 green · **format_version 19** · MsgCode 57 · 3-OS CI green. **🏁 typedef-recognition family 완료(§4.5.40~53, 9 슬라이스)** — 타입명이 올 수 있는 모든 parse-context(block decl·function ret·for-init·struct/union member·chained typedef·tf-port·body typedef DEF·enum base)서 user typedef 인식. **🟢 enum BASE 타입을 typedef명으로 `typedef enum b_t {R,G,B}`(2026-06-29, branch `feat-enum-base-typedef`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.53)** — family 마지막 멤버. enum-base 파서가 built-in 키워드/bare `[range]`만 수용→`typedef logic[1:0] b_t; typedef enum b_t {…}`가 E2002였음→`peek_typedef_name()` arm 추가(unsigned vector typedef는 `info.range`를 base로=built-in `enum logic[N]`과 동일 TypeInfo). honest-loud: struct/class/**signed**/multi-dim/range-less(atom). 근거: enum base 모델=`Option<Range>`뿐·built-in도 signed base 드롭(`enum logic signed[3:0]`=vita`15` vs ive`-1`=별개 pre-existing)→signed/atom은 silently-unsigned 대신 loud. 순수 파서·`.vu`/format 19 불변·IR-0. 적대 2-서브 **silent-wrong ZERO**: differential 29-probe(unsigned vector base·label wrap·enum methods·typedef-vs-builtin byte-identical·signed/atom honest-loud)·soundness 5-task 전수증명(byte-identity·guard tight·base=info.range 동일). 7 테스트. too-permissive(silent-wrong 아님): enum을 또다른 enum base로(iverilog 거부). **🟢 block-local struct VAR shadow scoping(`var_struct` clobber fix)(2026-06-29, `feat-block-struct-var-shadow`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.52)** — §4.5.51 발굴 PRE-EXISTING ① silent-wrong: block/function-local struct var가 module struct var를 shadow→`var_struct` clobber→outer `x.field` inner 레이아웃=`b` should `bb`→§4.5.51 scope snapshot을 VAR-keyed 4맵으로 확장(`ScopeSnapshot` 8맵). 적대 2-서브 CONVERGED on tf_body 갭(`function f; inner_s x; return 8'hff`=frame E3009 미발화 silent-wrong)→§4.5.36 ALL-variants대로 tf_body에도 trigger. 7 테스트. **🟢 procedural body-local typedef DEFINITION `function f; typedef logic[3:0] t; t x; …`(2026-06-29, `feat-body-typedef-def`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.51)** — function/task 바디·begin/end 블록서 `typedef ...;` 정의 statement가 E2002였음→`parse_body_typedef_def`(기존 `parse_typedef` 호출+노드 discard·enum honest-loud) + `ScopeSnapshot` snapshot/restore로 **lexical scope**(function-local `t`가 module `t` clobber 방지=`ff 15`). 적대 2-서브 silent-wrong ZERO(25-probe). 11 테스트. **별개 발견(§4.5.52서 수정)=PRE-EXISTING ① silent-wrong**: block-local struct var의 module struct var same-name shadow→`var_struct` clobber→outer `x.a` inner 레이아웃(`b` should `bb`)·body typedef 무관·var_struct 스코핑 필요. honest-loud: body enum typedef·frame struct field-assign. **🟢 tf-port 타입을 typedef명으로 `function f(byte_t a)`(2026-06-28, `feat-tf-port-typedef`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.50)** — function/task PORT 타입을 ANSI(`parse_tf_port`)+non-ANSI(`parse_tf_port_decl_into`) 둘 다 built-in 키워드만 파싱→typedef명 오소비 E2002였음→공유 `try_tf_port_typedef`를 양 port 파서가 경유(§4.5.36 ALL-variants·struct/class/multi-dim=honest-loud). 적대 2-서브 둘 다 CLEAN/SOUND(38-probe·ANSI/non-ANSI parity·inherited propagation·typedef-vs-builtin byte-identical). 10 테스트. **🟢 chained typedef alias `typedef base_t alias_t;`(2026-06-28, `feat-chained-typedef`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.49)** — `parse_typedef`가 typedef 뒤 enum/struct/union/built-in 키워드만 수용→기존 typedef명 alias가 E2002였음→`parse_typedef_chained_alias`가 base의 4 type-keyed 등록(typedefs/struct_layouts/enum_defs/union_type_names)을 새 이름으로 MIRROR(property 있으면 SET·없으면 CLEAR=§4.5.31 cross-module stale 방지). dims 추가=honest-loud. 적대 2-서브 둘 다 CLEAN/SOUND(36-probe·ALIAS-vs-DIRECT byte-identical). 10 테스트. **🟢 packed struct/union MEMBER에 user-defined 타입명(2026-06-28, `feat-struct-member-typedef`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.48)** — struct/union member 타입 파서가 built-in 키워드만 수용→typedef member(`byte_t a`)가 E2002였음→공유 `parse_struct_member_type`(simple typedef=`peek_typedef_name`→kind/sign/range)를 struct+union 루프 둘 다 경유(§4.5.36 ALL-variants). nested struct/class/multi-dim=honest-loud. 적대 2-서브 둘 다 CLEAN/SOUND(32-probe·typedef-vs-builtin byte-identical). 7 테스트. **🟢 function/task default argument values `function f(int a, int b=10)`(2026-06-28, `feat-tf-default-args`, IR-0·format 19·`.vu` 재핀, ROADMAP §4.5.47)** — tf-port default가 E2002였음→`TfPort.default`+공유 `fill_default_args`(생략 trailing formal의 default를 caller scope서 채움)를 4 call-site(inline/frame func·frame/inline task). 적대 2-서브 **둘 다 CONVERGED on silent-wrong 1종**: `expr_reads_ident`가 `Cast` 미순회→`b=int'(a)` cast-wrapped formal-ref guard 우회→`K::Cast` arm 추가. 8 테스트. **🟢 multi-dimensional unpacked array assignment pattern `int a[2][3]='{'{1,2,3},'{4,5,6}}`(2026-06-28, branch `feat-multidim-array-pattern`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.46)** — §4.5.33 deferred("v1: 1-D unpacked array") 해소. multi-dim 언팩 배열 nested `'{…}`이 E3009였음(iverilog `1 3 4 6`). 수정: 신규 `flatten_assign_pattern`이 (중첩) 패턴을 **row-major leaf로 flatten**(각 레벨 count를 residual dim과 검증·multi-dim이면 elem마다 nested `'{…}` 요구)→각 leaf를 `residual_word_offsets`(이미 row-major·per-dim 방향 존중)의 매칭 offset에 assign. 1-D=base case byte-identical·PACKED multi-dim(reverse-order)은 별도 경로 loud 유지. IR-0. 적대 2-서브 **둘 다 CLEAN/SOUND**: differential 28-probe(2D/3D/4D·asymmetric·descending·mixed-dir·fill·2-state·shape-mismatch loud·zero silent-wrong/byte-break); soundness가 **leaf↔offset alignment를 mixed-radix identity로 증명**(flatten=direction-agnostic·offset table이 desc 1회만 적용)·8-claim 전수증명. soundness rec 2종 반영: unroll cap(`ARRAY_PATTERN_UNROLL_CAP=4096`=array-copy convention)·stale "1-D" 메시지 수정. 9 테스트. pre-existing: function-body local·generate-block array decl-init(별개). **🟢 queue/dyn-array `'{…}` declaration initializer(2026-06-28, `feat-queue-dyn-decl-init`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.45)** — `int q[$]='{1,2,3}`이 E3009였음→decl-init을 var-init flush서 runtime ops로 EXPAND(queue `push_back`·dyn `new[N]`+write)·단일 declaration-ordered 리스트서 expand=decl-order 보존. 적대 2-서브 CONVERGED on 2종(generate/interface silent-drop→`allow_string_init` 게이트·scalar가 선행 queue `.size()` 0→ordered 리스트). 11 테스트. **🟢 multi-dimension `foreach(a[i,j,…])`(2026-06-28, `feat-multidim-foreach`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.44)** — multi-dim foreach가 E2002였음→`parse_multidim_foreach`가 nested `while`(2-arg `arr.first/next(idx,DIM)` desugar·innermost `wrap_break`)·row-major·break=innermost·empty-slot skip·single-index byte-identical. soundness가 중복 인덱스명 `foreach(a[i,i])` silent-wrong 발굴→parse-time loud. 11 테스트. **🟢 typed for-init with user-defined 타입명 `for(my_t i=0;…)`(2026-06-28, `feat-for-init-typedef`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.43)** — for-init 선언이 user typedef면 E2002였음→`parse_for`가 `peek_block_typedef_decl()`(§4.5.40 재사용)→`parse_for_typed_init_typedef`가 `TypeInfo`를 공유 `build_for_typed_init` tail에 매핑. 순수 파서·additive·byte-identity. 부수: class-handle for-init=parse-time 명확 loud(soundness rec). 11 테스트. **🟢 inline 함수 반환값+body local을 선언 폭/sign으로 resize(2026-06-28, `feat-inline-func-ret-width`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.42)** — INLINE 함수는 `fold_straight_line`이 net write 없어 return/local이 self-det 폭/sign 유지=broad silent-wrong(`f=8'hAB`→`ab` should `b`·`f=42`→`0000002a`)→신규 `resize_inline_assign`(§10.7 truncate/extend[RHS sign]/target sign stamp). soundness가 real-반환 FRAME 호출 `$signed(real_call)` silent-wrong 발굴→`cast_operand_is_real` 가드. 10 cli+3 elaborate 테스트. 잔여: context-width가 inline body NON-fill 서브식 미전파(`f=a<<2`→`0c`·`f16=t1*t2`→`0002`). **🟢 function RETURN type via user-defined 타입명 `function b_t f;`(2026-06-28, `feat-func-ret-typedef`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.41)** — 함수 반환 타입이 typedef/enum/struct면 E2002였음→헤더 반환-타입에 `peek_block_typedef_decl()` arm(typedef `TypeInfo`→반환 필드). soundness가 multi-dim packed typedef 반환 silent-wrong→`!info.packed.is_empty()`=loud. 7 테스트. **🟢 block-local/tf-body 선언에 user-defined 타입명 `my_enum_t s;`(2026-06-28, `feat-block-local-typedef-decl`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.40)** — 절차 블록·task/func 바디서 enum/typedef/struct 로컬 선언이 E2002였음→신규 `peek_block_typedef_decl`로 `block_body`+`tf_body` typedef arm. 적대 둘 다 CLEAN/SOUND. 9 테스트. **🟢 block-local unpacked-array decl-init `int a[4]='{1,2,3,4}`(2026-06-28, `feat-array-declinit-pattern`, IR-0·format 19 불변, ROADMAP §4.5.39)** — block-local array decl-init이 silently 드롭(`hoist_block_local_nets`가 `name.unpacked.is_empty()`만)=① silent-wrong. collect_var_init_drivers와 미러. 적대 둘 다 CLEAN/SOUND. 7 테스트. **🟢 ANSI tf-port comma-shared type stickiness `task t(input logic [7:0] x, y)`(2026-06-28, `feat-task-comma-port-type`, IR-0·format 19 불변, ROADMAP §4.5.38)** — comma-tf-port가 직전 타입 미상속(방향만)→`y`=1-bit ① silent-wrong. `parse_tf_port`가 `inherited_type` 스레드. AST shape 불변(VALUE만). 적대 둘 다 CLEAN/SOUND. 10 테스트. **🟢 packed-struct `'{…}` 패턴 — 1-D struct-array element `arr[i]`(2026-06-28, `feat-struct-pattern-array-elem`, IR-0·format 19 불변, ROADMAP §4.5.37)** — struct cluster 종결. `struct_1d_array_vars` 셋(provably disjoint)+desugar 코어 리팩터+`BitSelect` 처리. 적대 둘 다 CLEAN/SOUND. 6 테스트. **🟢 packed-struct `'{…}` 패턴 — continuous/force/for 컨텍스트(2026-06-28, `feat-struct-pattern-assign-contexts`, IR-0·format 19 불변, ROADMAP §4.5.36)** — `maybe_struct_pattern_rhs` 훅을 4 assign-RHS site(cont/proc/force/for)에 적용=§3 do-while 교훈의 assign-RHS판. soundness가 `parse_for_assign` missing 발굴→fold. 7 테스트. **🟢 packed-struct positional assignment pattern `s = '{e0,…,eN}`(2026-06-28, `feat-struct-assign-pattern`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.35)** — scalar struct `'{…}`=field-width 캐스트 concat(parser-only desugar). 적대 2-round COMPLEMENTARY가 2-state X/Z coercion·union truncation·cross-module collision 3종 발굴→수정. 16 테스트. **🟢 cast of fill literal takes the cast width(`8'('1)`=`ff`)(2026-06-28, `feat-cast-fill-width`, IR-0·format 19 불변, ROADMAP §4.5.34)** — fill을 width로 cast 시 vita가 1-bit lower 후 zero-extend(`8'('1)`=`01`)하던 pre-existing ① silent-wrong을 `lower_ctx_or_plain(operand, target_width)`로 수정. 3 cast form·6 테스트. struct `'{}` desugar UNBLOCK. **🟢 1-D unpacked array `'{…}` 패턴**(2026-06-28, `feat-array-assign-pattern`, format 19·`.vu` re-pin, ROADMAP §4.5.33 ← §4.5.15): AST `ExprKind::AssignPattern`·elaborate `array_assign_special` element-wise; soundness가 bare fill elem silent-wrong(`'1`=8'h01)→`lower_ctx_or_plain`. 12 테스트. **🟢 block-local scope leak honest-loud detection**(2026-06-27, `feat-block-local-scope`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.30 ← §4.5.18): nested block-local이 블록 밖서 참조되면 flat 테이블 때문에 outer 대신 block-local을 silently 읽던 silent-wrong을 **detection-only honest-loud**(subtree 밖 참조=loud E3009)로 처리. 적대 2-round(sibling false-positive·ancestor false-negative→`stmt_contains_block_span` guard) 후 CLEAN. 10 테스트. **🟢 `.*` 암시적 wildcard 포트 연결**(2026-06-27, `feat-dotstar-port`, format 19·`.vu` re-pin, ROADMAP §4.5.29): `.*`(IEEE §23.3.2.5) parse-reject→supported. AST `PortConnList::Named(Vec,bool)` wildcard flag·`wire_ports`서 미명시 포트마다 same-named ref 합성. 적대 differential이 silent-wrong 발굴(INPUT 포트를 same-named constant에 연결)→net only 합성·constant=loud. 10 테스트. **🟢 `.name` 암시적-named 포트 shorthand**(2026-06-27, `feat-dotname-port`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.28): bare `.port`(IEEE §23.3.2.3) parse-reject→supported. 순수 파서 desugar(`.port`≡`.port(port)`). differential CLEAN(76 probe)·soundness SOUND. 7 테스트. **🟢 `defparam inst.param = value` 직접-자식 override**(2026-06-27, `feat-defparam`, IR-0·format 19 불변, ROADMAP §4.5.27): `defparam`(IEEE §23.10.1) direct-child loud→supported. 적대 리뷰가 unmatched silent-skip 발굴→`.remove`+warn. 9 테스트. **🟢 zero-count replication `{0{x}}` width(2026-06-27, branch `feat-zero-replicate`, IR-0·format 19 불변, ROADMAP §4.5.26)** — `{0{x}}`(IEEE §11.4.12.1=width 0)가 `width.rs` Replicate self-width `.max(1)` 때문에 1-bit로 잘못 sizing돼 concat을 `45`(should `a5`)로 만들던 silent-wrong을 `.max(1)` 제거로 수정. differential CLEAN·soundness SOUND. 5 테스트. **🟢 `%v`/`%V` multi-bit strength**(2026-06-27, `feat-v-strength`, IR-0·format 19 불변, ROADMAP §4.5.25): `%v` of 멀티비트가 vita는 bit 0만(`4'b10xz`→`HiZ`)·iverilog 비트별 전체(`St1_St0_StX_HiZ`)이던 silent-wrong을 `strength_form` 전 비트 MSB-first 순회+`_` join으로 수정(1-bit byte-identical). differential CLEAN·soundness SOUND. 4 테스트. **format cluster 완결**(signed-fieldwidth→`%0s`→`%Ns`→uppercase→`%v` 5연속). **🟢 uppercase format spec `%S/%C/%M/%E/%G/%T`**(2026-06-27, `feat-uppercase-fmt`, IR-0·format 19 불변, ROADMAP §4.5.24): vita가 `%S`/`%C`/`%M`/`%T`를 리터럴 출력+arg 미소비(arg-shift silent-wrong)·`%E`/`%G` 소문자 exponent하던 것을 별칭+`to_ascii_uppercase`로 수정. 적대 differential이 over-eager `%F` uppercase 발굴(iverilog `%F` inf/nan 소문자)→`spec=='E'||'G'` 가드. byte-identical(대문자=이전 리터럴=additive). differential 1 silent-wrong→CLEAN·soundness SOUND. 7 테스트. **🟢 explicit `%Ns` field-width for `%s`**(2026-06-27, `feat-string-fieldwidth`, IR-0·format 19 불변, ROADMAP §4.5.23): vita `'s'` arm이 `field_width` 무시하던 silent-wrong을 수정—`%Ns`=content를 width N 우측정렬(MINIMUM·overflow truncate 안 함)·packed는 explicit-width면 stripped·bare `%s`면 reg-width·pad=`chars().count()`. bare `%s`/`%0s` byte-identical. differential CLEAN(88 probe)·soundness SOUND. 6 테스트. **🟢 `%0s` packed-reg leading-null strip + x/z byte→space**(2026-06-27, `feat-zero-s-strip`, IR-0·format 19 불변, ROADMAP §4.5.22): `%0s`로 packed reg 표시 시 leading NUL을 vita 패딩·iverilog strip하던 silent-wrong을 `fmt_packed_chars_min`(strip 변형)으로 수정(`%s`는 무변경). +적대 differential 발굴 z byte(`val=1`→`0xFF` leak) fold=byte 추출 `val & !unk` x/z 마스크(공유 `packed_byte` 헬퍼·`%s` 동시 fix). differential CLEAN·soundness SOUND. 7 테스트. **🟢 signed `%d` default 필드폭(sign column)**(2026-06-27, `feat-signed-fieldwidth`, IR-0·format 19 불변, ROADMAP §4.5.21): SIGNED 값 default `%d` 폭이 1 좁던 silent-wrong을 `dec_field_width(n, signed)`로 수정(signed=`1+digits(2^(n-1))`·NOT unsigned+1·`n=1` 예외=iverilog 폭 1). blast-radius 25 dyn_storage 핀=space-only script 일괄 갱신. 적대 differential이 n=1 발굴→수정 CLEAN·soundness SOUND. 7 테스트. **🟢 `$swrite`/`$swriteb/o/h` + Sformat dest 가드**(2026-06-27, `feat-swrite`, IR-0·format 19 불변, ROADMAP §4.5.20): vita가 `$swrite` 미매핑→W3056 silent-skip(타깃 empty)하던 silent-wrong을 `$sformat` 엔진으로 **2-line 매핑**($write-style append 공유)·전 arg 레이아웃 correct. +적대 리뷰 발굴 Sformat non-whole-net dest silent-no-op→elaborate loud-reject(iverilog 일치·`$cast` 가드 미러·$sformat 동시 수정). 적대 differential CLEAN·soundness SOUND. 9 테스트. **🟢 bare event control `@e`(괄호 없는)**(2026-06-27, `feat-bare-event-ctrl`, IR-0·format 19·AST/`.vu` 불변, ROADMAP §4.5.19): IEEE 1364 `@ hierarchical_identifier`(`@e`/`@clk`)를 loud→supported(named event는 이미 완비·유일 갭=bare 괄호생략). 파서 1-site(`parse_sensitivity`서 `is_ident()`면 `expr_postfix()` NO-EDGE 참조·`@a+b`는 loud)·불변식 **bare `@X`≡paren `@(X)` 바이트 동일**(미지원 `@a[2]`/`@u.s`/`@f(x)`는 paren과 동일 loud)·byte-identical(이전 parse-error=additive)·전 `@` site 공유. 적대 differential CLEAN·soundness SOUND. 9 테스트. honest-loud=bare edge(괄호 필수)·`->>e`. **🟢 function/task가 module-level 변수 READ(frame path)**(2026-06-27, `feat-func-module-var`, IR-0·format 19 불변, ROADMAP §4.5.17): frame-path 함수가 module 변수 읽으면 E3010이던 것을 `walk_scopes_key`의 `$func$` transparency **1-line** 추가로 loud→supported(`$itask$`와 동일·SV §13.4 non-pure·shadowing 정확·WRITE는 loud 유지)·byte-identical; 적대 differential CLEAN·soundness SOUND·보너스 param-width frame func `W` 미해소 동시 수정. 부수발굴 별개 §4.5.18(frame inner-block local collision). **🟢 enum value methods `.first/.last/.num/.next/.prev`**(2026-06-27, `feat-enum-methods`, IR-0·format 19 불변, ROADMAP §4.5.14): SV §6.19.5 enum value 메서드 5종을 파서서 라벨값/위치기준 ternary chain으로 desugar(literal-foldable일 때만·`var_enum` 추적·기존 AST 재사용)·byte-identical; 적대 differential CLEAN·soundness가 pre-existing `const_lit` i64 overflow 발굴→checked arith 수정. honest-loud=`.name`/non-foldable/`.next(N)` step. **🟢 break/continue loop control**(2026-06-27, `feat-break-continue`, IR-0·format 19 불변, ROADMAP §4.5.13): SV §11.5 `break`/`continue`를 고전 `disable <label>` idiom으로 파서 desugar(기존 `Stmt::Disable`→Goto lowering 재사용·6 루프 전부·byte-identical); 적대 differential이 do-while silent-wrong 1종(`parse_do_while` 미적용→enclosing 루프 잘못 타깃) 발굴→수정·재-hunt CLEAN. **🟢 increment/decrement + compound assignment `i++`/`i += e`(2026-06-27, `feat-inc-compound`, IR-0·format 19·`.vu` 불변, ROADMAP §4.5.12)**: SV §11.4 `i++`/`++i`/`i--`/`--i` + 12 compound op를 statement/for-step서 **파서가 기존 `Stmt::Blocking`으로 desugar**(렉서 14 토큰·longest-match가 SVA `|=>`/unary `+`/`<<<` 분리). expr-embedded(`a=i++`)=honest-loud. 적대 differential **CLEAN**(55 probe)·soundness **SOUND**; double-eval 우려 측정 반증. honest-loud: generate-for `g++`. **🟢 net-declaration delays `wire #3 w = a;`(2026-06-27, `feat-net-delay`, IR-0·format 19 불변[AST `.vu` re-pin], ROADMAP §4.5.11)**: IEEE §6.1.3 net-decl delay를 등가 `assign #d`로 desugar(파서 `NetVarDecl.delay`+elaborate `fold_ca_delay`→`ca_delays` 사이드카·staged 무료). 적대 differential이 silent-wrong 2종(generate net-init driver drop·procedural delay swallow) 발굴→수정·재-hunt CLEAN·byte-identical. **🟢 wand/wor wired-logic net resolution(2026-06-27, `feat-wand-wor`, IR-0·format 19 불변, ROADMAP §4.5.10)**: §4.5.9 후속—`wand`/`wor` 다중드라이버를 IEEE 1364 wired-AND/OR 4-state resolution **loud→supported**(plain `Wire`+`wired_and/or_nets` 사이드카·3 net-생성 경로 전부+staged trailer). 적대 differential이 CRITICAL 1건(output 포트 사이드카 누락→wire-x) 발굴→수정·재-hunt CLEAN·byte-identical. 잔여: `inout wand/wor`=기존 inout 갭. **🟢 multi-driver continuous-assign wire resolution(2026-06-27, `feat-multidriver`, IR-0·format 19 불변, ROADMAP §4.5.9)**: tristate/양방향 버스(E3001 hard-reject)→IEEE 1364 4-state wire resolution **loud→supported**(whole-net·non-delayed ≥2; partial-overlap loud 잔존). 적대 2-서브(differential 70+ probe·soundness 16-combo 전수증명) 둘 다 CLEAN·byte-identical. **🟢 코어 스케줄러 silent-wrong 연속 수정 + YELLOW 구현 배치(2026-06-26~27, 전부 IR-0·format 19 불변; 상세=ROADMAP §4.5.4~§4.5.8 · git 커밋)**: 스케줄러 #1/#2 글리치 wake-collapse·#3 self-retrigger·#4 gated-clock edge-collapse·narrow→wide 4-state posedge(`feat-sched-*` 5브랜치)·UDP(comb/seq)·N4 multi-event·SVA nested prop-ref/local-var(`feat-yellow-batch` 구현 5+honest-loud 4). **전부 적대 2-서브에이전트가 CRITICAL silent-wrong 발굴→수정**·byte-identical. 잔존 #5 mixed-edge t0·#6 `@*` t0/observer/Comb. **🟢 이전 배치(2026-06-26, 전부 IR-0·format 19 불변; 상세 = ROADMAP §4.5.3 · git 커밋)**: **Honest-loud 배치**(`feat-hlb`, 적대 리뷰 CRITICAL 3종 차단): gate rise/fall/turnoff 지연·real→longint cast·string concat 비대입·typed for-init·패킹구조체 필드폭+ascending write·class local/protected 접근제어 · **SVA/cast+C-perf 배치**(`feat-sva-cast-perf-batch`, 적대 리뷰 critical 1+important 1 차단): SVA empty-match 양측융합·cross-clock multi-term·N2c local-var·SVA-QUAD O(N) collapse(opt-in)·class up-cast·WAITER-POOL/DYN-HEAP-VEC/FORCE-REEVAL. **🟢 이전 배치(2026-06-25, 전부 IR-0·format 19 불변; 상세 = ROADMAP §4.5.2 · DEVLOG)**: ① **SV cast `casting_type'(expr)`**(숫자/size/signing·`N'(e)`·signed'/unsigned'; 4-lens 사전 review + 적대 2-round hunt silent-wrong 3종 수정[광폭 sign-extend·Z보존·real-fn bit-reinterpret]) · ④ **N4 clocking 코어**(Preponed sampler·`cb.sig`=preponed·`@(cb)`; 적대 4종 수정) · **N6 real-math 21종 + 비균등 `$dist_*`**(vendored 순수-Rust libm→3-OS 비트동일·real NaN/-0 표시 fix) · ② **SVA empty-match `##1`-인접**(trailing-`##0` silent-wrong→가드) · **`%d/%h/%o` unknown 대소문자**(§21.2.1.2 UNIFORM-소문자/혼재-대문자).
>
> 슬라이스별 누적 개발 이력(Stage C VM ~ 최신) → **[docs/DEVLOG.md](docs/DEVLOG.md)**(§누적 상태 로그 = 이 블록의 옛 상세를 이관). 향후 과제·전략 = **[docs/ROADMAP.md](docs/ROADMAP.md)**, 잔여 작업 트래커 = [docs/REMAINING_WORK.md](docs/REMAINING_WORK.md). SPEC `docs/preview/`가 단일 진실 공급원.

## 실행 (cargo-only · vita 자체 크레이트엔 build.rs 없음)

```bash
cargo build  --workspace --locked                       # 17-crate 워크스페이스 (15 prod + 2 dev)
cargo test   --workspace --locked                       # 결정성 골든 게이트 포함
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo fmt --all -- --check
```

MSRV **1.82** (`rust-toolchain.toml` 고정), **edition 2021**(edition 2024는 rustc≥1.85라 비호환). `blake3 = "=1.8.2"` 핀. `--locked` 필수(3-OS 재현성). **vendored libm**: `third_party/libm`(libm 0.2.16, MIT)는 path dep지만 `[workspace].exclude`로 **member 아님**(clippy `--workspace -D warnings`가 third-party 미린트). build.rs는 vita 자체 17 크레이트엔 없음 — vendored libm은 타겟 cfg 감지 build.rs를 가지나(deterministic), blake3/serde 등 기존 의존성도 이미 build script 보유.

## 핵심 정보

| 항목 | 값 |
|---|---|
| 파이프라인 | preprocess→lex→parse→elaborate→sim-ir→sim-engine→VCD (parse까지 언어의존, 이후 중립) |
| 결정성 | `#[derive(SchemaHash)]` 구조적 형상 해시로 `.velab`/`.vu` staleness 게이트, 3-OS 바이트 동일 (BTree-only, usize/float 금지, span-free) |
| 골든 루트 | `sim_ir::SimIr`. 형상 변경 시 루트 해시 flip → `format_version` bump(현재 **19**, 정의·버전별 주석은 `crates/vita-artifact/src/header.rs::CURRENT_FORMAT_VERSION`) + 전 `.velab` 재생성. 엔진-facing 사이드테이블(fork_modes·net_names·proc_multipliers·final_procs·wait_fork·sva 체커·deferred-assert 마커·frame nets·class_rand/constraints/dist/randc 등)은 `SimOpts`/elaborate IR-0 합성으로 out-of-band → 골든 무영향 |
| 직렬화 | `serde` + `postcard 1.x` 단일 인코더, `blake3` 다이제스트 |

## 주요 크레이트 (거의 전부 실코드; stub은 2개)

| crate | 역할 | 상태 |
|---|---|---|
| `cli` | `vita`/`vcmp`/`velab`/`vrun` multicall — 전 파이프라인 구동 + timescale 배선 | **실코드** |
| `hdl-preprocess` | `` `define ``/`` `include ``/`` `ifdef ``/`` `timescale `` 파싱 + SourceMap | **실코드** |
| `hdl-lexer`·`hdl-parser`·`hdl-ast` | logos 토크나이저 · hand-RD+Pratt 파서 · AST | **실코드** |
| `elaborate` | AST→sim-ir lowering(모듈/generate/func-task/다차원배열/timescale 등) | **실코드** |
| `sim-engine` | 이벤트구동 IEEE-1364 스케줄러 + eval + VCD 방출 | **실코드** |
| `vcd-writer` | 계층 `$scope`/`$var` VCD 출력 | **실코드** |
| `sim-ir` | 언어중립 IR(Expr/Stmt/Terminator/NetVar/SimIr 루트) | 실코드 |
| `vita-artifact`(+derive)·`vita-schema`·`diag` | 산출물 헤더+게이트(`format_version=18`) · SchemaHash · MsgCode(57, doc-15 bijection) | 실코드 |
| `vita-log` | 진단 게이트(GatePolicy/GatedSink — `-Wno-*`/`-Werror=`) | **실코드** (2026-06-10) |
| `hdl-builtins` | $task 핸들러 추출 대상 | **stub** (1줄; 기능은 sim-engine 인라인) |

## 상세 문서 (`docs/preview/`)

| 주제 | 파일 |
|---|---|
| 개요·목표·범위 | [00-overview](docs/preview/00-overview.md) · [01-goals-and-scope](docs/preview/01-goals-and-scope.md) |
| 언어·빌드·아키텍처 | [02-implementation-language](docs/preview/02-implementation-language.md) · [03-build-and-portability](docs/preview/03-build-and-portability.md) · [04-architecture](docs/preview/04-architecture.md) |
| 전략·시뮬엔진·VCD·timescale | 05-strategy-and-roadmap · 06-simulation-engine · 07-vcd-format · 08-timescale-and-timing |
| 테스트·진단·산출물 | 09-testing-and-verification · 13-diagnostics-and-logging · [14-staged-artifacts](docs/preview/14-staged-artifacts.md) |
| 에러코드·SchemaHash·IR백본 freeze | [15-error-code-reference](docs/preview/15-error-code-reference.md) · [16-schema-hash-spec](docs/preview/16-schema-hash-spec.md) · [17-sim-ir-ir-backbone-freeze](docs/preview/17-sim-ir-ir-backbone-freeze.md) |
| HDL 레퍼런스 | `docs/preview/hdl-reference/{verilog,systemverilog,vhdl,system-tasks}` |
| 가속 분석·실측 | [18-acceleration-analysis](docs/preview/18-acceleration-analysis.md) (§실측 = profile-driven ~6x 이력) |
| **향후 과제·로드맵** | **[ROADMAP](docs/ROADMAP.md)** · 잔여작업 트래커 [REMAINING_WORK](docs/REMAINING_WORK.md) |
| 개발 이력 (슬라이스별) | [DEVLOG](docs/DEVLOG.md) (Stage C VM ~ 51탄 + §누적 상태 로그=옛 상태 블록 이관분) |
| 구현 계획 | `docs/superpowers/plans/` (PR1-B·PR2·M3 · [Stage C 바이트코드 VM](docs/superpowers/plans/2026-06-06-bytecode-vm-stage-c.md)) |

## 개발 주의

- **동결(freeze) 타입은 verbatim.** sim-ir 직렬화 타입 형상은 SchemaHash로 동결됨 — 필드 추가/삭제/재배열은 루트 해시를 flip시켜 전 `.velab` 무효화. 의도적 변경만(format_version bump 동반).
- **sim-ir cross-type 필드는 `sim_ir::Foo`로 FQ spelling** (`extern crate self as sim_ir`). bare 참조는 body-ref⊆key 가드(`tests/body_refs.rs`)가 거부.
- **멀티세션은 `git worktree`로 분리.** 이 모노레포를 여러 Claude 세션이 단일 체크아웃 공유 시 브랜치/HEAD가 발밑에서 바뀌어 커밋이 strand됨(실제 발생). 커밋은 항상 `git add 016_claude_rtl/<path>`로 스코프(타 프로젝트 누수 금지).

# 목적
- **vitamin(vita/vcmp/velab/vrun)** 완성 — icarus·verilator·xcelium·vcs급 오픈소스 RTL 시뮬레이터.
- **최우선 원칙 = correct-or-loud**: silent-wrong(틀린 출력·무에러)이 최악. honest-loud(E3009 거부)은 항상 안전. 검증 불가는 구현하지 말 것.

# 매 반복 = 잔여 1개 슬라이스를 끝까지(그라운딩→구현→적대리뷰→문서→커밋·푸시). 아래 순서를 그대로.

## 0. 컨텍스트 고정값 (재발견 금지)
- 빌드/게이트: `cargo build -p cli --locked` (바이너리 `target/debug/vita`) · 전체 게이트 `cargo test --workspace --locked` · `cargo clippy --workspace --all-targets --locked -- -D warnings` · `cargo fmt --all -- --check`.
- 오라클: **iverilog 13.0** `/opt/homebrew/bin/iverilog` (compile `iverilog -g2012 -o x.vvp f.v`, run `vvp x.vvp`). ⚠️ iverilog는 **concurrent assertion(SVA)·clocking block을 거부** → 그 영역은 **vita-내부 차분**(검증된 등가식과 비교)이 teeth. macOS엔 `timeout` 없음 → SV 안에 `#N $finish` 워치독.
- 불변식: **format_version 19**(루트 SchemaHash=sim-ir SimIr). frozen sim-ir 형상 변경만 bump(드묾·골든 재생성 동반). 그 외 전부 **IR-0**(엔진/elaborate-local·사이드카). **3-OS byte-identical**이 perf보다 우선. ⚠️ **AST(hdl-ast) 필드 추가 = `.vu` 스키마 해시만 re-pin**(별개 게이트 `hdl-ast/tests/schema_hash.rs` EXPECTED+doc줄 갱신)·format_version은 불변(sim-ir 아님). 실패하면 새 해시로 갱신=의도된 변경. **⚠️ AST 노드 VALUE만 변경(필드 추가 없이 파서가 채우는 값만 수정=예: tf-port 타입 상속 §4.5.38)=schema SHAPE 불변→`.vu`/format 둘 다 불변·IR-0**(schema hash=구조 형상이지 채워진 값 아님; 파서-value fix는 가장 안전한 silent-wrong 수정 부류).
- 정본 문서: SPEC=`docs/preview/` · 잔여/전략=`docs/ROADMAP.md` · 이력=`docs/DEVLOG.md`.

## 1. 아이템 선택 (ROADMAP에서 1개)
- `docs/ROADMAP.md` 잔여 항목 확인(특히 §4.5.x의 발굴된 pre-existing silent-wrong·개발예정 목록).
- **선택 전 후보를 오라클로 재현 검증**: 문서화된 "미수정 버그"가 이미 오라클과 일치할 수 있음(misdiagnosed·이전 슬라이스가 부수 수정·특정 트리거 필요). 재현 안 되면 비목표로 기록하고 다음 후보로. (이번 루프: `@*` t0·`@*` Comb self-write·#5 mixed-edge가 전부 이미 일치였음.)
- **recorded 후보는 mechanism-level로 재그라운딩—기록된 증상이 실제 버그보다 좁을 수 있음**(이번 루프 §4.5.21): "32-bit decimal 필드폭 10 vs 11"로 기록됐으나 **param 공간(폭×signedness) sweep**이 실제 근본=**signed `%d`가 sign column 미예약**(8/16/32 전 signed 영향·32-bit specific 아님)임을 드러냄. 후보 착수 시 기록 framing을 믿지 말고 **변수 공간을 sweep**(width·signedness·value·edge)해 정확한 룰을 핀→슬라이스가 좁게 기록됐어도 fix는 일반해(localized 1-fn)로 더 큰 가치.
- **우선순위: ① 오라클 있는 CRITICAL silent-wrong > ② 오라클 있는 기능 갭(loud→supported) > ③ 전제조건 충족된 honest-loud 승격.** 오라클 없음·전제조건 미충족(예: arm-slot 추적·deferred hier-edge-sens 필요)은 **건드리지 말고 honest-loud 유지**.
- **쉬운 silent-wrong 소진 시 ②(loud→supported)로 전환**: vita가 loud-reject하나 iverilog가 지원하는 흔한 구문을 오라클로 찾을 것(예: multi-driver tristate). **loud→supported는 additive라 byte-identity가 강력**(거부되던 구문은 기존 디자인에 전무)=silent-wrong 수정보다 저위험. ⚠️ **단, elaborate가 허용을 시작하는 집합 = 엔진이 실제 처리하는 집합이 EXACTLY 동일해야 함**(불일치=허용됐으나 미처리=last-wins silent-wrong). 둘의 eligibility 술어를 같게 쓰고 적대 리뷰로 집합 동일성을 검증.
- **feature가 '거부된다'고 새 인프라 슬라이스로 잡기 전에 EXISTING 부분지원을 grep**(이번 루프 named event=최대 교훈): vita가 `@e`를 parse-reject했으나 grep이 event 인프라 **이미 완비**(`NetVarKind::Event`·`->`=`parse_trigger_stmt`·`@(e)`=`parse_event_stmt`·event-as-value 이미 loud)를 드러냄→유일 갭이 **bare 괄호생략 1개**=engine 신규 0의 1-site 파서 fix(moderate feature→tiny additive로 축소). **실패하는 EXACT sub-form만 격리**해 probe(`@e` vs `@(e)`)→어느 sub-form이 정확히 막혔는지 핀하고 기존 인프라와 대조(`grep 'fn parse_<feat>'`·관련 AST kind·관련 토큰). 갭은 'feature 전체'가 아니라 'sub-form 1개'인 경우가 흔함=슬라이스 대폭 축소.
- **'지원되는' 듯한(무에러) system task도 silent-wrong일 수 있다=empty/blank/wrong 출력을 적극 의심**(이번 루프 `$swrite`): vita 진단 비대칭(unknown `$task`=W3056 **silent-skip** vs unknown `$func`=E3009-loud) 때문에 미매핑 `$task`는 조용히 no-op→타깃 unchanged. ② probe 시 vita가 거부 안 하나 iverilog와 출력이 다른(특히 **빈 문자열·blank·default 값**) 케이스 = priority-① silent-wrong 후보(②보다 우선). `grep map_systask`로 미매핑 task 열거.
- **"byte-identity 리스크가 커 보여 defer"는 측정으로 검증**: 우려가 과대평가일 수 있음(이번 루프: narrow-posedge 와이드화의 "다수 골든 flip" 우려가 측정상 미현실화=t0 무영향·전체 스위트 무회귀). defer 전 실제 영향 케이스를 오라클로 핀.
- **후보 탐색은 time-box(3~4개 probe)·쉬운 후보 소진 시 pre-grounded deferred 후보로 직행**(이번 루프 교훈): 한 반복서 8+ 후보 probing은 컨텍스트 낭비. ROADMAP §4.5.x deferred 목록(assignment pattern·function-module-var-scoping 등)을 **먼저** 확인—이미 그라운딩된 후보가 있으면 새 probing 없이 그 전용 슬라이스에 착수. probing이 **deep/high-stakes 코어 변경**(예: function 핸들링·스케줄러)을 드러내면 **즉시 pre-grounded 후보로 기록·defer**(rush 금지=불완전 코어 변경은 silent-wrong 위험). 단, **연속 2회 defer는 신호**=다음 반복은 deferred 후보 1개에 처음부터 전념(그라운딩→구현 전용 반복). **defer-to-전용-슬라이스 ≠ fix가 크다**(이번 루프 교훈): pre-grounded 후보에 착수하면 root-cause를 **정확한 결정지점**(어느 함수·어느 조건문)까지 추적—'deep해 보이던' 코어 변경(예: function 핸들링)이 실제론 **1-line 일관성 수정**일 수 있음(이번 루프 function-module-var=`walk_scopes_key`의 `$func$` transparency 1줄 추가). fix 크기는 root-cause 후에만 추정. **pre-grounded NEXT 후보(미착수)**: **🏁 typedef-recognition family 완료(§4.5.40~53, #49 enum base로 종결)**=쉬운 typedef ② 소진→다음 반복은 **새 영역으로 branch-out**(§5 cluster-완결-후-branch-out 교훈). **(A·새 probe 필요)** 미탐색 SV 구문 영역을 fresh probe(streaming `{<<{}}`·`assign`-pattern 변종·interface modport·`bind`·`$cast` 동적·assoc-array 메서드 등 30+ sweep서 DIFFER 찾기). **(B·deep, pre-grounded)** context-width가 inline body NON-fill 서브식 미전파(§4.5.42 잔여=`f=a<<2`→`0c`·`f16=t1*t2`→`0002`·engine net-write 메커니즘 부재=deep). (typedef-family 전체 ✅: ~~for-init §4.5.43~~·~~struct/union member §4.5.48~~·~~chained §4.5.49~~·~~tf-port §4.5.50~~·~~body DEF §4.5.51~~·~~enum base §4.5.53~~; 부수 ① ~~var_struct shadow §4.5.52~~[#47발굴→#48수정]).
- **코어 견고(probe가 vita-ahead/no-oracle/SAME뿐)+잔여 갭이 전부 moderate-risk(deep·byte-identity flip·murky semantics·cosmetic·전용-인프라 필요)면, 억지 code 슬라이스 대신 thorough 그라운딩-기록 + LOOPROMPT 정리 iteration도 valid**(이번 루프 #29: struct `'{}`=`StructLayout`이 parser-only라 elaborate intercept 불가·decl-init은 `pending_var_inits` 경유=statement/decl-init scope 불일치=moderate 전용 슬라이스→그라운딩만 ROADMAP 기록). rush 금지(§2 '불완전 슬라이스보다 깨끗한 defer')의 iteration-레벨 적용=불완전 code보다 record/defer 우월. ⚠️ **probe로 코어 견고+잔여 moderate를 실제 확인한 뒤에만**(쉬운 ②/① 갭 남았는데 회피는 금지)·그라운딩은 다음 iteration이 즉시 착수 가능하게 정확한 난점(어느 함수·어느 scope 불일치)까지 기록. **그라운딩-기록은 다음 iteration이 prerequisite silent-wrong을 발굴하게 만든다=defer가 낭비 아님**(루프 #30이 #29의 defer 검증): deferred moderate feature(struct `'{}`)의 desugar를 핀하다(=`s='{e0,e1}`≡`{w0'(e0),w1'(e1)}`) **그 desugar의 prerequisite인 좁은 silent-wrong**(cast `8'('1)`=01)을 발굴—이건 그 자체로 **오라클 있는 ① 슬라이스**(moderate feature보다 우선·저위험)이자 **deferred feature를 unblock**. 즉 moderate feature 직접 착수가 막히면 그 desugar를 building-block 단위로 핀→각 block(cast·concat·width)이 독립 ① 슬라이스가 됨(큰 feature=작은 prerequisite fix들의 합).
- **deep/high-stakes resolution fix가 위험>가치면 detection-only honest-loud로 대체—correct-or-loud의 escape hatch**(이번 루프 §4.5.18 block-local scope leak=최대 전략 교훈): silent-wrong의 correct fix가 broad(5+ 등록경로)·fragile(slot 모델·`lower_stmt` per-block scope push/pop·`walk_scopes_key` 상호작용)해 **신규 silent-wrong 위험이 크면**, resolution을 바꾸지 말고 **read-only detection 패스로 그 패턴을 loud(E3009) 승격**. detection-only는 **구조적으로 신규 silent-wrong 불가능**(에러만 추가·해소 무변경)=최악도 false-positive(valid 거부=재review로 잡음)나 false-negative(pre-existing silent-wrong 유지=무회귀)뿐. full correct fix는 documented follow-on. **'연속 2회 defer는 신호=전념'의 올바른 해석**: 전용 슬라이스 착수=정확한 root-cause 그라운딩 + SAFE 해소(honest-loud 포함)이지 deep fix를 rush하란 뜻 아님(rush=신규 silent-wrong 위험). 패스는 기존 인프라 재사용으로 경량화(`expr_reads_ident` 등)·prescan 1-hook(Func/Task/Proc body).
- **probe의 'DIFFER'는 4-way 분류 후에만 슬라이스 대상—오분류=슬라이스 낭비**(이번 루프 port-conn sweep): 차이를 (a)**오라클 있는 진짜 갭**[`.name`/`.*` parse-reject] (b)**no-oracle**=iverilog도 거부[streaming concat=`sorry: not supported`]→honest-loud 이미 correct·skip (c)**vita-ahead**=vita 지원·iverilog 거부[`$typename`]→silent-wrong 아님·유지(오라클 없어 검증 불가=손대지 말 것) (d)**harness-formatting-only**=vita `simulation ended (Finish)` vs iverilog `$finish called at N`·iverilog `vvp.tgt sorry: ... ignored` 경고→**정규화 후 재비교**(실제 `$display`/`$monitor` 출력만 비교)로 분류. **정규화 없이 (d)를 갭으로 오인하면 첫 batch 전체가 가짜 DIFFER**(이번 루프 urandom_range·unique-case가 정규화 후 SAME). probe harness는 처음부터 finish/sorry 라인 strip하는 `norm()` 필터 내장. (a)만 착수. **(e)iverilog가 자기 자신과 INCONSISTENT면 vita의 spec-correct가 target—iverilog 아님**(루프 #43): 같은 식이 두 position서 다른 답 주면(`'1` default-arg=iverilog 1-bit self-det vs explicit-actual=formal 폭·signed-cast default=sign drop vs explicit=sign-extend) iverilog 버그→vita가 spec+자기 일관이면 그게 correct·DIFFER는 non-target. 판별=같은 식을 explicit actual로도 줘 iverilog 답이 바뀌나 확인(바뀌면 iverilog self-inconsistent).
- **갭이 관련 기능 cluster면 work가 어디 떨어지는지(risk-tier)로 분할**(이번 루프 port-conn=`.name`+`.*`): 같은 cluster(포트연결 편의구문)라도 `.name`=**파서-only desugar**(`.port`≡`.port(port)`=clean·additive·elaborate 무변경) vs `.*`=**elaborate-time 포트 introspection 필요**(미연결 포트 전수를 same-name 자동연결=callee 포트리스트 알아야 함)=risk-tier 상이→**저위험(파서) 먼저 착수·고위험(elaborate) 별개 슬라이스 기록**. cluster를 한 슬라이스에 욱여넣지 말 것(format cluster는 동일 fmt 함수라 묶였지만, 여기처럼 파이프라인 단계가 다르면 분할이 옳음).
- 선택 즉시 격리 브랜치: `git checkout -b feat-<slug>` (main에서 직접 구현 금지). **첫 EDIT 전에 브랜치 생성=절차 1순위**(루프 #48 슬립: 그라운딩 몰입하다 브랜치 없이 main서 편집→커밋 전 발견·`git checkout -b`가 uncommitted 변경 carry해 무손실 복구했으나, 커밋까지 갔으면 strand 위험). 그라운딩(probe)은 read-only라 브랜치 전에 해도 무방하나 **소스 첫 수정 직전 `git rev-parse --abbrev-ref HEAD`로 main 아님을 확인**.

## 2. 사전 리뷰 = 오라클 그라운딩 (브레인스토밍)
- **버그/기능을 오라클로 라이브 재현**: 최소 SV repro 여러 개를 iverilog와 vita 양쪽에 돌려 **정확한 IEEE 규칙을 핀**(예측 금지·측정). 엣지/x·z/멀티비트/NBA/경계까지 변형 probe.
- **murky해 보이는 후보는 stimulus 경로를 바꿔 재그라운딩—murkiness가 feature가 아닌 probe artifact일 수 있음**(이번 루프 §4.5.22 %0s): 직전 반복서 embedded-null이 깨져 보였으나(`[A Bc]` vs `[  Bc]`) 그건 **part-select 대입**(`s[8*4:8*3+1]="A"`)의 별개 버그였고, **whole-assign**(`s=32'h41004263`)으로 재그라운딩하니 embedded-null MATCH=feature는 clean(leading-null strip만). 같은 값을 다른 경로(whole-assign vs part-select·리터럴 vs 변수·직접 vs 함수반환)로 만들어 차이가 feature냐 stimulus냐 격리.
- **multi-construct repro가 틀리면 각 construct/path를 독립 테스트로 BISECT해 정확한 failing 조합을 핀**(루프 #35 array-init): `int a[4]='{1,2,3,4}; foreach() sum`=0이 array-decl-init 버그냐 foreach 버그냐를 **각각 독립 probe**(foreach-only=정상·array-decl-init-only=0)로 격리→array-decl-init 확정. 이어 **축별 bisect**: scope(module-scope=정상 vs block-local=버그)·path(statement `a='{…}`=정상 vs decl-init=버그)→정확한 failing 조합=**block-local + decl-init + unpacked-array**. 한 축씩 변화시켜 working/broken 경계를 좁히면 fix 지점이 명확(여기선 `hoist_block_local_nets` vs `collect_var_init_drivers` 비대칭). 여러 construct가 얽힌 repro를 통째로 디버그하지 말 것=어느 construct가 범인인지 먼저 bisect.
- 관련 코드(파이프라인 해당 단계)를 정독 → **byte-identity 논증**(이 변경이 비대상 디자인서 왜 무영향인지)을 먼저 세움. 못 세우면 범위를 좁히거나 honest-loud.
- **inline-path silent-wrong은 frame-path(`automatic` 버전)를 오라클-보조 정답 레퍼런스로**(루프 #38 inline-func-ret-width): 같은 함수의 `function automatic …` 버전을 vita에 돌리면 frame/net path가 engine resize로 CORRECT 동작(inline은 순수 ExprId·net write 無라 §10.7 resize 누락)→정답 핀(+잠재 fix 전략=route-to-frame). 단 routing은 `@*` sensitivity·VCD·timing 변경=value-resize 슬라이스엔 고위험→**byte-identity 예측 가능한 surgical fix 우선**. **width/sign-resize fix의 byte-identity는 `ir_bits_of(natural rhs) vs 선언 폭`으로 사전 예측**(no-op 조건=rw==w && sign 일치): `a+b`(8-bit)=`max(8,8)`=8=선언 8→무변경(common case)·`f=42`(32-bit literal)→선언 8=truncate=변경(=fix). 구현 전 `ir_bits_of` 룰(Add/Mul=max-operand·shift=left-operand만·literal=32)로 어느 케이스가 no-op인지 핀→high-blast-radius 변경 de-risk. **resize는 RHS 자기 sign으로 extend·TARGET sign으로 stamp**(루프 #38 핀: signed local widen=sign-extend·unsigned→signed wide=zero-extend후 $signed; `lower_prim_cast`가 모델).
- **format/rendering 서브시스템서 silent-wrong 1개 발견 시 클러스터를 의심—전 spec×flag 매트릭스를 sweep**(이번 루프 string-format 3연속: signed-fieldwidth→`%0s`→`%Ns`): 같은 fmt 코드가 `%d/%s/%h`×width×0-flag×signedness×x/z를 처리하므로 한 버그는 인접 버그의 신호. 매트릭스(spec letter × field-width{none/0/N} × 타입{signed/unsigned/packed/string/literal} × value{0/min/max/x/z/overflow})를 오라클로 전수 차분→cluster를 한 번에 발굴. **연속 슬라이스는 직전 그라운딩이 다음 오라클을 푼다**(직전 루프 `%05s`=오라클 없어 base 유지했으나 이번 `%Ns` 그라운딩이 `%05s`=strip+pad-to-5 오라클 제공). rendered byte-string의 column 폭은 `chars().count()`(byte당 1 char) 사용—`len()`(UTF-8 byte)은 ≥0x80 byte서 overcount. **단 family를 batch-handle 시 멤버가 uniform하다 가정 금지—각 멤버를 개별 오라클 핀**(이번 루프 uppercase: `%X`≡`%x`·`%S`/`%C`/`%M`=별칭·`%E`/`%G`=exponent 대문자·**`%F`=비대문자**[inf/nan 소문자 유지]). "uppercase=전부 대문자화" 같은 일괄 규칙은 differential이 깸(`%F` over-uppercase 발굴)→spec letter마다 오라클 동작 확인 후 가드 좁힘. **cluster 소진 신호=in-cluster 마지막 갭+인접 영역 probe가 모두 clean/done**(이번 루프 format 5연속[signed-fieldwidth→`%0s`→`%Ns`→uppercase→`%v`] 완결·non-format 6 probe 전부 SAME)→다음 반복은 **새 영역으로 branch-out 또는 deferred deep 후보**(§4.5.18 frame inner-block collision=14탄부터 deferred). 한 cluster에 무한 머물지 말 것. **cluster를 여러 iteration에 걸쳐 완결하는 것도 valid(각 piece가 oracle-backed+자연 완결일 때)이나 완결 직후 MUST branch-out**(루프 #30~33 struct `'{}`=4-iteration 완결[cast-fill prereq→scalar→cont/force/for→array-elem]·**cluster DONE→다음 iteration은 struct 회피, 새 영역 probe 필수**). **clean 슬라이스의 differential 광범위 probe도 인접 pre-existing 버그 수확**(루프 #33 task comma-typed port `task t(input logic[7:0] x,y)`가 `y`=1-bit silent-wrong[function port는 정상]=별개 ① 후보 ROADMAP 기록·fold 금지=§6).
- **desugar 슬라이스는 인프라 구축 전에 desugar EXPRESSION 자체를 sim서 검증**(이번 루프 enum `.name()`): 소스 구문뿐 아니라 desugar가 생성할 표현식(예: string/value ternary chain)을 vita에 직접 돌려 결과 정확성 확인. building block이 '돈다'고 의미가 맞는 건 아님(packed-string ternary는 짧은 라벨을 패딩→iverilog dynamic string과 불일치)→불일치 메서드/형태는 **honest-loud로 격리**(메서드-패밀리는 per-method 오라클 지원 기준 분할; 5/6 정확+1 loud ≫ 6/6 중 1 silent-wrong).
- **context-determined 기능(의미가 타깃 타입에 의존)은 ALL 변종을 구현 전 오라클로 전수 핀**(이번 루프 assignment pattern `'{…}` 중단·revert): "packed='{…}≡concat" 같은 **예측을 측정 없이 구현에 착수→라이브 차분서 오류**(`'{1,0,1,1}` on `logic[3:0]`=각 1비트 `1011`, concat-of-int=`0001`). target packed-width sizing(`W/N`)·unpacked array 방향·비-assign-context loud·미파싱 변종(repl/default/named) 전부 **사전 핀**. 큰 의미공간이면 **전용 슬라이스로 분리·이번 반복 비목표**로 기록(rush 금지=불완전 슬라이스보다 깨끗한 defer가 우월).
- 계획을 scratchpad에 durable 기록(파일·메커니즘·테스트 매트릭스·loud 유지선).

## 3. 구현
- 가능한 IR-0. 단일 write/엣지 등 **공통경로엔 청크포인트가 하나인지 먼저 확인**(인터프리터+VM 공유 funnel 여부). 모든 동치 경로를 빠짐없이 커버.
- 비-대상 디자인은 byte-identical 유지(가드/사이드카=값 다를 때만).
- **순수 파서 desugar(기존 AST 노드 재사용)는 최저위험 loud→supported**(inc/compound·break/continue): 신규 구문이 기존 구문의 shorthand면(`i+=e`≡`i=i+e`·`i++`≡`i=i+1`) elaborate/IR 손대지 말고 **파서서 기존 `Stmt::Blocking` 등으로 desugar**→AST/sim-ir/`.vu` 해시 전부 불변(신규 AST 타입 0). lvalue가 양변에 등장하면 `lvalue_to_expr`(역 `expr_to_lvalue`)로 rhs 합성·precedence는 operand를 `expr(0)` 전체 파스 후 wrap. 부수효과 expr 형태(`a=i++`)는 Pratt expr 파서에 미추가→loud 유지(scope 분리).
- **feature를 1-instance→N-instance로 확장 = 기존 per-unit lowering을 DISCRIMINATOR-arg로 파라미터화 + DISCRIMINATOR로 BRANCH(restructure 금지=byte-identity)**(루프 #40 multi-dim foreach): single-index `foreach(a[i])`=`arr.first/next(idx)`(engine iterator over dim 1)였음→multi-dim은 **dimension arg 추가**(`arr.first/next(idx, DIM)`)+elaborate handler를 dim-aware로(`net_dim_extents.get(dim-1)`). **핵심 byte-identity 기법**: 기존 경로를 재구성하지 말고 **discriminator(여기선 comma)에서 BRANCH**—comma 없으면 기존 single-index 코드 verbatim(1-arg=dim 1=`.get(0)`≡`.first()`=증명가능 동치)·comma면 신규 multi-dim 경로. soundness가 "comma 없을 때 기존 AST 동일" 전수증명. N-instance가 nesting이면(multi-dim=nested loop) **leftmost=outermost**·각 레벨 synth name은 variant간 distinct prefix(single=`__foreach_st_<L>`·multi=`__foreach_st<N>_<L>`). 신규 lowering이 non-기본 instance(dynamic array·dim 초과)에 도달하면 **honest-loud**(fixed-array만 per-dim bounds).
- **1-D→N-D 확장 전에 기존 OFFSET/INDEX 테이블이 이미 N-D를 처리하는지 확인—그러면 확장=입력을 그 테이블에 맞게 flatten뿐 + DIRECTION은 정확히 1곳서만 적용**(루프 #42 multi-dim array pattern): 1-D array `'{…}` 패턴이 multi-dim서 loud였으나 **`residual_word_offsets`가 이미 row-major multi-dim flat offset 생성**(per-dim asc/desc 존중)→확장=신규 `flatten_assign_pattern`(중첩 패턴을 declaration-order leaf로 flatten·각 레벨 count 검증)뿐, offset 계산 재사용. **핵심 silent-wrong 회피=DIRECTION 1곳 적용**: flatten은 **direction-AGNOSTIC**(순수 declaration order)·offset table만 desc 적용→leaf[k]↔offset[k] 정렬. flatten이 desc 또 적용하면 **double-flip silent-wrong**(descending서 값 오배치). 확장 전 `grep`로 기존 stride/offset/coord 헬퍼가 multi-dim 인자를 받는지 확인—받으면 코어 재구현 0.
- **control-flow 구문(loop/break/continue/early-exit)은 'intuitive' 의미 가정 금지—오라클로 break/continue 타깃을 핀**(루프 #40 multi-dim foreach=silent-wrong 회피): `foreach(a[i,j]) break`이 "전체 foreach exit" 같지만 **iverilog nested-for desugar는 break=INNERMOST 루프만 exit**(`a[3][3]` break@(1,1)=7 not 4)·continue=innermost advance. 가정대로 "전체 exit"로 구현했으면 silent-wrong. desugar 슬라이스서 break/continue/return/disable이 어느 스코프를 타깃하는지 **반드시 오라클 측정**(nested 케이스로)·nested면 innermost에만 `wrap_break` 적용·outer는 plain.
- **AST 노드 추가 전에 "기존 end-to-end 메커니즘이 이미 그 일을 하는지" 확인**(이번 루프 break/continue): control-flow도 기존 구문으로 표현되면 재사용=elaborate/engine 신규 경로 0. 예: `break`/`continue`는 `disable <label>`→Goto(파서+elaborate+engine 完備)로 desugar→IR/엔진 무변경(AST 노드+loop-target-stack 설계보다 우위). 사전에 `grep`로 등가 메커니즘(disable·goto·기존 desugar) 존재 확인.
- **미매핑 system task는 새 핸들러 작성 전에 의미상 근접한 기존 `SysTaskId` 핸들러로 라우팅 가능한지 확인**(이번 루프 `$swrite`→`$sformat`): 공유 포맷/eval 헬퍼(`format_args_str` 등)가 이미 원하는 동작($write-style append 등)을 하면 `$swrite`≡`$sformat`=**2-line 매핑**(이름→기존 SysTaskId+radix). 엔진 핸들러+공유 헬퍼를 **먼저 정독**해 '이미 그 일을 하는지' 확인 후 매핑(= §3 'AST 노드 추가 전 기존 메커니즘 확인'의 system-task판). arg 레이아웃(dest/fmt/radix 추출)이 기존 task와 호환되는지 전 변종 핀.
- **같은 구문의 N개 parser 변종은 ALL을 공유 헬퍼로 라우팅**(do-while 누락=silent-wrong·루프 #32 assign-RHS=완결 적용): 루프처럼 변종이 여러 파서 함수면(for/while/repeat/forever/foreach + **do-while**=별도 `parse_do_while`) `grep 'fn parse_'`로 전수 열거→하나라도 공유 헬퍼(`parse_loop_body`) 미경유 시 그 변종서 desugar 미적용=잘못된 타깃/드롭(do-while 안 break가 enclosing 루프 종료=silent-wrong). = §3 'ALL elaborate 컨텍스트 전수'의 **parser-변종판**. (do-while: soundness은 'honest-loud'로 분류했으나 differential이 중첩 시 silent-wrong 입증→differential 우선.) **'assign-RHS'는 ≥7-site construct-class**(루프 #32 struct `'{}`): blocking `=`·nonblocking `<=`·decl-init·continuous `assign`·procedural-continuous `assign`·`force`·for-init/step—파서 헬퍼(`maybe_*_rhs`)를 ALL site에 적용해야 일관(빠진 site=그 컨텍스트만 loud gap=혼란). **완결성 체크 = soundness 서브에이전트에게 'construct-class의 EVERY site 열거+각 hook 有無/필요 판정' 명시 의뢰**(루프 #32: soundness가 `parse_for_assign` missing 발굴=differential은 못 잡음[loud는 silent-wrong 아님→probe 무관심]·soundness의 site-enumeration이 MISSING-site 전담). **reviewer가 찾은 loud-gap이 동일 validated 메커니즘으로 닫히면(1-line·오라클 검증·byte-identity 확인) 같은 슬라이스서 fold**(defer 말 것=부분 feature 혼란 방지). = §4 differential(EXISTING path silent-wrong)↔soundness(MISSING path/site)의 또 다른 COMPLEMENTARY 축.
- **loud→supported 파서 fix는 오라클로 acceptance boundary를 전수 핀한 뒤 parse primitive를 고름**(이번 루프 bare `@e`): iverilog가 `@e`/`@clk`/`@u.s`/`@a[2]`=accept·`@posedge clk`(bare edge)/`@a+b`/`@a&&b`=reject임을 핀→이게 `expr_postfix()`(primary+postfix 참조, binary infix서 멈춤)를 고르게 함(full `expr(0)`이면 `@a+b`를 `@(a+b)`로 silently over-accept=iverilog 거부분 수용). **어느 primitive가 정확히 그 경계서 멈추는지 grep으로 확인**(`fn expr_postfix`/`fn expr_primary`/binding-power 표)한 뒤 선택. keyword guard도 경계의 일부(`posedge`=키워드→`is_ident()` false→bare edge 자동 loud).
- **identifier-led 모호 opener(decl `T name` vs statement `name …`)는 SECOND-token lookahead로 disambiguate—`<ident> <ident>`는 모호성 0 decl**(루프 #36 block-local typedef decl=② parse gap): user typedef명으로 시작하는 절차 블록 선언(`e x;`·`s_t s;`)이 E2002였음—decl 루프가 built-in 키워드만 인식. 핵심 disambiguation: `peek_at(1)`이 ident면 decl(어떤 statement도 `ident ident` 형태 없음)·`::`/`'`/`(`/`[`/`.`/`=`/`<=`/`++`면 statement→fall-through. **기존 recognizer를 미러하되**(module-item 파서의 `peek_typedef_name`→`parse_typed_decl`) **statement도 있는 컨텍스트(block/tf-body)엔 offset-1 guard 추가**(module-item엔 statement 無라 guard 불필요). **`self.typedefs`엔 타입명만**(enum-label·var는 별개 맵)=var/label 오인 불가. **검증 분담**: differential에게 **기존 RTL 모듈 byte-identity sweep**(statement 오인-grab=valid→parse-error regression 잡기)·soundness에게 **statement-opener의 offset-1 token 표 전수**(necessary&sufficient 증명)+**body-decl site 완전성**(begin/fork→block_body·task/func/class-method→tf_body, §3 line 45 ALL 변종). 순수 파서·AST 불변·IR-0·additive.
- **렉서 토큰 추가 시 logos longest-match를 prefix 공유 ALL 기존 토큰과 대조**(이번 루프 14 토큰): 다char 연산자 충돌 전수(SVA `|=>`/`|->` vs 신규 `|=`·`<<=` vs `<=`/`<<`/`<<<`·`>>>=` vs `>>>`). **필요한 모든 길이가 정의돼야** longest-match가 기존 스트림 보존(공백 없는 `a++b`만 신규 융합=iverilog도 loud=무해). SVA/unary-`+`/shift/reduction repro로 byte-identity 실측.
- **per-net 사이드카는 net 생성하는 ALL 경로에 populate**(이번 루프 CRITICAL): body decl·**ANSI `elaborate_ports`**·**non-ANSI `PortDecl` 루프**·heap-handle/dyn-array 분기가 전부 별개 add_net 사이트. body decl 한 곳만 채우면 `output wand` 등 포트 net이 사이드카 누락→default 처리로 silent-wrong. 구현 전 `grep add_net`로 사이트 전수 열거.
- **staged 경로(velab→vrun) 패리티는 필수, "한계로 문서화"는 금지**(정정): 엔진-facing 사이드카가 one-shot `vita`만 타고 staged서 드롭되면 = 경로별 결과 불일치 = silent-wrong. `StagedExtraSidecars` 14th `.velab` 트레일러에 **append-only**로 추가(struct 필드·`from_sidecars` clone·vrun apply 3곳)+`staged_extra_sidecars_wire_shape_is_pinned` 픽스처 갱신+`REGEN_GOLDEN=1`로 핀 해시 재생성. out-of-band 트레일러라 **format_version bump 불필요**(선례=clocking/ca_delays). staged 회귀 테스트=`$fatal`-on-wrong로 exit-code 검증(`cli::run_vcmp/velab/vrun` lib API). **단 기존 사이드카(예: `ca_delays`)로 desugar하면 staged는 무료**(이미 trailer에 있음).
- **"기존 path로 desugar" 슬라이스는 그 path가 도는 ALL elaborate 컨텍스트를 전수**(이번 루프 generate silent-wrong): 같은 desugar 함수(예: `elaborate_net_init_drivers`)가 module-item body 루프엔 연결됐어도 **generate-Logic phase·block-local 등 다른 컨텍스트의 dispatch arm이 누락**이면 그 컨텍스트서 driver가 silently drop. 신규 구문이 거기서 파스되면 pre-existing drop이 신규 silent-wrong로 노출. `grep '<desugar_fn>'`·`grep 'GenPhase::Logic'`로 호출 컨텍스트 전수 확인 → 누락 arm 추가(무delay pre-existing도 동시 수정됨).
- **파서/elaborate 공유 함수가 도는 ALL 스코프·caller 전수 점검—미처리=silent-drop/parse-error를 silent로 격하; SAFE 패턴=`allow_*: bool` scope-gate(검증 스코프만 true·나머지 loud=전부 지원보다 저위험)**(이번 루프 procedural delay-swallow·string decl init): (a) **새 구문 수용**: `parse_net_var`가 module/generate/block/func/task/class 6곳 호출→elaborate 실제 처리 스코프서만 수용되게 `allow_*` 스코핑(미처리 스코프서 silently 삼킴). (b) **GLOBAL gate(reject) 제거도 신규 수용=ALL caller 점검 trigger**: `elaborate_netvar_decl` 4-caller의 reject 제거→`collect_var_init_drivers`가 module.body만 스캔이라 module-body만 init 수집·나머지 3 스코프 net등록+silent-drop→`allow_x` scope-gate로 unverified=loud 격리. **+ kind-predicate가 신규 타입 누락=wrong-path silent**(String이 `netvar_kind_is_var` 누락→`elaborate_net_init_drivers` early-return 안 함→spurious continuous `assign s=init`이 procedural write 되돌림[`a="x";…a="ZZZ"`가 a=x]): 'var는 X·net은 Y' 갈림길 술어(`*_is_var`/`*_is_net`)에 신규 타입 추가했는지 `grep` 전수. = §1 eligibility-set parity의 스코프/caller판.
- **여러 pass서 수집해 ONE 리스트로 drain하는 deferred init/assign은 수집 순서=의존성 순서여야—earlier-pass 수집 init이 later-pass 의존성 읽으면 t0서 unset default 봄**(이번 루프 block-local string init): block-local string init(`hoist_block_local_nets`=이른 pass)이 module string(`collect_var_init_drivers`=늦은 pass)을 읽는데 synth t0 `initial`서 block-local 대입이 module보다 **먼저** drain→module 아직 empty→`b=""` silent-wrong. 수정=**별도 카테고리 리스트**(`pending_block_local_string_inits`)로 모아 producer(module init) **후** append=drain 순서 교정(기존 카테고리 경로 불변=byte-identity·per-instance `mem::take`=cross-instance leak 방지). pending/deferred를 ONE 리스트에 합칠 때 'A가 B 읽나?'→producer-before-consumer 보장. **proven 메커니즘 재사용 슬라이스도 cross-scope 의존성은 새로 검증**(block-local INT init이 이미 동작했어도 string은 module-read 노출=int는 const-fold라 가려졌던 순서버그를 string이 드러냄).
- **새 init surface는 기존 runtime ops로 flush서 EXPAND + 단일 DECLARATION-ORDERED 리스트 유지(별도 리스트=순서 silent-wrong)**(루프 #41 queue/dyn decl-init): whole-value init이 없는 핸들(queue/dyn array)의 `'{…}` decl-init을 **var-init flush서 기존 runtime ops로 expand**(queue=`push_back` 시퀀스·dyn=`new[N]`+element-write=엔진 신규 0). **핵심 순서 교훈**: decl-init을 scalar와 **별도 리스트**로 모아 scalar 후 flush하면 **scalar가 선행 queue `.size()` 읽을 때 0**(silent-wrong)→**단일 `pending_var_inits`(declaration-ordered)에 두고 flush가 `dyn_handle`로 감지해 그 자리서 expand**=양방향 cross-read 정확(scalar→queue·queue→scalar 둘 다). 별도 리스트 분리는 한 방향만 고치고 역방향 깸→단일 ordered 리스트가 정답.
- **scope-gated ALLOW는 desugar가 실제 도는 scope와 정확히 일치해야(불일치=silent-DROP)**(루프 #41=양 reviewer CONVERGED=고신뢰 critical): decl rejection을 relax해 init 허용 시, **허용 scope ⊋ desugar-실행 scope면 그 차집합서 init이 silently 드롭**(루프 #41: 모든 scope서 허용했으나 desugar(flush)는 module-body+block-local만 실행→generate/interface body decl-init silent-drop). 수정=**desugar 도는 scope를 표식하는 기존 flag(`allow_string_init`)로 ALLOW 게이트**(string init이 같은 이유로 그 flag 사용=선례). = eligibility-set parity의 SCOPE판(ALLOW-set ≡ PROCESS-set). 첫 설계가 양 reviewer 공통 지적이면 patch 말고 **재설계**(단일 ordered 리스트 재설계가 순서+scope 2 findings 동시 해결+block-local 보너스).
- **"collect→key로 match-apply" 패턴(override/binding 수집 후 적용)은 consumption-tracking 필수—unmatched entry=silent-skip silent-wrong**(이번 루프 defparam): override를 map에 모아 instance bind 시 `.get(key)`로 적용하면, 어떤 instance도 key와 안 맞는 entry(typo·array `u.N` no-index)는 **영구 잔존·무진단**=사용자 의도(override) 무효+무에러=silent. 수정 패턴=**`.remove`(consume)+전 적용 후 leftover entry를 loud/warn**. **warn vs error는 오라클에 맞춤**: output이 이미 correct(unmatched override는 default 유지=iverilog도 default)고 iverilog가 **warn+continue**면 **warn**(error는 오라클보다 strict=valid 디자인 halt). differential("output 일치=clean")과 soundness("silent-skip=wrong")가 갈리면 **오라클의 실제 동작**(warn? error? default?)을 측정해 레벨 결정.
- **comma-list서 STICKY 속성을 파서가 ALL 스레드해야—하나만 propagate=나머지 silent-default**(루프 #34 tf-port=① silent-wrong): port/decl 리스트(`task t(input logic[7:0] x, y)`·`reg [7:0] a, b`)서 bare `,name`은 직전 항목의 **방향+타입(net_or_var/sign/range) 둘 다** 상속해야(IEEE §13.3)하나 vita가 **방향만 상속·타입 미상속**→`y`=1-bit silent-default(호출인자 low-bit truncation). **fix=`inherited_attrs` 전체를 루프서 스레드**(`dir_present || type_present`=fresh+propagate-onward·bare=inherit·방향 재등장=타입 RESET 등 정확 규칙은 오라클로 핀). comma-list 파서 손댈 때 **모든 sticky 속성 열거**(dir·type·sign·range)—한 축만 보면 나머지 축 버그 놓침. **asymmetry tell의 변종**(루프 #34: function은 정상·task만 버그인데 **둘이 같은 파서 공유**→파서 fix가 둘 다 교정[function은 elaborate width-infer가 보상하던 것]·but 보상-경로[function]는 **byte-identity 필수 검증**[보상이 fix와 충돌 가능]). **helper 분해 시 대수동일 증명**(`signed_eff`=`opt_signed().unwrap_or(atom_default)`를 explicit_signed 캡처 위해 분해→soundness가 unchanged-path 대수동일 확인=byte-identity).
- **resolved-type(TypeInfo)를 기존 경로의 필드에 매핑해 feature 확장 = (1)전 sub-kind가 기존 arm과 PER-CASE 동일 + (2)필드가 표현 못하는 sub-case는 LOUD**(루프 #37 function return-type typedef): typedef 반환타입(`function b_t f`)을 헤더 반환필드(range/signed/ret_type/ret_two_state)에 매핑—built-in 키워드 arm과 **전 NetVarKind(vector/int/byte/enum/struct/time…) per-case 동일** soundness 전수증명(time만 ret_type Implicit vs Time이나 downstream width/sign 동일=benign divergence는 OK·단 flag). **핵심: 필드가 표현 못하는 sub-case=silent-wrong**—`typedef logic[3:0][7:0] m_t` 다차원 packed가 `info.packed` drop→4-bit 반환(should 32)=**§4.5.39 broadened-reach→loud 교훈 그대로**(`!info.packed.is_empty()`=loud). **이 교훈이 soundness 체크리스트에 있어 #37서 실제 발굴**=line 64 검증. **moderate ② 후보 여럿이면 VALUE(흔함)로 골라 즉시 실행**(과도 deliberation 금지·#37=function-return이 for-init/`==?`보다 common→선택). differential의 'pre-existing vs introduced'는 **built-in 등가구문과 byte-identical인지**로 판별(루프 #37 logic[3:0] truncation=built-in도 동일=pre-existing inline-return-width quirk·별개 ① 기록).
- **`<T> <name>` typedef-recognition은 RECURRING family—NEW parse-context마다 같은 갭(built-in 키워드만 인식)+같은 fix(shared-tail refactor+disambig arm)**(🏁 **family 완료 §4.5.40~53**: 루프 #36 block-local decl·#41 function return·#43 for-init·#44 struct/union member·#45 chained typedef·#46 tf-port·#47 body typedef DEF·#49 enum base): 타입 선언을 파싱하는 모든 context(block decl·function ret·**for-init**·**struct/union member**[#44 ✅=공유 `parse_struct_member_type`를 struct+union 루프 둘 다 경유=§4.5.36 ALL-variants]·**chained typedef `typedef base_t alias_t`**[#45 ✅=typedef-PARSER판=base 등록 MIRROR]·**tf-port 타입**(`task t(my_t x)`)[#46 ✅=ANSI(`parse_tf_port`)+non-ANSI(`parse_tf_port_decl_into`) 양 파서가 공유 `try_tf_port_typedef` 경유=§4.5.36 ALL-variants 또 적용]·**enum base 타입**(`typedef enum b_t {…}`)[#49 ✅=unsigned vector typedef만 `info.range`로 base·signed/atom=honest-loud(built-in도 signed base 드롭=별개)]·**body typedef DEFINITION**(`function f; typedef int t; …`)[#47 ✅=USE 아닌 DEFINITION을 body서 허용=`parse_typedef` 호출+노드 discard·**lexical scope=snapshot/restore**, 아래 별도 교훈]·**enum base 타입**(`typedef enum b_t {…}`)[미착수=다음 후보])가 독립적으로 `net_var_kind()`만 보고 typedef명 미인식일 수 있음=각각 ② loud→supported 갭. probe 시 built-in version이 동작하면(`for(int i=…)` OK) typedef version(`for(my_t i=…)`)을 반드시 같이 probe. **fix 패턴**: (a) built-in 경로의 tail을 `build_X(start, kind, signed, range, packed)` 공유 헬퍼로 추출 (b) 신규 typedef arm이 `peek_block_typedef_decl()`(disambig) 게이트 후 `TypeInfo.{kind,signed,range,packed}`를 그 헬퍼에 전달(typedef명=단일 토큰→`opt_range`/`opt_packed` 불필요). **refactor-to-shared-tail은 capture 순서 보존 필수**(루프 #43 自수정): `start=cur_span()`을 bump 전 캡처(=TYPE 토큰 span=synth-name 기반)인데 추출 시 bump 후 캡처로 바뀌면 VAR-name span→synth-name 불변식 깨짐; soundness가 검증한 capture 불변식을 refactor가 보존하는지 self-check. **class명은 ClassHandle TypeInfo로 typedefs에 등록**=신규 typedef arm이 `info.class_name.is_some()`를 받을 수 있음→**§4.5.41 'TypeInfo 표현불가 sub-case=loud'의 또다른 멤버**=명시 guard(아니면 elaborate "class handle without a class type"+cascade=misleading). **파서 `self.error`는 "expected <X>, found <token>" 템플릿 전용**(free-form 없음)→custom loud 메시지를 그 템플릿에 맞게 phrase(found 토큰=offender naming=정보적); `return None`로 cascade 제거(plain-assign fallback이 `name=init` 소비→단일 follow-on). **probe 격리 함정(루프 #44=핵심 신규 교훈)**: 타깃 typedef-form이 별개 pre-existing 갭과 얽히면 baseline도 함께 실패—**built-in 등가 baseline이 ALSO 실패하면 잘못된 갭을 probe 중**(anonymous `struct packed{…} w;` 직접 var-decl은 built-in member도 실패=별개 미지원 갭이지 typedef-member 갭 아님→자칫 "내 변경이 깨졌다" 오판). 타깃만 격리하는 최소폼으로 재-probe(`typedef struct packed{byte_t a,b;} w_t; w_t w;`=built-in `{logic[7:0] a,b;}`이 동작하는 폼서 typedef명만 교체). **probe 전 baseline(built-in 등가)이 PASS함을 먼저 확인**=DIFFER가 타깃 feature 때문임을 보장. = line 32 BISECT의 baseline-isolation판. **alias/copy-registration feature는 X-keyed 맵 전수 + set-OR-CLEAR(단순 set 아님)**(루프 #45 chained typedef=핵심 신규 교훈): 기존 등록을 새 이름으로 복제하는 feature(typedef alias·import·rename)는 (a) **그 이름으로 keyed된 ALL 사이드맵 전수**(typedef=typedefs/struct_layouts/enum_defs/union_type_names 4개·VAR-keyed[var_struct/var_enum]은 decl-time lazy라 제외)—하나라도 누락=alias가 base와 silently 다름(struct_layouts 누락→`alias_t x; x.field` 안 됨·enum_defs 누락→`.next` 안 됨·union_type_names 누락→union이 struct width SUM로 오취급) (b) **각 맵을 base에 property 있으면 SET·없으면 CLEAR(remove)**=단순 insert만 하면 cross-module same-name stale 누수(§4.5.31 union-desync의 alias판: 타 모듈 `n_t`=struct였는데 이 모듈 `typedef logic n_t`면 stale `struct_layouts[n_t]` 잔존→`n_t x; x.field` 오desugar). soundness에게 **'X-keyed 맵 ENUMERATION 완전성 + set/clear 양방향'** 명시 의뢰. AST node는 기존 variant(`TypedefKind::Alias`) 재사용→elaborate가 base AST(enum 상수 등록 등)서 이미 처리=alias는 재등록 불요(parser-side map 경유). = §3 line 49 'ALL site' / line 56 'eligibility-set parity'의 registry-copy판. **scope 도입 feature(flat global map이 백킹하는 name resolution서 NESTED scope에 DEFINITION 허용)는 LAZY-snapshot-at-first-def + restore-at-scope-exit 필수=아니면 clobber/leak silent-wrong**(루프 #47 body typedef DEF=핵심 신규 교훈): vita의 `self.typedefs` 등은 module-flat 맵이라, body-local typedef DEFINITION을 그냥 허용하면 (a)**clobber**: function-local `typedef logic[3:0] t`가 module `typedef logic[7:0] t`를 덮어→body 후 `t z`가 4bit=silent-wrong(iverilog는 `ff 15`로 scope) (b)**leak**: body-local 이름이 body 밖서 가시(iverilog는 reject). 수정=`block_body`/`tf_body`가 body 첫 typedef서 type-registry 4개를 LAZY snapshot(`None` until 첫 typedef=무typedef body 무overhead·byte-identity)+**statement 파싱 후** restore(statement의 cast `t'(e)`가 local typedef 가시해야=restore 너무 일찍이면 안 됨). 오라클로 scope 확정(iverilog가 shadow를 `ff 15`로·leak/nested를 syntax error로 처리=내 snapshot/restore가 정확). **AST-node discard 패턴**: body construct가 parser-side 등록만 필요(elaborate 불요)하면 module-item 파서(`parse_typedef`) 호출+ModuleItem discard(런타임 decl 0); 단 elaborate-needing 변종(enum 라벨 상수 등록)은 honest-loud. **scope 도입은 broadened-reach가 pre-existing silent-wrong을 honest-loud로 개선+인접 pre-existing 발굴**(루프 #47=line 72 재현): 내 struct_layouts 복원이 body-typedef shadow 변종을 silent-wrong→loud로 만듦(개선)·동시에 **별개 pre-existing ① 발굴**=block-local struct VAR shadow가 `var_struct` flat-map clobber→outer `x.a` inner 레이아웃(`b` should `bb`·body typedef 무관·var_struct 스코핑은 별개 슬라이스). snapshot 대상=TYPE-keyed 맵만(VAR-keyed는 별개 슬라이스로 후보 기록). **[#48 정정·핵심] VAR-keyed 맵도 SAME-name shadow면 silent-wrong=snapshot 필요(self-heal 가정 금지)**: #47 soundness가 "VAR-keyed(var_struct)는 `struct_layouts.get()?`로 self-heal=제외 정당"이라 했으나 #48 differential이 반증—block/function-local `inner_s x`가 module `outer_s x`를 shadow하면 `var_struct[x]=inner_s` clobber→struct_layouts엔 inner_s가 여전히 있어(module typedef) `x.field`가 inner 레이아웃으로 silently desugar=`b` should `bb`(self-heal 아님=진짜 silent-wrong). 수정=block scope snapshot을 VAR-keyed 4맵(var_struct/var_enum/struct_scalar_vars/struct_1d_array_vars)으로 확장. **교훈: soundness의 'self-heal/제외 정당' 주장도 differential로 실측**(§4 line 78 재현)·scope 도입 시 TYPE+VAR keyed 맵 둘 다 점검. **[#48 핵심] scope/snapshot 메커니즘은 ALL sibling decl-region에 적용(block_body AND tf_body)=§4.5.36 ALL-variants의 scope-trigger판**: #48 첫 컷이 block_body의 typed-decl arm에만 snapshot trigger 추가→**양 reviewer가 tf_body 누락 CONVERGED**(differential이 live silent-wrong `function f; inner_s x; return 8'hff`→`ff b` 발굴·soundness 독립 지적)→tf_body typed-decl arm에도 동일 trigger. snapshot/restore 같은 scope 메커니즘 손댈 때 `grep`로 ALL decl-region(block_body·tf_body·class-method·generate) 열거+각각 trigger 有無 확인. **[#48 핵심] "not reachable" 안전논거는 hand-wave 금지·differential로 reachability 실측**: #48서 "function-local struct var shadow는 frame E3009로 unreachable"이라 가정했으나 **E3009는 struct FIELD-ASSIGN만 커버**(`x.a=…`)·단순 DECLARATION(`inner_s x;`)+plain return은 E3009 미발화하면서 var_struct clobber=reachable silent-wrong. 안전논거("X 때문에 도달 불가")는 그 X의 정확한 트리거 조건을 differential로 확인(가드가 내 우려 케이스를 실제 커버하는지)—reviewer가 반례 probe로 반증함.
- **name-reference를 합성하는 기능(wildcard `.*`·implicit connection·auto-bind)은 referent의 KIND를 명시 검증—일반 resolution은 너무 permissive해 wrong-kind를 silently 수용**(이번 루프 `.*`=최대 교훈): `.*`가 미명시 포트마다 `Ident(pname)` 합성→일반 해소에 위임했더니 INPUT 포트의 same-name이 **constant**(parameter/localparam/enum-label)면 const 해소 성공=silent-wrong(spec=§23.3.2.5는 net/variable만 매칭). 일반 expr 해소는 net+const+func+genvar 다 받으므로, 합성 ref의 spec이 특정 kind(net/variable)를 요구하면 **그 kind table을 직접 조회**(`self.symbols`=net/var only via `walk_scopes_key` scope-walk)하고 wrong-kind/no-match=**loud**(float/silent-connect 금지). **asymmetry가 tell**: OUTPUT 포트는 우연히 loud(const는 lvalue 아님)·INPUT만 silent였음→**한 방향(read/lvalue·input/output)만 loud면 다른 방향 silent-wrong 의심**=양방향 전수 probe. = §1 'consumption-tracking'의 referent-kind판(too-permissive default가 silently 잘못 동작).
- **SHARED registry에 keyed된 feature는 sub-kind를 구분해야—공유 맵이 이질 종류를 담으면 eligibility-parity 함정**(루프 #31 struct vs union=둘째 silent-wrong): `struct_layouts`가 struct+union을 BOTH 담음(union도 `u.field` 읽기 위해)→"struct_layouts에 있나"로 gate한 `'{}` desugar가 union을 silently 포함→union overlay(width=MAX)를 concat(width=SUM)로 잘못 desugar=truncation silent-wrong(이전엔 loud였음). **수정=sub-kind 구분 셋**(`union_type_names`)으로 제외(union read는 `var_struct` 유지=무영향). **+ 동반 함정=side-set이 last-writer-wins 맵과 비대칭이면 desync→over-loud**(R2 적대 수렴 발굴): `struct_layouts`는 last-writer-wins(later same-name struct가 union 덮음)인데 `union_type_names`는 누적-only→cross-module same-name union→struct가 stale union 엔트리로 **false-positive loud**(valid 거부). **side-set은 추적 대상 맵의 lifecycle(last-writer-wins=overwrite 시 remove)을 미러**해야. = line 18/53 eligibility-parity의 TYPE-registry판(shared map=sub-kind 미구분 silent-wrong·side-set desync=over-loud).
- **기존 feature를 새 dispatch-shape로 확장 = 코어를 RESOLVED-input 받게 리팩터 후 공유(중복 금지)+신규 eligibility set은 기존과 PROVABLY DISJOINT**(루프 #33 struct `'{}` array-elem `arr[i]`): scalar `s='{…}` desugar를 array-elem로 확장 시 (1) 코어를 `build_X(tyname, rhs)`(=resolved struct type)로 추출→scalar wrapper(`var_name`→`var_struct`→tyname)와 array path(`BitSelect{Ident(arr)}`→element tyname) 둘 다 공유=중복 0·**scalar 행동 보존**(soundness가 guard 순서·fall-through 전수 대조). (2) 신규 set(`struct_1d_array_vars`=정확히 1 dim)은 기존 set(`struct_scalar_vars`=0 dim)과 `if 0-dim … else if 1-dim …` 구조로 **mutually exclusive**(한 var이 두 set=ambiguous dispatch silent-wrong)→**soundness에게 'disjoint 증명' 명시 의뢰**. (3) dispatch-shape 가드는 정확한 shape만(`BitSelect{base:Ident}` 1-level만=multi-dim nested base·scalar bit-select·part-select 전부 fall-through loud). **공유 훅이면 확장이 ALL site에 자동 전파**(루프 #33: array-elem가 7 assign-site 전부서 동작). medium-risk 표식도 root-cause 추적 후엔 clean일 수 있음(=line 22 'fix 크기는 root-cause 후 추정'; #33 둘 다 first-round CLEAN/SOUND).
- **AST variant 형상 변경(필드 추가)은 컴파일러가 전 match-site로 안내하나 `_`-binding은 신규 필드를 silently drop**(이번 루프 `PortConnList::Named(Vec)`→`(Vec, bool)`=8 site/3 crate+test): E0061이 전 destructure 사이트를 드러내나, `Named(v, _)`로 매치하면 신규 flag 무시=honor해야 할 곳서 drop 시 silent-skip. 컴파일 통과 ≠ 완전—**각 `_`-drop 사이트를 수동 감사**(여기서 flag drop이 correct? 아니면 미확장=silent?)+soundness 서브에이전트에게 'flag honor해야 하는데 drop하는 site 있나' 명시 의뢰. `.vu` 해시 re-pin은 schema_hash 테스트 패닉의 left(computed) 바이트로 EXPECTED 갱신(format_version은 sim-ir 아니라 불변)·checked-in golden 없으면 REGEN 불필요.
- **width 버그는 self-width TABLE(`width.rs`)과 eval(`eval.rs`)이 일치하는지 둘 다 확인—불일치=silent-wrong**(이번 루프 zero-rep `{0{x}}`): vita는 폭을 두 곳서 계산(table=context-sizing·eval=실제 값). `{0{x}}`서 table은 `.max(1)`로 1·eval은 width 0→**불일치가 concat sizing을 9-bit로 만들어** `a5`→`45` silent-wrong. width 수정 시 table+eval 양쪽 핀(`$bits`로 table-width·실제 출력으로 eval-width 차분). **table의 `.max(1)`/floor는 흔한 silent-wrong 원인**(0/empty를 spurious 1-bit로)=zero-width 합법 케이스(zero-rep·empty concat) 점검. eval이 이미 옳으면 table만 맞추면 IR-0.
- **target-width를 가진 ALL 컨텍스트는 fill operand를 그 폭으로 lower(`lower_ctx_or_plain(e, width)`)—bare `lower_expr`는 self-determined 1-bit라 fill literal(`'1`/`'x`/`'z`)이 zero-extend=silent-wrong**(루프 array `'{}`·**cast `N'(e)`**): `a='{'1,8'hAA}`서 `'1`→`8'h01`(should `ff`); `8'('1)`→`01`(should `ff`)·`int'('1)`→`00000001`(should all-ones)·`8'('x)`→`0X`(should `xx`). fill이 닿는 site 전수=**assignment pattern·concat·array fill·port-conn·size/param/prim cast(`N'(e)`/`W'(e)`/`int'(e)`/`byte'(e)`)**—전부 normal assign의 `resize_fill_rhs`/`lower_ctx_or_plain(e, width)`과 동치여야(non-fill은 fall-through=byte-identical). **신규 feature가 width context를 만들면 `grep lower_expr`로 그 폭 site를 찾아 fill 통과 여부 점검**(같은 버그군이 여러 site에 반복). **sized literal(`8'hFF`) probe로 안 잡힘=BARE fill(`'1`/`'x`/`'z`) probe 필수**(array 루프: differential 35-probe가 sized만 써 놓침·soundness가 `resize_fill_rhs` 누락 발견; cast 루프: #28 패턴 복제라 양 리뷰 first-round CLEAN/SOUND=proven idiom 재사용은 리뷰 통과 쉬움).
- **TYPED 구문을 UNTYPED form으로 desugar하면 target 타입의 per-element 의미(2-state coercion·sign)가 손실—명시 복원 필수**(루프 #31 struct `'{}`=fill-width의 형제 교훈): struct `'{e0,…}`을 bit-concat `{w0'(e0),…}`으로 desugar하면 폭은 맞으나 **native `'{}`가 하는 per-field 2-state coercion**(`byte`/`int`/`bit` 필드에 X/Z→0, §6.11.3)을 concat은 안 함→`'{8'hxx,…}`이 byte 필드서 `xx`(should `00`) silent-wrong. **classification 함정**: '동등 기존 구문'(line 62)을 STRUCTURAL 등가(plain concat)로 잡으면 vita·iverilog 둘 다 `xx`=SAME처럼 보여 버그 은폐→**SEMANTIC 등가**(native `'{}` 자체)로 차분해야 발현. 즉 desugar 슬라이스의 '동등 구문'은 desugar TARGET(untyped)이 아니라 SOURCE(typed) 자체. **fix=per-element 타입 coercion을 명시 합성**: 2-state 필드는 `w'(longint'(e))`(=`longint'`가 X/Z→0 squash[≤64bit]·size cast가 low w bit, AST 변경 0=기존 cast 합성)·4-state 필드는 `w'(e)`·squash 불가(>64bit 2-state)=honest-loud. target 타입의 2-state/sign/width를 per-element 핀(struct 필드 kind=`member_kind_two_state`로 추적). **fill-width(line 56)는 WIDTH-context·이건 TYPE-context**=desugar-to-concat의 두 per-element 축.
- **4-state Value서 byte/값 추출(렌더)은 `val & !unk`로 x/z 마스크 필수**(이번 루프 %0s z byte): vita 인코딩서 z=val 1·x=val 0이라 `v.val`만 읽으면 z byte가 `0xFF` leak(iverilog는 x·z 둘 다 공백). packed 문자열·VCD·display 등 "4-state 값을 raw byte로 해석"하는 코드는 전부 `val & !unk`(known bit만)로 추출. **신규 코드가 기존 함수의 COPY면 기존 함수의 잠복 버그를 상속**(이번 루프 `fmt_packed_chars_min`=`fmt_packed_chars` 복사→z 버그 상속)→**공유 헬퍼로 리팩터**(`packed_byte`)해 fix가 양쪽 동시 적용(DRY+§4 fold). 복사 전 원본을 grep해 잠복 버그(unk 무시·overflow·edge) 점검.
- **유사 구문 둘이 같은 해소 코드를 공유하나 한쪽만 특례를 가지면 그 비대칭이 버그**(이번 루프 name-resolution scope transparency): 이름 해소(`walk_scopes_key`)서 inline-task `$itask$` 스코프는 **이미 transparent**(바깥 module net 가시)였는데 frame-function `$func$` 스코프는 하드 정지였음—둘 다 non-pure 바디(SV §13.4)로 동일 취급돼야 함=1-line 추가로 해결. 코어 해소/lowering 수정 시 **유사 구문(task↔func·gen↔block)을 `grep`로 찾아 특례 parity 대조**. + **scope-resolution fix는 인접 증상을 동반 수확**(이번 루프 보너스): value READ 실패와 param-width(`function [W-1:0]`서 `W` 미해소→width 1 fallback) 실패가 **같은 root**—한 fix가 둘 다 고치므로 같은 슬라이스서 인접 증상(width·hier·shadow)을 오라클로 전수 핀. + **한쪽 경로(A=working)를 미러해 다른쪽(B=broken)을 고칠 때, B서 새로 도달가능해진 unsupported 입력이 A와 동일 loud에 떨어지는지 검증**(루프 #34 task-port·#35 block-local array-init=같은 패턴): #35서 block-local push 조건을 collect와 미러하니 dyn-storage(`int q[$]='{…}`)도 새로 push됨→이게 silent-wrong이면 fix가 새 버그 유발이나 **실제론 module-scope와 동일 LOUD(E3009)에 떨어짐=correct-or-loud 개선**(silent-drop→loud). 미러 fix는 byte-identity(working sub-case)뿐 아니라 **broadened-reach의 unsupported sub-case가 A와 동일 loud인지** soundness에게 명시 검증 의뢰(아니면 새 silent-wrong).

## 4. 사후 리뷰 = 적대 서브에이전트 (이번 루프서 CRITICAL 회귀 1건을 여기서 잡음=`output wand` 포트 net이 body-decl과 별개 생성 경로라 사이드카 누락→wire-x silent-wrong; differential이 포트/계층/generate 변형 probe로 발굴 — 절대 생략 금지)
- **병렬 ≥2 서브에이전트**, 각 다른 렌즈: (a) **differential silent-wrong 헌트**(오라클로 수십 케이스 차분, byte-identity 위반·신규 divergence) (b) **로직 soundness**(staleness·reset 타이밍·완전성·통합지점=force/clocking/NBA/다른 write 경로). 4축(Architecture·Performance·Maintainability·Robustness) 포함.
- 각 발견을 (a)신규 silent-wrong (b)pre-existing 무관 (c)문서화된 out-of-scope로 분류. **(a)는 즉시 디버깅·수정**. **모든 differential DIFF는 '동등 기존 구문'으로 (a)vs(b) 판별**(이번 루프 dec-field-width·`%0s` 패딩=plain `$write(42)`·`s="hello"`로 재현→공유 코드 pre-existing 확정).
- **적대 리뷰가 내 변경에 인접한 pre-existing silent-wrong을 발굴하면, 오라클이 loud-reject를 명시+가드가 엔진 수용형과 정확히 일치하면 같은 슬라이스서 fold**(이번 루프 Sformat non-whole-net dest): vita가 silently no-op하나 iverilog=loud인 dest를 elaborate서 loud-reject(엔진 `Signal{word:None}` 수용형과 **정확히 동일** 검사=쓸 수 있는 dest는 거부 안 함=byte-safe·correct-or-loud 개선). 단 broadened scope(공유 핸들러=`$sformat`도 영향)는 **full suite로 무회귀 확인 필수**. **불일치-root pre-existing**(dec_field_width·`%0s` 패딩=blast-radius 큼·전 default-radix flip)은 fold 말고 별개 슬라이스(§6).
- **soundness(hand-proof)와 differential(라이브 오라클)이 충돌하면 differential이 이긴다.** (이번 루프: soundness가 per-timestep 디둡을 "SOUND"라 했으나 differential이 CRITICAL 회귀 발굴→옳았음. hand-proof는 가정 누락 가능, 라이브 차분은 실측.) 수정 후엔 라이브 오라클로 재확인. **핵심 구분**(이번 루프 signed-fieldwidth n=1): soundness는 **formula-vs-자기-spec**을 검증(내 룰이 내 의도대로 동작?)·differential은 **spec-vs-oracle**을 검증(내 룰이 iverilog와 일치?). formula가 self-consistent여도 미시험 지점서 oracle과 어긋날 수 있음(soundness가 n=1 signed=2를 "검증"했으나 value 0 미시험→differential이 iverilog 1-bit signed=width 1 발굴). **differential은 경계값(0·min·max·width 1/2/n)을 oracle로 전수**. (이번 루프 `.*`: soundness가 match-coverage·`.p()` reconnect·borrow·determinism 전부 "SOUND" 검증했으나 **net-vs-constant라는 SEMANTIC 질문은 hand-proof 불가=오라클 필요**라 놓침→differential이 발굴. **SOUND verdict=self-consistent일 뿐 correct 아님**=soundness 통과해도 differential 절대 생략 금지.) **역도 성립=둘은 COMPLEMENTARY·어느 쪽도 subsume 안 함**(이번 루프 array `'{}`: differential 35-probe CLEAN이 bare fill `'1` 미시험으로 silent-wrong 놓침→**soundness가 code-path서 `resize_fill_rhs` 누락 추적해 발굴**). differential=probe-coverage 한계·soundness=code-path 추적 강점→**둘 다 필수**(differential CLEAN도 soundness가 잡을 수 있음). **soundness가 'iverilog와 일치/implementation-defined'라 주장하면 그건 측정 아닌 ASSUMPTION=differential로 반드시 실측**(루프 #41: soundness가 decl-order cross-read를 "implementation-defined·matches iverilog"로 SOUND 판정했으나 differential이 vita `sz=0` vs iverilog `sz=3` 실측 발굴). soundness는 oracle을 안 돌리므로 'oracle과 같다'는 주장 불가—그 주장이 나오면 differential의 해당 케이스 실측이 teeth. **두 reviewer가 SAME finding으로 CONVERGE=고신뢰 critical**(루프 #41 generate/interface drop=양쪽 독립 발견→surface가 진짜 구멍·즉수정 우선; 반대로 R1서 DISTINCT 발견=구멍 다수=미성숙).
- **index/offset/stride 매핑은 soundness에게 ENUMERATION-ORDER 정렬을 STRUCTURAL identity로 증명 의뢰**(probe보다 강함·루프 #42): 두 시퀀스(예: flatten된 leaf 순서 vs `residual_word_offsets`의 flat offset 순서)가 `output[k]`서 정렬되는지=probe는 몇 케이스만 확인하나 **soundness는 둘이 동일 mixed-radix 분해(같은 stride/digit)로 position을 enumerate함을 증명**(leaf[k]↔offset[k] ∀k). offset/stride/coord 매핑 슬라이스(packed flatten·multi-dim 배열·part-select)는 soundness에게 "두 순회가 같은 인덱싱 수식인가" 명시 의뢰=off-by-one/transpose/direction silent-wrong을 전수 차단(probe는 대칭 케이스서 놓침).
- **soundness의 이론적 silent-wrong 제기는 측정으로 검증(가정 금지)**(이번 루프 "double-eval"): desugar 슬라이스면 ① 신규형 ≡ explicit-desugar-target byte-identical 확인(=transform 정확) ② 의심 quirk가 explicit 단독서도 재현되는지 격리(재현=pre-existing·무관). 둘 다 통과면 신규 silent-wrong 아님(별개 pre-existing 후보로만 기록).
- **신규 path가 pre-existing 헬퍼를 호출하면 그 헬퍼의 잠복 버그가 노출됨**(이번 루프 enum-fold가 `const_lit` 호출→i64 산술 overflow가 debug panic): attacker-영향 입력(예: enum 라벨 산술 `{A=i64::MAX+1}`)을 새 경로가 헬퍼에 통과시키면 **panic/overflow 감사**(crash·debug-panic은 correct-or-loud 위반=loud 아님). `grep`로 헬퍼의 unchecked `+`/`-`/`*`/`-neg` 찾아 `checked_*`로 공유 수정(overflow→None→loud, 다른 호출처도 견고화).
- **user identifier로 synthetic name을 생성하는 feature는 DUPLICATE/collision 입력을 soundness에게 명시 검증 의뢰**(루프 #40 `foreach(a[i,i])`=differential이 놓친 silent-wrong): 사용자 인덱스/변수명에서 `__foreach_<name>_<L>` 같은 synth name을 만드는 desugar는, 같은 이름이 2번 등장(`foreach(a[i,i])`·중복 포트·중복 라벨)하면 **동일 synth name 2개→두 엔티티가 같은 net/slot 공유=오동작**(iverilog는 "already declared" reject). differential은 정상 입력만 probe해 놓치기 쉬움→**soundness에게 '중복 user-name이 synth collision 유발하나' 전수**+발견 시 parse-time 중복검출 loud(iverilog reject 일치=correct-or-loud). = §4 differential(probe coverage)↔soundness(code-path enumeration)의 synthetic-name판.
- **name-scanning GUARD(`expr_reads_ident` 등)를 추가하면 그 traversal 헬퍼가 ALL expr-wrapping variant를 순회하는지 감사—누락 1개면 wrapped ref가 guard 우회=silent-wrong 복귀**(루프 #43=양 reviewer CONVERGED): formal-ref default를 loud하려 `expr_reads_ident(default, formal)` 가드 추가했으나 헬퍼가 `ExprKind::Cast` 미순회→`b=int'(a)`/`signed'(a)`가 우회→caller scope eval=silent-wrong(vita 100 vs spec 5). 가드 추가 시 traversal 헬퍼의 `_ => false` catch-all이 삼키는 variant 전수(`Cast`·`AssignPattern`·`New`·size-cast의 width expr) 점검·`grep 'ExprKind::'`로 전 variant 대조. **soundness에게 '이 guard의 traversal이 모든 wrapping을 cover하나(어떤 variant가 false로 빠지나)' 명시 의뢰**=differential이 bare form만 probe해 놓침. = guard 우회는 synthetic-name collision과 같은 부류=불완전 분석이 silent-wrong 복귀.
- **IR-노드 추가 fix(resize/cast/wrap)는 expr 구조를 assert하는 기존 unit 테스트를 깸—value-correct여도**(루프 #38 elaborate `ft_e1/e2/e3`): resize가 Select/SysFunc wrapper를 추가하면 'rhs root=Binary(Add)' 같은 구조 assert 실패. **테스트 intent(inline folding=no-net) 보존하며 wrapper만 벗기는 `peel_resize` 헬퍼 추가**(value-only로 약화 말 것=구조 검증이 목적이면)—wrapper 식별은 정확히(offset-0 PartConst Select·`$signed`/`$unsigned` 1-arg). 변경이 value-correct(=net-write가 어차피 truncate하던 것을 명시화·wider-context 소비 시 오히려 high-bit 누출 fix)임을 오라클로 먼저 확인 후 테스트 갱신.
- **soundness가 찾은 'currently-masked' silent-wrong도 수정**(루프 #38 real-call `$signed`): 출력 차이가 없어도(engine이 `$signed(real)`=pass-through로 우연히 가림) 의미상 틀린 노드는 fragile+byte-identity 깸→수정(robustness+미래 engine 변경 방어). **IR-level 타입검사(`expr_is_real`)는 frame `Expr::Call`을 못 봄**(Call에 타입 flag 無→`_=>false`)→**AST-aware companion(`cast_operand_is_real`=`expr_is_real ‖ ast_has_real_call`)으로 가드**(AST `rhs`가 있는 call-site서). real/string/dynamic-width 등 IR로 불투명한 타입을 resize/cast/wrap하는 신규 코드는 이 패턴 적용(`lower_prim_cast`가 선례).
- **detection-only 패스(honest-loud)는 false-POSITIVE만 blocker·false-NEGATIVE는 safe**(이번 루프 block-local): 적대 differential을 **'vita가 이제 wrongly 거부하는 valid 코드'(=false-positive)**에 정조준 의뢰(detection은 해소 무변경이라 silent-wrong 신규 불가=false-negative는 pre-existing 유지=무회귀=tolerated, 문서화만). **false-positive를 fix하려 suppression 추가 시 false-negative regression 유발 주의**(이번 루프 2-round saga: ① sibling 각자 own same-named local=valid인데 오flag→sibling 재선언 suppress 추가 ② 그 suppress가 **ancestor** coalescing hazard까지 막아 silent화=regression→`stmt_contains_block_span` guard로 true-sibling만 suppress·ancestor=recurse). suppression은 정확한 케이스만 막고 반례를 오라클로 핀. **각 fix 후 재-review 필수**=differential이 sibling-fix가 유발한 ancestor regression을 2nd round서 발굴(아래 'CLEAN까지 반복'의 실증).
- **silent issue 1건이라도 → 수정 후 사후 리뷰 재시작. CLEAN 나올 때까지 반복.** stash 차분으로 pre/post 회귀를 실증. **R1서 두 렌즈가 DISTINCT 버그(서로 못 본)를 찾고 R2서 SAME 버그로 수렴하면=surface 성숙 신호**(루프 #31: R1 differential→2-state coercion·soundness→union[각자 상대 것 놓침]=구멍 다수; R2 둘 다 cross-module collision 수렴=남은 구멍 적음→다음 R 생략 가능). 반대로 R2서도 distinct 신규 발굴이면 아직 미성숙=계속.
- **write-access 서브에이전트(debug-master)는 내 파일을 직접 수정할 수 있음**(이번 루프 differential이 `%F` 가드를 직접 edit): dispatch 후 **`git diff`로 변경 전수 확인**+오라클로 그 수정이 옳은지 재검증(서브에이전트 fix를 맹신 말 것)+이후 Edit 전에 Read 재실행(파일 stale). soundness(read-only)와 differential(write 가능)의 권한 차이 인지.

## 5. 게이트
- `cargo test --workspace --locked` 0 fail·카운트 증가(신규 회귀 테스트 포함, 오라클 핀; 오라클 없으면 vita-내부 차분 핀). clippy·fmt clean. format_version 의도대로 불변.
- **fmt-check·clippy은 `cargo test`와 별개 게이트—test green이어도 fmt/clippy 실패가 main에 샐 수 있음**(루프 #38이 #36의 fmt-noncompliant 머지 테스트 파일을 발견·별도 커밋 정리; `cargo test`는 fmt 안 봄). 커밋 前 `cargo fmt --all -- --check`+`cargo clippy ... -D warnings` **둘 다** 필수. clippy `doc_lazy_continuation`은 `//!` doc comment의 markdown `*` bullet list서 발화→**doc comment는 prose로**(bullet/multi-line list 금지). 신규 테스트 파일 작성 후 `cargo fmt --all`로 `run("…")` 멀티라인 포맷 자동정리(rustfmt 선호형과 불일치 흔함).
- **"기존 구문으로 desugar" 슬라이스의 최강 회귀 테스트 = vita-내부 등가 differential**(이번 루프): 신규 형태가 등가 기존 형태와 **byte-identical stdout**임을 assert(`wire #3 w=a` ≡ `wire w; assign #3 w=a`). 오라클 비의존+pre-existing 양자(예: delay pre-drive z/x)에 견고=구현이 정확히 그 path로 가는지 직접 증명. 오라클 차분은 settled 값만(pre-drive 윈도 회피).
- **additive 새 구문(기존 등가 form 존재)의 최강 테스트 = new form ≡ 기존 form 바이트 동일, loud 케이스까지 포함**(이번 루프 bare `@X` ≡ paren `@(X)`): 지원되는 X는 둘 다 시뮬(오라클 핀)·**미지원 X는 둘 다 동일 loud**(stdout+exit code 동일 assert). 신규 구문이 기존 downstream로 정확히 라우팅됨을 증명=신규 semantics 0=silent-wrong 불가능. paren-vs-bare처럼 '기존 형태'가 있으면 그것과의 등가가 오라클보다 강한 teeth(미지원 feature[`@a[2]`·`@u.s`·`@f(x)`]에도 견고—오라클 없어도 bare==paren로 적대리뷰 방어). = §3 'desugar 등가'의 **non-desugar(직접 라우팅)판**.
- **big blast-radius 테스트 갱신은 mechanically-verifiable invariant가 있으면 defer 불필요**(이번 루프 signed-fieldwidth 25 dyn_storage 핀): "blast-radius 큼"이 자동 defer 사유는 아님—변경이 **검증가능한 단일 변환**(예: space-only 필드폭=value 불변)이면 script로 일괄 갱신+**invariant assert**(`strip_spaces(old)==strip_spaces(new)`=non-space char 무변경 증명). 룰 자체는 오라클 핀(plain reg)이므로 no-oracle 테스트(assoc/queue)도 같은 룰로 correct. 각 diff가 예상 변환과 일치함을 프로그램으로 확인 후 적용(수동 space 카운팅 금지=오류원).
- **full-suite 실패는 isolation+직전 run으로 flake 판별**(이번 루프 backend_equiv 2 fail): 동시실행 부하로 heavy 테스트(compiled-vs-interpreter corpus 등)가 flake 가능. **패닉 귀속 금지**—해당 테스트만 격리 재실행(pass=flake)+직전 full run 결과(이미 green이었나) 대조+내 변경이 그 테스트 입력(신규 구문 미사용 corpus)에 영향 줄 수 있나 논증. 셋 다 무관이면 flake 확정·진행(전체 재실행으로 green 핀).
- **실패 테스트 = 옛 버그 동작 인코딩 의심**(의미론/스케줄러 수정 시 흔함): 수정 중인 구문(예: `a=~a` 오실레이터·글리치)을 기존 테스트에서 grep해 **선제 점검**. 실패 테스트가 오라클과 불일치하면 **올바른 구조로 갱신**(삭제 말고)·주석에 옛 가정이 왜 틀렸는지 명시. (이번 루프: differential 리뷰가 옛-무한루프 테스트 2건 발굴.) **deferred 갭을 닫을 때는 그 deferral을 assert하는 `*_is_loud` 테스트를 선제 grep해 `*_now_supported`로 flip**(루프 #42: §4.5.33의 `multidim_pattern_is_loud`가 multi-dim 구현 후 실패→positive 테스트로 갱신). 구현 전 `grep -rn 'is_loud\|not.*supported\|deferred'`로 그 feature의 loud-assert 테스트를 먼저 찾아 갱신 대상으로 표시.

## 6. 문서
- **ROADMAP**: 완료 항목을 ✅로 표시(이력 보존·삭제 금지)·새 슬라이스 항목 추가. 사후 리뷰가 부수 발굴한 별개 silent-wrong은 **각 별개 슬라이스 후보로 신규 기록**(서두르지 말 것).
- SPEC(`docs/preview/`): 구현이 계획서 벗어나면 2회+ 검토 후 타당할 때만 수정.
- **CLAUDE.md 상태줄**: 최신 1~2 배치만 상세, 그 이전은 **ROADMAP §x·DEVLOG 포인터로 압축**(무한 증식 방지 = context 낭비 절감). 압축 전 해당 상세가 ROADMAP/DEVLOG/git-커밋에 있는지 확인.

## 7. 커밋 & 푸시 (사용자 의사 무관 진행 OK)
- 스코프: `git add 016_claude_rtl/<path>`(타 프로젝트 누수 금지·.env 금지·.superpowers/scratch 금지).
- 커밋 메시지 = `Fix/Add/Update/Refactor <요약>` + 본문(버그·메커니즘·byte-identity·리뷰결과·out-of-scope) + 푸터 2줄:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` / `Claude-Session: https://claude.ai/code/session_015e8bYmYjqLGoqzYi8oD2P4`.
- main FF 병합(`git checkout main && git merge --ff-only feat-<slug>`) → 브랜치 삭제 → `git push origin main`.

## 8. 루프 유지
- 본 LOOPROMPT.md를 이번 반복서 배운 점으로 더 핀포인트하게 보강(이 항목 포함).
- 다음 반복 예약(자기 페이싱). 보고는 한글·간결.

# ROADMAP — 잔여 과제 (vitamin)

> **이 문서 = 전방(남은 것)-전용.** 완료 항목의 상세 로그·옛 §번호(§0~§7·§4.5.x) 원문은 전부 [ROADMAP_ARCHIVE.md](ROADMAP_ARCHIVE.md)(§번호 보존)로 이관했다. 이력 내러티브 = [DEVLOG.md](DEVLOG.md), 상위 스냅샷 = [REMAINING_WORK.md](REMAINING_WORK.md), 실행 큐 = `LOOPROMPT.md` NEXT(로컬 dev-meta), SPEC 정본 = `docs/preview/`.
>
> **기준선(2026-07-25)**: format_version **23** · **4469 tests green** · 3-OS CI green · MsgCode **59**(W3057 auto-top ambiguity) · **MSRV 1.85**(§4.5.150). 최신 완료 = **§4.5.221(real-valued parameters `parameter real` loud→supported — 단 **정수-상수 문맥에서는 loud**. elaborate-local `real_param_val: BTreeMap<String,f64>` 사이드맵(§4.5.217 `str_param_raw` 동형·`params`와 분리해야 정수 fold가 조용히 반올림하지 않음)·**선언 타입 키잉**(`parameter real R = 3;`도 real)·`lower_index_expr`가 **IR 위에서** real 인덱스 거부(12 site·값 판정이라 `v[R+0]`·lvalue까지 구조적 완전). **★리뷰 2라운드가 내 fix를 뒤집음**: `$clog2(R)`의 조용한 1-bit width를 고치려 `const_eval_in_scope` **leaf**에서 real→i64 변환했더니 differential/soundness가 각각 다른 진입점으로 NOT-CLEAN — generate-if `R>2`(R=2.4)가 **generate body를 조용히 삭제**·`R==2`가 TRUE·`localparam real B=A`가 1.5→2.0·`CLK/2`가 2.5→2.0. IEEE §11.8.1은 real operand 식을 **real 도메인**에서 평가하고 문맥 경계에서 한 번 변환하는데 leaf 변환은 둘러싼 연산자가 결정하기 **전에** real 값을 파괴 = 조용한 1-bit를 조용한 잘못된 분기와 맞바꾼 것(사다리 하강). **되돌리고 `check_const_range_bound`에서 loud.** ★재감사가 **PRE 바이너리 3-way 측정**으로 양방향 하강 4건 추가 발견: const-fn 호출이 gate를 우회해 `logic [f(R)-1:0]`가 PRE loud→POST **조용히 1-bit**(`Call` arm 부재)·`parameter real R = 4`(정수 초기화)를 real-ONLY 바인딩해 정수 능력 8형 **false-loud 회귀**. fix=`Call` arm + **정확한 i64로 fold되면 `real_param_val`/`params` 양쪽 등록**(두 표현 일치=안전). **3라운드**가 그 fix 의 새 구멍 3개 적발: real 식 override 가 자식 default 를 조용히 유지(guard 가 **구문적 리터럴** 테스트였음)·`{R{1'b1}}`/`v[0 +: R]` 가 **16 MiB 폭주 exit 0**(`lower_expr` 는 `real_param_val` 을 우선하는데 gate 는 `params` 기준으로 판정=**classifier≠lowering resolver**). fix=**술어를 resolver 별로 분리**(fold판/lower판)+`lower_index_expr` 를 6 site 전부에. 상세=ARCHIVE §4.5.221)** · 직전 = **§4.5.220(DYN string-array element의 byte select가 조용히 0이던 SILENT-WRONG 수정 + write-twin 5형 loud화 — `handle_is_str_readable` gate가 word-indexed Signal을 거부해 width-0 packed bit-select로 낙하했으나 엔진 `handle_str_bytes`는 이미 읽을 수 있었음[gate가 자기 술어를 under-approximate]. **§4.5.219가 기록한 T1-2/3 전제조건**(dyn ⊇ fixed). 적대 2-lens가 write-twin family 5형 발굴[part-select 3형·concat·real]·soundness의 **배치 논거**로 `lower_lvalue` 단일 퍼널에 공유 술어 `is_string_valued_net`으로 통합. 재감사 644-file sweep 미귀속 0. 상세=ARCHIVE §4.5.220)** · 직전 = **§4.5.219(FIXED string-array decl-init `string s[N]='{…}` loud→supported — DYNAMIC 형태는 이미 동작하는데 FIXED만 blanket-loud였던 비대칭. `'{…}`를 선언 인덱스별 `s[k]=<elem>`로 t0 pre-sweep에 전개(공유 헬퍼 1개를 module/block collector가 공유). **capability-parity 실측이 설계를 바꿈**: fixed→dyn 통째 라우팅은 byte-select `s[0][0]`를 silent 0으로 퇴행시켰을 것(어느 쪽도 우세하지 않음)→additive로 축소. 적대 2-lens가 결함 5건 포착[descending write ORDER silent-wrong·hier 원소 loud→silent 확대·negative bound·i64 overflow panic·decl/collector 재판정]+false-loud 1건. 상세=ARCHIVE §4.5.219)** · 직전 = **§4.5.218(inner-scope local이 OUTER string-array side-map을 shadow 못 하던 SILENT-WRONG 수정 — `walk_scopes_key`가 `hit`이 probe하는 한 map 안에서만 innermost-wins라 inner NET binding을 지나쳐 outer side-map으로 resolve·read/write 양 경로·非-string 로컬·generate-local의 bogus range-LOUD 포함. **opt-in** `walk_scopes_key_shadowed`로 분리해 나머지 13 consumer는 byte-identical. 적대 2-lens가 내가 넣은 결함 2건 포착: shadow 검사를 공유 walk 전체에 넣으면 `symbols` 순서 의존성이 generate per-phase 재-fold와 충돌해 **generate body가 통째로 조용히 삭제(exit 0)**·block-local loud가 byte-correct 설계 11건 false-reject. 상세=ARCHIVE §4.5.218)** · 직전 = **§4.5.217(string-ARRAY ELEMENT가 concat/replicate/relational에서 packed로 새던 SILENT-WRONG 수정 — AST 문자열-도메인 분류기 `expr_is_string_ast`에 인덱스 arm 부재로 5 consumer × 6 구문형에 동형 복제된 갭[`{s[0],s[1]}`=iverilog `abcb` vs vita `""`]·적대 2-lens 중 soundness가 분류기/lowering resolver 불일치[신규 silent-wrong]를 포착→각 clause를 자기 lowering과 동일 resolver로 정렬·byte select on element `s[0][0]`도 동반 수정·format 23 불변·상세=ARCHIVE §4.5.217)** · 직전 = **§4.5.216(round-19 follow-on: F-record-out short-circuit output-formal call을 if-cond + `?:` arm + general `&&`/`||` rhs로 확장[while/for/if 全 condition + assignment-rhs 커버]·adversarial review가 ternary definite-arm sign/width silent-wrong 포착→coercion-safety gate·Q string-member direct hardening·format 23 불변·상세=ARCHIVE §4.5.216)** · 직전 = **§4.5.215(round-19 리포트 4-가족[BL·Q·F-struct·F-record-out] loud→correct-support·리포트 2-오진 정정[Q "param-aware width"=override-silent-wrong·F-record-out "≥2-string 경계"=부재]·m.name=F-struct downstream 증상·adversarial review가 silent-wrong 4건 포착[3 latent]·format 23 불변·상세=ARCHIVE §4.5.215)** · 직전 = **§4.5.214(fork…join[_any|_none] inside a suspendable task body loud→supported·Case A/B split + interior-mutable window arena·format 23 불변** — §4.5.213(C1 part 2)이 "깊은 스케줄러 rework·blast radius=frame 서브시스템 전체"라 correct-or-loud LOUD 유지·ROADMAP §0 NEXT로 남긴 항목을 실제 구현. **핵심 재발견**: 스케줄러가 **단일-스레드**라 `stash_frame_windows`/`restore_frame_windows`가 이미 실행 중인 activity의 window만 stack에 두도록 보장→concurrent fork children과 parked parent가 결코 동시 공존 안 함(교대 실행)→"single-owner move-based window라 concurrent children 불가"는 **parent가 parked인 동안 child가 그 window에 도달해야 하는 경우(Case B)에만** 진짜 문제. **Case A/B split**: 모든 arm이 부모 task의 frame-local range `[base,base+locals_len)`을 안 건드리면(리포트 repro `fork a(); b(); join`) **Case A**=기존 owned-window 모델로 즉시 동작(신규 인프라 0)·하나라도 건드리면 **Case B**=interior-mutable **arena**(`WindowSlot::Shared(h)`→`frame_windows`, dyn_heap/class_heap과 동일 패턴) 필요. **3-stage 전달**(각 단계 미도달 시 항상 LOUD): Stage 1(Case A·`classify_one_arm`/`fork_arms_self_contained` elaborate classifier·`exec_fork` frame-aware·in-frame child-completion intercept `call_stack.len()==1`·`FRAME_FORK_KEY`=u32::MAX join-mode keying·2 review-fix[classifier gap·backstop 회귀]+breadth[`wait fork`가 misleading runtime F4016 대신 clean E3009로])·Stage 2(Case B `join`-all·`enter_task_frame`서 arena 할당·parent `Return`서 직접 free·`FuncMeta.contains_shared_fork` sidecar)·Stage 3(Case B `join_any`/`join_none`·`frame_window_rc` refcount[alloc 1+N retain=N release+1 release→0에서 free]·surviving child가 parent보다 오래 살아도 안전). **final-review가 fork arm 내부 `return`의 silent frame-corruption 회귀**(parked parent의 `exit_task_frame`/dyn-array free를 arm이 실행)를 발견해 `classify_one_arm`의 `Return => Loud` 1줄로 즉시 E3009화(iverilog도 컴파일 거부). **적대 검증**: iverilog 13.0이 실행하는 모든 형태(join/join_any/join_none·비대칭 타이밍·Case-B 공유변수 가시성·sibling visibility·arena 재사용/격리) 全 MATCH·iverilog가 크래시하는 surviving-detached-child(`of_JOIN_DETACH` assertion)는 hand-IEEE(§9.3.2·rc soundness debug-assert 활성)로 검증. **correct-or-loud 잔여**: nested fork·frame body의 `wait fork`/`disable fork`·fork arm 내 `return`·static task Case B·fork하는 task를 호출하는 arm(F4004 tie-cap). format 23 불변(런타임 엔진 타입 + `FuncMeta.contains_shared_fork` sidecar뿐)·+26 tests(Stage1 vertical-slice+review-fix 12·breadth 5·Stage2 arena 3·Stage3 refcount 4·final-review 2). **교훈**: "single-owner window라 concurrent children 불가"라는 최초 framing이 과보수적(단일-스레드+기존 stash/restore가 Case A를 공짜로 해결)·interior-mutable arena가 shared frame state의 반복 정답(dyn_heap/class_heap과 동형)·refcount가 join_none/join_any survivor lifetime 해결·2-stage+전체-브랜치 리뷰가 solo-pass라면 배포됐을 이슈 3건(classifier gap·fix-유발 회귀·전체-브랜치에서만 보이는 return-in-arm silent-wrong) 포착·Owned-path-byte-identical 규율이 hot-path 타입 변경(`Vec<Value>`→`WindowSlot`)을 안전화. **ROADMAP §0 NEXT 완전 소진**(round-18 8-가족 + C1 part1/2 全 RESOLVED). 상세 ARCHIVE §4.5.214). 직전 = **§4.5.213(round-18 리포트 8-가족 loud→supported + C1 const-repeat·format 23 불변** — 외부 리뷰어 round-18 리포트의 잔여 8-가족을 correct-support화: **F2** severity($info/$warning/$error/$fatal) in frame function/subset-task body[`had_error`→Cell·`frame_emit_severity`·unique-case 위반 warning] · **E1** enum method(.name/.next/.first) on tf-port enum FORMAL[`TfPortType` 5-tuple로 `var_enum` 등록] · **E2** method on struct string-member `r.name.substr`[파서 member-method rewrite] + string method on string dyn/queue ELEMENT[`ir_expr_is_string` element arm] · **A/G** queue/fixed array of NON-packable record(param-width/mixed member)[SoA fallback + queue method fan-out `try_soa_queue_method_stmt`/pop-into-record·all-or-loud] + foreach over SoA record array[iterator receiver rewrite] · **D** automatic block-local WITH init in module process[`compute_per_entry_block_locals`·under_fork guard·block-entry re-init·fork=loud] · **F1** output/inout-function call in while/for CONDITION[loop head hoist] · **F3** dyn-formal fn call in NBA rhs + ternary arm[hoist·ternary는 pure만·impure=loud] · **C1 part1** const `repeat(N) @(edge)` in suspendable task[unroll-aware `ast_has_repeat_with_timing`]. hand-IEEE(iverilog가 unpacked struct/output-port/automatic-lifetime 거부) + iverilog 오라클(F2/F3/E1/C1) 全 MATCH·+58 tests·flip 8. **C1 part2 fork-in-frame = LOUD 유지**(deep scheduler·shared frame-window rework·correct-or-loud→§0 NEXT). 상세 ARCHIVE §4.5.213). 직전 = **§4.5.212(SILENT-WRONG 수정: size-cast `N'(expr)` context-width 미전파 RESOLVED·size-cast 전용 재귀 ctx-width lowering·format 23 불변** — `8'(a*b)`=vita 13 vs iverilog 45 등, size cast가 inner 산술을 operand self-width로 계산 후 resize(carry 소실)·문서화된 §2 DEEP silent-wrong. **"DEEP" 재평가**: 공유 ctx 머신 변경 대신 **size-cast 전용 재귀 헬퍼로 격리**하면 회귀 표면 없음. `lower_size_ctx`가 context-det 연산자 구조(arith/bitwise 양 operand·shift/`**` base·unary±~·ternary)를 재귀하며 각 self-det leaf를 N으로 widening(shift amount/cmp는 self-det)·extension sign=operand 전체 sign(`ast_ctx_signed`·§11.8.1 mixed→unsigned zero-ext all·iverilog 실측 확정)·leaf sign 재-stamp로 signed div/ashr/cmp 의미 보존. param/call leaf 판정 불가→기존 fill-only fallback(회귀 0). **iverilog 오라클** 있어 exhaustive+random 차분 全 MATCH(unsigned 全op·signed mul/div/ashr·mixed-sign 15/65·ternary·nested-cast·power). format 23 불변·+23 tests·잔여=param/call leaf fallback·상세 ARCHIVE §4.5.212). 직전 = **§4.5.211(NON-ZERO-BASE DESCENDING unpacked-array formal RESOLVED·over-conservative reject 제거·format 23 불변** — §4.5.206이 `int m[4:1]`(non-zero-base descending)을 "base+direction not cleanly verifiable"라며 loud로 gated했으나, **오라클 부재 ≠ 검증 불가**: iverilog가 array formal·whole-unpacked-copy를 거부해 직접 오라클은 없지만 IEEE §13.5.1이 same-declared-range formal에 `m[k]==a[k]`를 강제하고 이건 완전 관측 가능(구별 자릿값이 mis-map 가시화). §4.5.206이 도입한 per-dim `(lo,size,ascending)` + `array_formal_ext_dims`가 packed dim에 lo를 세팅→`flatten_word`가 pack(`lower_array_actual_packed` flat-word position order)과 read(`m[k]`→`k-lo`) 양쪽서 `idx-lo`를 일관 정규화→**descending도 이미 forward로 처리**(저자가 자기 머신러리를 불신해 과보수 gate). **fix**=`classify_unpacked_array`의 `lo != 0 && !ascending` reject 1줄 제거. distinct-digit 全 MATCH(1-D `[4:1]`=4321·**non-square 2×3 `[2:1][3:1]`=654321 reversal clincher**·mixed `[1:2][3:1]`·offset `[5:2]`·signed·output/inout/hier-in/hier-out/frame-local/forward)·direction·base MISMATCH은 여전히 loud(진짜 §7.6 guard)·format 23 불변·+14 tests·flip 2·상세 ARCHIVE §4.5.211). **이로써 배열 formal shape 스토리 완전 종결**(any-base × any-direction × 1-D/multi-dim × input/output/inout × local/hier/forward/frame-local). 직전 = **§4.5.210(forwarding a frame task/function's OWN unpacked-array FORMAL into a nested hierarchical TASK enable RESOLVED·whole md-packed net forward·format 23 불변** — §4.5.207/209가 STATIC array actual을 지원했으나, frame formal array를 nested hier enable로 forward(`task driver(input int a[]); u.tk(a);`)하면 actual이 md-packed FRAME net(not static array)이라 loud였음(defer gate가 static array만 record·whole array formal을 value로 lower 자체가 loud). **fix**: defer gate(`inline_task`)가 frame array formal net(`frame_arr_formal_meta`)도 record·resolve 시 `pack_hier_array_actual`이 whole md-packed net 값을 forward(양 slot이 동일 `array_formal_ext_dims` layout→repack 불필요·`lower_array_actual_packed`의 UARR2 forwarding과 동일)·shared `hier_array_shape_ok`(per-dim dims + elem_w MATCH) gate. INPUT forward만(OUTPUT/INOUT forward=copy-out writeback into frame net→loud). NO ORACLE→hand-IEEE §13.5.1·1-D/multi-dim/signed/frame-local/**mutated-value(current-value read + caller immune)**/**chained 2-level**/double-enable 全 MATCH·correct-or-loud(shape mismatch·output/inout forward·non-hier whole formal=loud)·+11 tests·flip 1·상세 ARCHIVE §4.5.210). 직전 = **§4.5.209(hierarchical TASK enable OUTPUT/INOUT unpacked-array formal RESOLVED·deferred copy-out at resolution·format 23 불변** — §4.5.207이 INPUT array를 지원(defer actual-net + resolve-time pack)했으나 OUTPUT/INOUT은 copy-OUT을 resolve 시점에 합성해야 해서 loud였음. **fix**: hier_tasks gate가 fixed-array formal을 방향 무관 admit·`resolve_deferred_hier_task_call`이 OUTPUT/INOUT array formal마다 fresh packed-temp net 예약 + scalar out-bind(callee md-packed slot→temp at exit) + ret 블록 **앞에** unpack(`caller[i]=temp[i*ew+:ew]`) **직접 IR 주입**(position i ↔ caller flat word i = `pack_hier_array_actual`의 정확한 역대칭→round-trip correct-by-construction·scope/lower_stmt 불필요). INOUT은 copy-IN(§4.5.207 pack)도 추가. NO ORACLE→hand-IEEE §13.5.2 pass-by-value-result·1-D/2-D/non-square 2×3[reversal clincher]/3-D/signed byte/mixed-scalar+array/partial-write=0/nested-in-frame-body[func_block route]/cross-suspend/deep-path/per-instance 全 MATCH·correct-or-loud(descending non-zero-base·shape mismatch·scalar actual·string/dyn=loud)·+17 tests·flip 1·상세 ARCHIVE §4.5.209). 직전 = **§4.5.208(hierarchical TASK enable NESTED in a frame-task body RESOLVED·format 22→23·FuncMeta.has_hier_call force-suspend** — §4.5.197/201이 hier enable을 top-level process에서만 defer(nested-in-frame-body는 loud). 근본 원인=nested enable의 placeholder `Call.target`이 finish resolve서만 patch되는데 per-instance `compute_suspendable_tasks`가 그 前 실행→elaborate(pre-resolve)/engine(post-resolve) suspend 분류 divergence→§4.5.197 pure-function 계약 위반(P0). **fix(sound over-approximation)**=`FuncMeta.has_hier_call: bool`(staged-trailer sidecar·format 22→23)로 frame-body hier call 가진 task를 `compute_suspendable_tasks`(force_suspend param)서 BOTH computes 일관되게 suspendable 강제(callee suspend 무관·SOUND). frame-body defer machinery(`func_block` route·`pending_hier_task_calls` rebase). **iverilog ORACLE**(scalar hier enable 지원): basic/repeated/**suspendable callee `#5`**/input·output formal/per-instance 全 MATCH·correct-or-loud(named args·frame-formal array forward)·flip 2·상세 ARCHIVE §4.5.208). 직전 = **§4.5.207(hierarchical TASK enable with an INPUT unpacked-array formal RESOLVED·defer actual-net + resolve-time pack** — §4.5.197/201 hier-task scalar formal 후속(H-B). array formal이 loud였던 이유=호출 시점 whole-array actual value-lower 불가 + callee array shape이 child instance elaborate 전 미지. **fix(§4.5.202~206 array machinery 재사용)**: hier_tasks gate가 INPUT fixed-array formal도 admit·`DeferredHierTaskCall.arg_arrays`에 defer 시 actual static-array net을 caller scope서 resolve해 저장·resolve 시 `frame_arr_formal_meta[base_net+slot]`로 callee shape 조회→신규 `pack_hier_array_actual`(=`lower_array_actual_packed` resolve-time twin)로 md-packed slot value 생성해 in_bind. INPUT만(output/inout array=loud·copy-out 미구현)·shape/type mismatch=loud·per-instance 격리. no-oracle→hand-IEEE·1-D/mixed-register-file/2-D/per-instance/signed-deep-path 全 MATCH·format 22 불변·+9 tests·상세 ARCHIVE §4.5.207). 직전 = **§4.5.206(NON-ZERO-BASE ASCENDING array formal RESOLVED·per-dim base threaded** — 배열 formal shape 마지막 갭(`int m[1:4]`). base+direction 상호작용이 예측 불가라 **cleanly-verifiable subset만**: non-zero-base ASCENDING만 지원·non-zero-base DESCENDING은 loud(correct-or-loud·억지 지원=silent-wrong). **fix**=`ArrayFormal.dims`를 `(lo,size,ascending)`로 확장(tuple widening·compiler가 모든 reader 잡음)·`classify_unpacked_array` non-zero reject 제거(lo=min·non-zero+desc는 loud)·`array_formal_ext_dims`가 packed dim에 lo 세팅→`flatten_word`가 `idx-lo`(zero-based lo=0→byte-identical)·dim-check가 actual base도 MATCH·copy-out가 `caller[lo+pos]`. classify 공유로 frame-local도 bonus. no-oracle→hand-IEEE·1-D/2-D/output/mixed 全 forward·non-zero-desc/base-mismatch loud·zero-based byte-identical·format 22 불변·+6 tests·flip 2·상세 ARCHIVE §4.5.206). 직전 = **§4.5.205(DESCENDING / mixed-direction multi-dim array formal RESOLVED·over-conservative gate 제거** — §4.5.202가 descending multi-dim을 loud로 gated("index-major vs declaration-major reversal" 우려)했으나 **1-D descending은 이미 forward 동작**(그 우려는 잘못된 mental model). 경험적 재검증(distinct-value·**non-square 2×3이 reversal 판별 clincher**)이 descending/mixed/3-D 全 forward(m[i][j]=a[i][j]) 확인·direction MISMATCH은 `lower_array_actual_packed` per-dim 체크가 이미 loud 처리. **fix**=`classify_array_formal`의 descending reject gate 제거(per-dim direction MATCH가 진짜 correctness guard·gate는 redundant+wrong). no-oracle→hand-IEEE·format 22 불변·+2 tests·flip 3·상세 ARCHIVE §4.5.205). 직전 = **§4.5.204(OUTPUT/INOUT multi-dim array formal RESOLVED·multi-index copy-out unpack** — §4.5.202가 output/inout multi-dim을 loud로 남겼음(§4.5.193 copy-out unpack이 `caller[i]=packed[i*ew +: ew]` 1-D 인덱스라 multi-dim caller엔 partial-index→E3009). **fix**=unpack loop LHS를 fully-indexed `caller[i0][i1]…`로 일반화(row-major flat index를 `af.dims`로 strides 분해→nested `BitSelect`). packed temp가 flat row-major라 `packed[i*ew +: ew]`=element `i`·1-D는 strides=[1]로 byte-identical. reserve/copy-in은 §4.5.202가 이미 multi-dim→unpack만 갭이었음(최소 수정). no-oracle→hand-IEEE §13.5.2·output/inout 2×2·non-square·3-D·signed·INOUT RMW·static output(§4.5.203 force-frame 조합) 全 MATCH·descending output loud·format 22 불변·+4 tests·flip 3·상세 ARCHIVE §4.5.204). 직전 = **§4.5.203(STATIC(non-`automatic`) task with a FIXED unpacked-array formal RESOLVED·force-frame** — §4.5.202 적대 검증 중 발견한 인접 갭. `task load(input int m[4]);`(static·array formal)이 vita E3009 — array formal의 md-packed value slot이 FRAME 경로에만 존재하고 inline(static-task) 바인딩엔 slot 없어 `task automatic`만 지원됐음. **fix**=`build_task_frame_set`에 트리거 1개 추가(신규 `&self` 헬퍼 `is_fixed_unpacked_array_formal`이 true인 formal 가진 task를 force-frame·§4.5.200 hier force-frame 골격 재사용). frame ⊇ inline(§4.5.198/199/200)이라 static task framing해도 local caller 능력 손실 0→md-packed 경로 바인딩. reject shape(descending·non-zero-based·output/inout multi-dim)은 framed reserve가 Some(Err)이라 여전히 loud. no-oracle→hand-IEEE §13.5.1/2·1-D/2-D/output/inout/register-file/suspend/inline→framed/pass-by-value/signed/refresh 全 MATCH·static-local 저장 의미 보존·format 22 불변·+14 tests·상세 ARCHIVE §4.5.203). 직전 = **§4.5.202(multi-dimensional array FORMAL on framed function / `task automatic` RESOLVED·hand-IEEE** — §4.5.199의 N-D md-packed slot(`array_formal_ext_dims`+`flatten_word`)을 subroutine array FORMAL로 재사용. `classify_array_formal`의 `dims.len()>1` blanket-reject를 **ASCENDING zero-based-only** gate로 완화(descending은 md-packed index-major vs actual physical declaration-major 비대칭→dim 내 element reverse silent-wrong→loud)·formal reserve 2곳(`reserve_frame_func`/`reserve_frame_task`)을 `array_formal_ext_dims`로 통일(1-D byte-identical)·`lower_array_actual_packed`의 actual dim-check을 per-dim (size+direction) 일치로 일반화. iverilog 구문 거부(no-oracle)→hand-IEEE §13.5.1 pass-by-value(body element write=local·caller immune·cross-suspend immune)로 검증·2D/3D/non-square/signed byte $signed/packed-elem/2-state coerce/runtime index/forwarding 全 MATCH. LOUD=descending·shape-mismatch·partial-index·STATIC task(pre-existing)·hier array formal(별개)·OUTPUT/INOUT multi-dim(copy-out가 multi-dim caller에 whole-array assign 거부·별개 follow-on). format 22 불변·+19 tests·flip 2·상세 ARCHIVE §4.5.202). 직전 = **§4.5.201(hierarchical task OUTPUT/INOUT scalar formal RESOLVED·cross-boundary copy-out** — hier-task이 input-only였던 것을 output/inout까지. defer 시점 callee port 방향 미지 문제를 각 arg를 value+lvalue로 dual-lower + `hier_task_port_dirs` sidecar로 resolve 시 방향별 라우팅(Input→in_bind·Output→out_bind·Inout→both). 기존 out_binds copy-out machinery 재사용·엔진 무변화·format 22 불변. iverilog 全 MATCH(IEEE partial-write 포함)·+2 tests·상세 ARCHIVE §4.5.201). 직전 = **§4.5.200(STATIC-task hierarchical call `u1.tk()` RESOLVED·frame↔inline parity step 3·hier-task 완성** — §4.5.199로 frame⊇inline 완성 후 static task도 force-frame 안전. 전역 pre-scan(`collect_hier_task_stmt`가 모든 모듈 proc block서 2+세그 UserTaskCall 이름 수집)→`build_task_frame_set` force-frame→기존 §4.5.197 hier defer/resolve가 bind. name-based over-application·under-collection 모두 무해/correct-or-loud. **local+hier 동시 호출 회귀 0**·format 22 불변·적대 differential 全 MATCH·**hier-task 완성(automatic+static)**·+9 tests·상세 ARCHIVE §4.5.200). 직전 = **§4.5.199(multi-dim frame-LOCAL array RESOLVED·frame↔inline parity step 2·마지막 갭** — differential-sweep로 frame⊂inline 유일 잔여 갭=multi-dim frame-local array 규명. frame-local array=md-packed part-select(`packed_dims`+`flatten_word` N-D 머신러리 이미 존재)라 `classify_unpacked_array` multi-dim 수용 + `reserve_frame_local_decl`가 dim당 packed_dims entry 예약하면 `m[i][j]`가 기존 packed chain으로 동작·1D byte-identical·partial-index silent-wrong guard·format 22 불변·적대 differential 全 MATCH·**frame ⊇ inline 완성**→STATIC hier-task force-frame 회귀 없이 안전·+10 tests·상세 ARCHIVE §4.5.199). 직전 = **§4.5.198(frame task MODULE array-element write RESOLVED·frame↔inline parity step 1** — 정확도 사다리[silent-wrong ≪ loud ≪ correct-support·올라가되 내려가지 마라]: static hier-task naive force-frame은 동작하던 local 호출을 loud화=오답→올바른 방향=frame⊂inline 비대칭 해소[hier-task 무관한 오늘의 갭 `task automatic; mem[i]=v;`=vita loud vs iverilog 정상]. fix=§4.5.197 signal 규칙의 `word.is_none()` 게이트 제거→모든 out-of-frame write chunk[array-elem 포함]를 suspend-signal화→suspendable `&mut` path lift·format 22 불변·적대 differential 全 MATCH·class-field over-lift 무해·+1 test·상세 ARCHIVE §4.5.198). 직전 = **§4.5.197(hierarchical TASK call `u1.tk(x)` RESOLVED + frame-aware suspend-classification 인프라** — 진짜 blocker=`compute_suspendable_tasks`가 frame-aware 아니었음[automatic task의 module-net blocking write를 non-signal로 봐 subset 분류→`&self` executor write 불가라 loud]→`base_nets` 배선으로 frame window 밖 write를 suspend-signal화[format 22 불변·func_metas verbatim-thread라 pure-fn 유지]·이게 non-hier P1도 해소·hier-CALL은 §4.5.196 defer 미러[`hier_tasks`+`DeferredHierTaskCall`+`resolve_deferred_hier_task_call`·input-only scalar]·적대 differential 全 MATCH·잔여 loud=output/inout formal·STATIC task hier-call[force-frame=large·**단 §4.5.198이 회귀 없는 정공법[frame⊂inline 동등화] step 1 착수**]·~~module array-elem write~~[**§4.5.198 RESOLVED**]·+20 tests·상세 ARCHIVE §4.5.197). 직전 = **§4.5.196(round-17 D-가족 tractable 4항목 RESOLVED** — import-in-pkg[apply_import_consts]·$display-in-subset-function[frame_print_stmts+render arm]·hierarchical function call `u1.f(x)`[hier_funcs+DeferredHierCall·M-subset]·task inout array[§4.5.188+193 조합]·follow-on=hier-task[L]·function-reforward[risky]·block-local-auto-lifetime[deep]·chained-method[vague]·format 22 불변·+14 tests·상세 ARCHIVE §4.5.196). 직전 = **§4.5.195(round-17 리포트 3-가족 A/B/C + CLI 이원-top 전부 RESOLVED** — A=queue-of-record[파서 `Dim::Queue(None)`→packed-vector lowering + packable struct/tf-port unified whole-vector]·B=non-packable struct tf-local[파서-only, block-local 청사진 미러]·C=frame-body dyn-formal call[§4.5.194 RefCell로 `&self` executor marker 실행·gate `in_frame_body` 제거]·CLI=one-shot `--top` 허용+auto-top ambiguity W3057 경고화·format 22 불변·+25 tests·상세 ARCHIVE §4.5.195). 직전 = §4.5.194(**round-16 executor-bound gaps 전부 RESOLVED** — `dyn_heap`를 `RefCell`로 interior-mutable화[6-phase: 0 plumbing·1a V2A-dyn subset task·1b write-path·2a V5 function/task dyn-local+new[]·2b V2B output/inout dyn formal]·framed-nested 동작·format 22 불변·+25 tests·상세 ARCHIVE §4.5.194). 직전(round-16 7-슬라이스) = §4.5.193(**output unpacked-fixed array formal on TASK loud→supported** — 리포트 "unpacked-array formal on task" 잔여 OUTPUT half[`output byte digest[N]`]. body가 input과 동일 md-packed slot 씀→exit서 whole slot을 caller-side packed temp로 out-bind[scalar whole-net copy]→`ret` block서 `caller[i]=packed[i*ew+:ew]` unpack[AST+`lower_stmt`·heap/format 무변화·동기·suspendable 양쪽]·correct-or-loud[INOUT/non-bare/classifier-reject=loud·IEEE §13.5.2 pass-by-value=formal default 0/X]·element-wise ref differential 全 MATCH·`frame_task_output_array.rs`×8·+8) · §4.5.192(**packable unpacked-struct scalar body-local in task/function loud→supported** — round-16 V8[TB=partial]. `rec_t p;`가 tf-body서 E2002[unpacked struct 타입은 `typedefs` 아닌 `unpacked_struct_layouts`에만→`peek_block_typedef_decl` miss]. `parse_body_unpacked_struct_local`가 인식→packed-vector frame-local로 lowering+scalar-struct 등록→`p.field`가 §4.5.190 part-select desugar[`struct_field_select`에 `packable_record_layout` fallback]·correct-or-loud[non-packable/array/decl-init=loud·module-scope member-net 유지]·`frame_local_unpacked_struct.rs`×8·+8) · §4.5.191(**FIXED 1-D array of packable unpacked struct loud→supported** — round-16 V6[TB=top·axi_mem_model `mem[addr].field`]. packable record 고정배열을 packed-vector 고정배열 `logic/bit [W-1:0] mem[N]`로 lowering+`struct_1d_array_vars` 등록→`mem[i].field`가 §4.5.190 desugar[`struct_array_field_geom`에 `packable_record_layout` fallback]·correct-or-loud[non-packable/multi-dim/decl-init=loud·queue·dynamic 무영향]·`fixed_record_array.rs`×8·+8) · §4.5.190(**`arr[i].field` on packed-struct 1-D array loud→supported** — register-file/memory idiom가 read/write E3010였음[element-member desugar는 dynamic record array/SoA만]. `struct_member_expr`를 base=EXPR로 일반화[`struct_member_expr_of`]·`parse_struct_field_lval`를 base=LVALUE로 일반화·postfix branch[read+write]로 element part-select desugar·iverilog가 packed-struct array 필드접근서 크래시→plain packed-vector part-select offset 교차검증+self-consistency·`struct_array_member.rs`×10·+10) · §4.5.189(**SILENT-WRONG 수정: loop-body block-local initializer 매 진입 아닌 1회만 실행 → per-entry** — automatic task/function 루프 body의 `int t=f(k)` init이 frame ENTRY서 1회만[iverilog는 매 반복 재실행·1/11/21 vs vita 1/1/1]. `lower_frame_task_body`/`lower_frame_func_body`가 nested block-local init을 frame entry에 방출하던 것을[코드 주석="single-entry approximation"] block ENTRY[`Block` arm·`in_frame_body` gate]로 이동→IEEE §6.21·top-level body_decls는 frame entry 유지·no-init read-before-write는 slot 지속[iverilog parity]·module-process static 불변·`frame_block_local_init.rs`×9·+9) · §4.5.188(**input unpacked-fixed array formal on TASK loud→supported** — round-16 최대 task 클래스[sha2 16·sha3 3]. 함수는 md-packed로 이미 지원·task만 blanket-loud였음. `reserve_frame_task`/`emit_frame_task_call`/UARR guard 3곳에 함수 md-packed 경로 미러[value slot이라 동기·suspendable 양쪽]·correct-or-loud[static task/output/inout=loud·pass-by-value no-leak·size mismatch=loud]·`frame_task_unpk_formal.rs`×9·+9) · §4.5.187(**$fopen 런타임 파일명 loud→supported** — round-16 file-I/O 층[CAVP walker·sha2 9]. `$fopen`이 string LITERAL만 수용했음. elaborate 게이트 완화+engine `k_fopen`이 Const/is_str/packed-reg 모두 resolve[§21.3 iverilog parity]·`fopen_runtime_name.rs`×4·+4) · **그 이전** §4.5.186(**constant-function evaluation in const contexts loud→supported** — fresh-area probe서 발견. `localparam W = clog(256)`[while-loop 함수]가 vita E3009 "not foldable"인데 iverilog `8`·흔한 파라미터 계산 idiom. `const_eval_in_scope`가 사용자 `Call` 미처리였음→elaborate-time 정수 함수-body 인터프리터: `const_func_table`을 param fold 전 populate·`Call` arm→`eval_const_call`[formal bind·body 실행·return width coerce]·`eval_const_env`+`exec_const_stmt`[Block/blocking `=`/if/for/while/repeat/return]·`const_binop` 추출로 폴딩 공유. **correct-or-loud 극엄[param 값 silent-wrong=P0-5 최악]**: 정수 도메인 밖·output/array formal·런타임 신호 참조·systask/timing·recursion depth cap(64)·loop step cap(100K→비종료가 hang 대신 loud) 全 loud. i64 도메인=기존 const_eval 일관. 적대 differential 全 MATCH[clog/recursion/for/byte-coercion/param+runtime 회귀]·`const_function_eval.rs`×15·format 22 불변·+15) · §4.5.185(**`$bits(TYPE)` loud→supported** — fresh-area probe서 발견. `$bits(logic[15:0])`(parse error)·`$bits(s_t)`(typedef name→E3010)가 vita 거부인데 iverilog `16`·`7`·`$bits(변수)`는 동작[TYPE 인자만 gap·흔한 idiom `logic [$bits(T)-1:0]`]. `$bits`는 compile-time 상수·파서가 타입 폭 보유→SystemTask arm서 `$bits(TYPE)`를 IntLit로 fold[`parse_bits_type_arg`=keyword+dims 또는 bare type-name·`bits_of_type_name`=struct Σ/union MAX/typedef width×∏packed]·decl-range/param 위치도 동작. correct-or-loud: 초판 `$bits(real)`=1 silent-wrong 적대 probe서 발견→**INTEGRAL만 fold**[real/string/event는 loud]·`$bits(real_var)`=64는 elaborate라 무영향·적대 differential 全 MATCH·`bits_of_type.rs`×11·format 22 불변·+11) · §4.5.184(**multi-dimensional packed array struct/union member loud→supported** — fresh-area probe서 발견. `union packed { logic [1:0][3:0] nib; }`가 vita parse error[2번째 dim]인데 iverilog ✓·독립 multi-dim packed net은 지원[멤버만 거부·의도적 v1 한계]. 핵심=struct 멤버가 파서서 part-select로 desugar→**파서-only·format 불변**[AST StructMember만 확장·sim-ir 미도달]. StructMember에 `packed_dims`·`member_flat_dims`[flat=base×∏inner·elem_stride=∏inner]·StructLayout tuple에 elem_stride·`parse_struct_field_sel`가 `s.m[i]`→`Indexed{i*stride +: stride}`[기존 IndexedPart 재사용]. correct-or-loud=element WRITE/range/ascending/nested `s.m[i][j]`/record-array multi-dim은 loud[follow-on]·runtime index는 지원[iverilog 초과·hand-verify]·적대 differential 全 MATCH·`multidim_packed_struct_member.rs`×13·format 22 불변·+13) · §4.5.183(**SILENT-WRONG 수정: block-local `string s[] = '{…}` dyn-array init 조용히 drop → supported** — §4.5.182 적대 검증 중 발견. `string s[]='{...}`를 initial/always BLOCK 내부에 두면 vita SZ=0[빈 배열] vs iverilog SZ=3[module-scope 선언은 정상]. 근본 원인=block-local decl-init 수집기의 `scalar_string` 판정이 per-name unpacked dim 무시→dimensioned string도 scalar 취급→push gate `name.unpacked.is_empty()`서 drop. module-scope 수집기는 이미 `is_dyn_str_init` 특례 有→**두 scope 비대칭**. fix=block-local gate에 동일 조건[`Dim::Dyn`+`AssignPattern`] 미러→기존 flush 라우팅으로 supported화[module parity]·fixed/multi-dim/`{…}`concat string은 조건 미매칭→loud 유지·적대 differential 全 MATCH·format 22 불변·+9) · §4.5.182(**queue/dyn-array `{…}` unpacked-array concat decl-init loud→supported** — fresh-area probe서 queue 메서드 차분 중 발견. `int q[$]={1,2,3,4}`·`int d[]={a,b,c}`가 vita는 `'{…}` assignment-pattern만 수용해 E3009였음[SV §10.10 unpacked concat=합법·iverilog ✓]. 신규 헬퍼 `dyn_pattern_elems`가 `AssignPattern|Concat`서 균일 element 추출→Concat init을 기존 `'{…}` var-init flush에 라우팅[handle-gate+flush-dispatch 2곳·collect는 `fold_init`/`const_eval`이 Concat에 None이라 무수정]. **correct-or-loud by construction**: `{…}`를 `'{…}`와 byte-identical화→모든 원소 타입서 기존 경로의 loud 상속[array-elem `{a,3}`=E3009·replication `{n{x}}`=별도 Replicate node라 loud·STRING 원소는 gate서 제외→loud, silent-empty 방지]·적대 differential 全 MATCH·format 22 불변·+14) · §4.5.181(**enum `.next(N)`/`.prev(N)` CONSTANT-step loud→supported** — fresh-area probe서 발견한 oracle-backed loud gap. `e.next(2)`가 step 인자로 arg-less enum-method desugar를 건너뛰어 generic Call→E3009였음. 파서에 constant-step 분기: `var_enum` 멤버의 `next|prev`+single literal arg면 `enum_step_n_expr`가 N-step ternary chain 생성[label→N 위치·`rem_euclid` wrap·`prev(N)`=`next(len-N)`]·non-constant step은 loud 유지·arg-less `.next()` byte-identical·AST/IR 무변화·적대 differential 全 MATCH·format 22 불변·+5) · §4.5.180(**SILENT-WRONG 수정: same-named STATIC block-locals in DISJOINT blocks → loud** — fresh-area iverilog 차분 probe로 발굴한 pre-existing 버그. 두 sibling/named/cross-process 블록이 같은 이름 static 블록-로컬을 선언하면 v1 flatten이 한 module net으로 합침→둘째 블록이 첫째의 TYPE[sign/width]+STORAGE를 조용히 상속[`%d` 부호·`>>>` 산술·`%h` 폭·uninit read 全 오염]. `hoist_block_local_nets`의 collision 경로에 type-mismatch + definite-assignment guard[E3009]·SAME-type+assigned-first coalesce는 무영향·module-var SHADOW는 `local_decl_names` 제외로 무영향·`block_local_name_collision.rs`×9·format 22 불변·+9) · §4.5.179(**FRAMED function dyn-formal call BURIED in an expression loud→supported** — §4.5.177 direct-rhs 후속. `$display(f(a))`·`r=f(a)+1`·`if(f(a)>0)`의 buried framed dyn-formal call을 R5-B inout-hoist 骨格 재사용해 `__t=f(a)` temp[direct-rhs blocking assign]으로 hoist→§4.5.177 snapshot marker 재발화·읽기 위치는 temp로 대체. framed function=pure[output formal 無]라 eval-order guard 불요·무조건-평가 위치만 hoist[If-cond·Blocking-rhs nested·SysTaskCall args]·short-circuit `&&`/`||` RHS·`?:` arm·`while`/`case` scrutinee·frame-body 내부 call=loud 유지[correct-or-loud]·각 hoist가 자기 snapshot marker→mutation 사이 pass-by-value freshness·gate=`dyn_formal_func_names` 비어있으면 byte-identical·적대 differential 全 MATCH[15·115·big·snapshot 15→110·two-args]·format 22 불변·+14) · §4.5.178(**SILENT-WRONG 수정: dyn-array `input` formal WRITE in a synchronous frame body → loud** — §4.5.177이 formal을 heap snapshot으로 READ 지원했으나 body가 그 formal을 WRITE[`b[0]=9`·`foreach(b[i]) b[i]=…`]하면 `&self` frame executor[`run_frame_call`/`run_task`]의 heap store[`write_lvalue`→`dyn_write`]가 `&mut`라 불가→write가 unused scalar frame slot로 조용히 유실·`r=f(a)`가 pre-write 값[1 vs IEEE 9] 반환하던 pre-existing 버그[snapshot-soundness 논증이 read-only body 암묵 가정]. **fix**: `frame_write_lvalue` 상단 runtime guard가 heap dyn-array write 도달 즉시 F4004[실제 write 시도 포착→element/whole/`new[]` 균일·string slab-store 제외·read-only body 무영향]·적대: write=loud[exit≠0]·read-only=정상 sum·format 22 불변·+4) · 그 이전 §4.5.177(**FRAMED function `input` dynamic-array formal loud→supported** — "순차적으로 전부" 원래 목표. control-flow-body automatic function의 `input c[]`을 per-activation `NetKind::DynArray` net으로 reserve[`reserve_frame_func`]하고 direct-rhs `r=f(arr)`서 caller 배열을 `handle_copy` snapshot marker로 deep-copy[module-process level의 `&mut` executor가 marker 실행]·함수 body는 heap서 read[§4.5.176이 `foreach(c[i])` 동작시킴]·framed function은 `&self`라 dyn-array 변형 불가→snapshot=sound·**correct-or-loud by construction**[marker 없는 call=`emit_frame_call`서 loud→non-direct-rhs/nested-caller/recursion/non-bare/sign-mismatch 全 loud]·적대 differential 全 MATCH[int/byte/signed·two-call 격리·pass-by-value]·format 22 불변·+9) · §4.5.176(**`foreach` over dynamic array/queue/assoc inside a FUNCTION/SUBSET-task body loud→supported** — §4.5.175서 loud화한 것을 supported로: iteration key가 항상 frame-local이라 `&self` 동기 executor[`run_frame_call`/`run_task`]가 interior-mutable frame window로 advance 가능·`assoc_iter_step`의 read/compute를 `SimState::assoc_iter_compute`로 추출[process path는 이걸 call→byte-identical]·`frame_assoc_iter`가 key를 `frame_write_lvalue`로 write·적대 differential 全 검증[dyn/queue/assoc·first/next/last/prev·nested·signed]·잔여 loud=non-local key direct iter·format 22 불변·+2) · §4.5.175(**SILENT-WRONG 수정: `foreach` over dynamic array/queue/assoc inside a FUNCTION/SUBSET-task body → loud** — desugar된 walk step `__st=a.first/next(__i)`이 iteration key를 side-effect로 write하는데 동기 `&self` frame executor[`run_frame_call`/`run_task`]가 못 advance→조용히 0 반환하던 것을 runtime fatal F4004로 loud화[`rhs_is_assoc_iter` guard·correct-or-loud]·적대 리뷰서 `function; foreach(marr[i])`=vita 0 vs iverilog 15로 발굴·suspendable task/`for`-loop workaround/fixed-array 미영향·format 22 불변·+8) · §4.5.174(**inline static-task `foreach`-on-dyn-formal loud→supported** — 파서가 `foreach(b[i])`를 `b.first/next(__i)`로 desugar하는데 dyn/queue dense-walk dispatch가 `dyn_handle`로 array를 resolve해 `dyn_subst` formal alias를 못 봐 E3009였음→`dyn_handle_read`[alias 우선]로 2 site 교체·formal이 module dyn-array와 동일 dense walk 라우팅·`dyn_subst` empty 밖에선 모든 array byte-identical·적대 differential 全 MATCH·`inline_foreach_dyn_formal.rs`×8·format 22 불변·+8) · §4.5.173(**V2A-frame: AUTOMATIC task `input` dynamic-array formal loud→supported** — formal을 per-activation `NetKind::DynArray` heap slot으로 예약하고 caller 배열을 frame ENTRY서 deep-copy[pass-by-VALUE·IEEE §13.5.1·`frame_dyn_snapshot_formals`]·V5 net-range lifecycle이 formal 자동 커버[free/reentry]·적대 differential 全 MATCH[특히 **snapshot-immune-across-suspend**]·LOUD=recursion/concurrent/non-suspendable-subset/sign-mismatch·wire 무변화[TaskCallInfo 필드 추가 회피→format 22 불변]·`frame_dyn_formal.rs`×13·+13) · §4.5.172(**frame-body validator over-scan false-REJECT 수정** — subset task를 not-defined-last일 때 뒤 func 블록까지 over-scan해 E3009 false-reject하던 것을 linear scan→**reachable-block CFG walk**로 교체[entry서 자기 CFG 엣지만·`Call`→`ret_bb`라 함수 밖 안 나감]·correct-or-loud[미방문=타 func/dead→false-reject만 DROP]·적대 differential 全 MATCH·`frame_subset_overscan.rs`×4·format 22 불변·+4) · §4.5.171(**V5: frame-local(task-body) DYNAMIC array loud→supported** — "순차적으로 전부" 3순위·`task automatic; int loc[]; loc=new[n]; loc[i]; loc.size()`·**per-net heap + lifecycle**[30-site 재키잉 회피: `dyn_heap[net]`=현재 활성화 배열·`enter_task_frame` reentry guard(recursion/concurrent→fatal F4004)·`exit_task_frame` free-at-exit]·read_net/write_lvalue가 frame dyn-array element op을 heap으로 라우팅·회귀 발굴[frame-local STRING이 `dyn_is_handle` 공유→`kind!=String` 가드로 수정]·function은 `&self` 동기 executor 한계로 loud·적대 differential ~18 probe MATCH·format 22 불변) · 직전 §4.5.170(**V2A: TASK `input` dynamic-array formal loud→supported** — "순차적으로 전부" 2순위·R11 함수 dyn-array 머신러리를 **static task inline 경로**에 확장·**pass-by-value SNAPSHOT**[적대 soundness agent가 초판 direct-alias의 IEEE §13.5.1 위반 silent-wrong 발굴→entry deep-copy(`alloc_dyn_snapshot`+`handle_copy_stmts`)로 수정; 함수는 statement body reject라 alias 안전하나 task는 statement body라 mid-body mutation 누출]·automatic/frame task는 handle-in-slot(V5) 필요→loud-defer·공통 fix로 `dyn_array_actual_net`이 `dyn_subst` 우선 consult→**re-forward가 함수·태스크 양쪽** 동작·format 22 불변) · 직전 §4.5.169(**frame-local unpacked ARRAY loud→supported** — §4.5.168 잔여 loud 첫 항목·"순차적으로 전부" 1순위·배열 FORMAL의 md-packed 표현 재사용[`classify_unpacked_array` 공유·`reserve_frame_local_decl` 4-site 통일]·엔진 frame_local=net-range라 md-packed net 자동 격리[per-activation/재귀]·적대 differential ~20 probe common 全 MATCH+automatic-array 격리·subroutine array port는 vita가 iverilog 초과[hand-IEEE]·잔여 loud=multi-dim/non-zero-based/non-simple-element/whole-copy/foreach/NBA-elem·format 22 불변) · 직전 §4.5.168(**V3/V4 suspendable tasks** — task 内 `@`/`#`/wait/NBA/$systask/재귀/nested가 scheduler call-stack + window stash/restore로 동작·format 22 불변[recompute, no sidecar]·적대 2-lens로 core SOUND 실증 + 5 over-lift 결함 correct-or-loud화). 최종 목표 = **G1**(icarus·verilator·xcelium·vcs급 정확성·correct-or-loud) + **G2**(AI-Agent 친화·SPEC=[preview/19](preview/19-ai-agent-observability.md)).
>
> **운용 규칙**: 슬라이스 완료 시 → 상세 로그를 ARCHIVE "완료 슬라이스 로그"에 append(§4.5.x 양식·최신이 위), 이 문서의 해당 잔여 항목 삭제. 신규 발굴은 아래 해당 섹션에 1줄로 추가.

## 0. correct-support 승격 큐 (2026-07-25 전수 재그라운딩 — **오너 지시로 최상위**)

> **목적**: 지금까지 correct-or-loud로 **LOUD 유지**한 항목 중 *실제로 구현 가능한 것*을 골라 correct-support로 올린다. 아래는 §3/§4 전체를 훑고 **12개 후보를 iverilog로 직접 재현**해 (a)아직 loud인지 (b)오라클이 있는지 확인한 결과다. 재확인에서 **4건이 stale**로 드러나 아래 "정정" 항목으로 이동했다.
>
> **순위 근거**: 전부 iverilog 오라클 有 = §1 우선순위 ②(additive·저위험). T1은 한 가족이라 머신러리가 공유되고, **dyn string 배열 경로가 이미 全 지원**이라 fixed를 그쪽 표현으로 라우팅하는 미러가 유력(§4.5.217서 확인). T2는 서로 독립적이라 개별 슬라이스.

**T1 — string-array 가족 (오라클 ✓ 7/7 재현·공유 머신러리·가장 높은 ROI)**

> **1번 RESOLVED §4.5.219** · **전제조건(dyn byte-select) RESOLVED §4.5.220** — 이제 **dyn ⊇ fixed**가 성립하므로 T1-2/3(runtime index·`foreach`)의 fixed→dyn 라우팅이 byte-select 퇴행 없이 가능하다. 라우팅 시 확인할 것: 저장-클래스 변경이므로 read 全 경로 재확인 + `new[]`/`.size()`가 fixed에서 loud로 남는지(fixed 배열에 `new[]`은 silent resize가 되면 안 됨) + non-zero-base/descending 선언의 인덱스 오프셋. 아래는 §4.5.219 당시 실측 기록: **fixed를 dyn 표현으로 통째 라우팅하는 안은 기각** — capability-parity 매트릭스에서 dyn이 init/runtime-index/`foreach`/`.size()`는 앞서지만 **byte-select `s[0][0]`는 fixed만 정확**(119 vs 0)이라, 라우팅하면 byte-select가 silent 0으로 퇴행한다. 2·3(runtime index/`foreach`)을 하려면 **먼저 dyn element의 byte-select를 고쳐 dyn ⊇ fixed를 만든 뒤** 라우팅하거나, element-net 경로를 직접 확장해야 한다.

1. ~~FIXED string array **decl-init**~~ **RESOLVED §4.5.219**. 원래 항목: FIXED string array **decl-init** `string s[2]='{"aa","bb"}` — iverilog `aa bb` / vita E3009. module·block 양쪽.
2. FIXED string array **runtime index** `s[i]` — iverilog `bb` / vita E3009 "requires a constant index".
3. FIXED string array **`foreach`** — iverilog `0 aa 1 bb` / vita E3009(2와 동근).
4. **`string q[$]`**(queue of string) — iverilog `1 aa` / vita E3009 "takes no packed/unpacked dimensions".
5. **multi-dim** `string s[2][2]` — iverilog `aa` / vita E3009(4와 동일 gate).
6. **hierarchical** `u.s[0]` — iverilog `aa` / vita E3010.
7. **frame-local**(task/function body) string array — iverilog `aa` / vita E3009 subset reject.

**T2 — 독립 항목 (오라클 ✓·각자 전용 슬라이스)**

8. **`real` const-fold 전면 미지원** — `localparam real R = 2.0+3.0` = iverilog `5.0` / vita E3009 "not a foldable". `*`·`/`·`-`·`**` 전부 동일. **`localparam real`은 흔한 관용구**라 체감 가치가 크다(§4.5.141 발굴).
9. **generate/interface 스코프 string decl-init** — `begin : g string s = "hi";` = iverilog `hi` / vita E3009 "supported only at module scope". 모듈 스코프는 이미 동작 → **스코프 비대칭 = 정확도 갭**(§4.5.198 논리).
10. **sized-literal enum label → enum-method** — `enum bit[3:0]{A=4'h3}` + `.name()` = iverilog `A` / vita E3009. `const_lit`이 unsized-decimal만 fold해 `enum_defs` 미등록. 진단 문구도 오도("hierarchical function call").
11. **음수 range bound** `logic [3:-2]` — iverilog 6-bit `101010` / vita **W3056 warn+clamp**(non-silent이나 whole-value 손상).

**T3 — 전제조건 필요 (즉시 착수 대상 아님)**

12. `$fmonitor`/`$fstrobe` — 파일 출력이 W3056로 skip. 지원엔 **format bump** 필요(SysTaskId 변종 or 사이드카).
13. `case (x) inside {…}` — **no-oracle**(iverilog 13.0이 `case inside`/`inside` op/array reduction 전부 거부) → hand-IEEE + 내부 차분.

**정정 — 재그라운딩에서 stale로 판명(§3에서 삭제, 비목표)**

- ~~package `function string`~~ · ~~package control-flow 함수~~ — 둘 다 **이미 동작**(`hi` / `9` 확인).
- ~~generate 스코프 queue/dyn decl-init~~ — queue **이미 동작**(`4` 확인). 잔여는 string decl-init뿐(위 9번).
- ~~`always @(*)` string concat 조용히 drop~~ — **오진**. vita는 `[abcd]`로 **정확**하고 iverilog가 빈 문자열을 낸다. 명시 `@(a,b)`는 vita가 정직하게 loud. §4.5.217 리포트의 이 줄은 잘못 기록된 것.

> **주의(정직한 순위 고지)**: 프로젝트 정본 우선순위(§1)는 **① 오라클 있는 CRITICAL silent-wrong > ② loud→supported**다. 아래 §0-B의 **inner NET vs outer PARAM shadow**는 여전히 ①-급(silent-wrong)이므로, 위 승격 큐를 먼저 하더라도 그 항목이 사라진 것은 아니다.

## 0-B. NEXT — 재개할 deep-defer follow-on

> **round-18 리포트 8-가족 RESOLVED(§4.5.213·2026-07-24)** — 외부 리뷰어 round-18 리포트의 잔여 8-가족(A/G queue/array-of-non-packable-record + foreach·D automatic-block-local-init·E1 enum-method-on-formal·E2 struct-member-method + string-dyn-element·F1 output-formal-fn-in-loop-cond·F2 severity-in-frame-body·F3 wrapped-dyn-formal) + C1 const-repeat를 correct-support화(hand-IEEE/iverilog 차분). 상세=ARCHIVE §4.5.213.
>
> **C1 part 2: fork-in-frame RESOLVED(§4.5.214·2026-07-24)** — `fork…join[_any|_none]`을 suspendable(framed) task 내부에서 실행하는 것이 "깊은 스케줄러 rework·blast radius=frame 서브시스템 전체"라 correct-or-loud LOUD 유지 중이었으나, 재조사 결과 **단일-스레드 스케줄러 + 기존 `stash_frame_windows`/`restore_frame_windows`가 이미 concurrent children을 parked parent로부터 격리**하고 있어 arm이 부모 frame-local을 안 건드리는 **Case A**(리포트 repro)는 기존 owned-window 모델로 즉시 동작함을 확인 — 신규 인프라가 필요한 건 arm이 부모 frame-local을 read/write하는 **Case B**뿐(interior-mutable arena `WindowSlot::Shared`, dyn_heap/class_heap과 동형). 3-stage(Case A·Case B `join`-all arena·Case B `join_any`/`join_none` refcount)로 전달 + final-review가 fork arm 내부 `return`의 silent frame-corruption 회귀를 잡아 즉시 loud화. format 23 불변. 상세=ARCHIVE §4.5.214.
>
> **§0 NEXT 최상단(2026-07-25 갱신·§4.5.218서 string-array 절반 RESOLVED)** — **inner NET이 outer PARAM/enum-label을 shadow 못 함**: §4.5.218이 string-array side-map은 opt-in shadow walk로 해소했으나 `params`/`param_meta` consumer는 **순서 의존** 때문에 손대지 못했다(초판이 시도했다가 중첩 generate body를 조용히 삭제 — ARCHIVE §4.5.218 S1). 오라클 有(function-local `int W`가 module `localparam W`를 안 가림=vita 4 vs iverilog 9). **전제 = order-INDEPENDENT AST-gathered per-scope name set**(`gather_local_decl_names`/`compute_scoped_block_locals`의 "pure function of the AST" 패턴)—이것 없이 params consumer를 켜면 S1이 그대로 재발한다. 재현·형제 항목(package 변수 clobber·block-local 잔여 2형) 상세 = §2.
>
> 그 외 재개할 deep-defer 항목 없음(round-18 리포트 8-가족 + C1 part1/2 全 RESOLVED). 남은 것은 소형 follow-on(아래) 또는 §1 우선순위 큐(loud→supported/OBS) 재개.
>
> 소형 follow-on(correct-or-loud loud 유지): void-cast of output-formal fn(`void'(getnext())`)·frame-formal array를 nested hier로 forward(OUTPUT/INOUT)·param/call leaf size-cast(`8'(P*a)`, §4.5.212 잔여)·**fork-in-frame 잔여(§4.5.214, 전부 Minor/safe)**: `fork_arms_self_contained`의 resolve-time 재-walk 중복 제거·공유 `enter_task_frame` arm에 load-bearing comment 보강·forking task를 호출하는 fork arm의 elaborate-time reject(현재는 F4004 tie-cap runtime guard로 안전하나 clean E3009가 더 명확)·same-instant zero-delay sibling visibility가 differential-미검증(iverilog 자체가 이 케이스서 스케줄링 특이).

## 1. 착수 우선순위

1. **오라클 있는 CRITICAL silent-wrong** (§2에서 선정) — 항상 최우선.
2. **오라클 있는 loud→supported** (§3 · additive=저위험).
3. **전제조건 충족된 honest-loud 승격** (§4~§5).
4. **G2 OBS 슬라이스** (§6).

현재 NEXT 큐(상세=LOOPROMPT): **§0 correct-support 승격 큐 T1(string-array 가족 7종) → T2(real const-fold·generate string decl-init·sized-literal enum label·음수 range)** → inner-net vs outer-param shadow(§0-B·①-급 silent-wrong·AST-gathered name set 선행) → OBS-2 sva.jsonl.

## 2. Silent-wrong 잔여 (전부 pre-existing·baseline 동일 — deep defer 또는 기록됨)

- **🔴 §4.5.221이 도입한 좁은 하강(pre-existing 아님 — 내 책임)**: 계층 real param 이 상수 범위 바운드에 오면 조용히 1-bit. `logic [$clog2(u.R)-1:0]`(u 는 real param R 을 가진 인스턴스) → PRE 는 loud(`E3009`) 였으나 POST 는 **진단 없이 width 1**. 원인 = `count_reads_real_param` 의 `Ident` arm 이 `segments.len()==1` 을 요구해 `u.R` 을 못 봄. **iverilog 도 거부**하므로 무오라클이고 범위는 좁으나 loud→silent 는 하강. **fix 전제** = 바운드 문맥에서의 계층 이름 해석(현재 `nonconst_bound_reason` 은 false-loud 회피 때문에 call/hier 로 안 내려감) — 이름 매칭 근사는 동명 정수 hier param 을 false-reject 할 수 있어 실측 hazard set 없이는 금지(§4.5.218 선례).
- **파라미터 구조적 지연이 조용히 무시됨(pre-existing·PRE==POST)**: `assign #P y = x;` · `wire #P y = x;` · `and #P g(o,a,b);` 가 P 가 **정수 param 이어도** 지연을 무시(리터럴 `#3` 은 동작). §4.5.221 이 `parameter real` 을 지원하면서 **클럭 주기/지연 관용구가 이 경로의 주 사용처**가 되어 도달성이 크게 넓어졌다 — 우선순위 상향 후보.
- **real→`input int` formal 미강제(pre-existing·PRE==POST)**: `f(2.4)` → vita `24`, iverilog `20`.

> 발굴 경위·재현·범위 상세는 ARCHIVE의 해당 §4.5.x 참조.

**✅ RESOLVED (silent-wrong → loud):**

- **~~block-local `string s[] = '{…}` dyn-array init dropped~~ silent-wrong → RESOLVED §4.5.183(supported)**: §4.5.182(queue/dyn `{…}` concat) 적대 검증 중 발굴. `string s[] = '{"a","b","c"}`를 initial/always BLOCK 내부에 두면 vita SZ=0(빈 배열·빈 문자열) vs iverilog SZ=3(a b c). **module-scope 선언은 정상**(SZ=3)·block-local만 silent-empty. **근본 원인**: block-local decl-init 수집기(`hoist_block_local_nets` 내부)의 `scalar_string` 판정이 base range/packed dims만 보고 per-name unpacked dim은 무시→`string s[]`(dimensioned)도 `scalar_string=true`→push gate `name.unpacked.is_empty()`서 dimensioned string은 push=false→`'{…}` init이 t0 pre-sweep에 수집 안 됨→drop. module-scope 수집기(`collect_var_init_drivers`)는 이미 `is_dyn_str_init` 특례(unpacked.len==1 && Dim::Dyn && AssignPattern)로 push→정상. **두 scope 수집기 비대칭**이 silent-wrong 원천. **fix(supported, not loud)**: module-scope가 이미 동작하므로 block-local도 parity로—block-local push gate에 동일 조건 미러(`name.unpacked==[Dim::Dyn]` && `AssignPattern`)→기존 `pending_block_local_string_inits` 라우팅 타고 flush(`new[N]`+element writes)로 확장. fixed(`s[2]`)/multi-dim/non-`'{…}` string은 조건 미매칭→기존 loud 유지·`{…}` concat string은 §4.5.182가 loud 유지(silent-empty 방지). 적대 differential 全 MATCH(basic·named·single·new[]-copy·always·module/scalar/int-dyn 회귀). `block_local_string_dyn_init.rs`×9. **SUPPORTED follow-up(loud→supported·별개 machinery)**: FIXED string array init(`string s[2] = '{"a","b"}`)은 module·block **양쪽 모두** loud(E3009 "string-array initializer is unsupported")—iverilog는 지원(`a b`). dyn string(§4.5.183)과 달리 비대칭 없음(uniformly loud=correct-or-loud)·`string_array_elems`(element-net) 경로라 별개 슬라이스. 상세=ARCHIVE §4.5.183.
- **~~same-named STATIC block-locals in DISJOINT procedural blocks~~ silent-wrong → RESOLVED §4.5.180(loud)**: fresh-area iverilog 차분 probe로 발굴. v1은 절차 블록-로컬을 module net으로 flatten하고, 같은 이름이 다른 블록에서 또 선언되면 "시간상 재사용"으로 보고 한 net에 coalesce했다. 그러나 이는 iverilog의 distinct-per-scope 변수와 byte-identical한 조건이 **둘째 블록의 TYPE이 같고 read 전에 assign될 때뿐**이다: (1) 타입[sign/width]이 다르면 공유 net을 잘못된 부호/폭으로 읽고 씀(`begin logic signed [7:0] y; …end begin logic [7:0] y; y=x; $display("%0d",y);end`=vita -3 vs iverilog 253·`>>>`가 산술 vs 논리·`%h` 폭 오염); (2) read-before-write는 첫째 블록의 leftover를 봄(vita 5 vs iverilog x). sibling·named block·cross-process 全 발생. **fix**: `hoist_block_local_nets`의 collision(`existing`) 경로 static-scalar 분기에 **type-mismatch + `automatic_local_definitely_assigned` guard**[E3009]. SAFE same-type+assigned-first coalesce(흔한 `for`/`tmp` 재사용)는 무영향; module-scope var를 SHADOW하는 경우(struct/enum shadow-scoping·`check_block_local_scope_leaks`가 이미 loud)는 `local_decl_names.contains(nm)` 제외로 무영향. `block_local_name_collision.rs`×9. **SUPPORTED follow-up**: 충돌하는 static 블록-로컬에 `$blk$<span>` scope를 줘 distinct net화(automatic·struct가 이미 하는 것)→loud 대신 iverilog-parity로 supported화. 상세=ARCHIVE §4.5.180.
- **~~writing a dynamic-array `input` formal inside a synchronous frame body~~ silent-wrong → RESOLVED §4.5.178(loud)**: §4.5.177이 framed function의 `input` dyn-array formal을 heap snapshot으로 READ 지원했으나, body가 그 formal을 WRITE(`b[0]=9`·`foreach(b[i]) b[i]=…`)하면 write가 **조용히 유실**됐다(`&self` frame executor의 heap store `write_lvalue`→`dyn_write`는 `&mut`→element write가 unused scalar frame slot로 떨어지고 read는 heap서 옴). `function f(input int b[]); b[0]=9; return b[0];` = vita 1 vs IEEE 9(§13.5.1 pass-by-value copy write). clean HEAD의 direct-rhs `r=f(a)`도 동일→**§4.5.177 pre-existing**(§4.5.179 buried-call 적대 검증서 발굴). **fix**: `frame_write_lvalue` 상단 runtime guard가 heap dyn-array write(`dyn_is_handle && kind!=String`) 도달 즉시 F4004—실제 write 시도 지점서 포착해 element/whole/`new[]` 균일 커버(elaborate 열거의 unsound miss 회피)·string slab-store 제외·read-only body 무영향·§4.5.171 V5(suspendable `&mut`)는 이 경로 안 탐(회귀 0). 신규 `frame_func_dyn_formal_write_loud.rs`×4. iverilog는 by-ref write(`a0=9`)로 IEEE 위반이라 vita는 loud(no-oracle 아님). 상세=ARCHIVE §4.5.178.
- **~~`foreach` over dynamic array/queue/assoc inside a FUNCTION or SUBSET-task body~~ silent-wrong → RESOLVED §4.5.175(loud) → §4.5.176(supported)**: 파서가 `foreach(a[i])`를 `__st=a.first/next(__i)`로 desugar하는데, 이 walk step이 iteration KEY를 side-effect로 WRITE한다. **동기 frame executor**(`run_frame_call`=함수·`run_task`=subset task)는 `&self`라 key를 못 advance→plain eval이 key를 stuck시켜 walk 미진행→**조용히 0 반환**(적대 de-risk probe서 `function int g(); foreach(marr[i]) s+=marr[i]` = vita 0 vs iverilog 15로 발굴). **§4.5.175(loud)**: runtime fatal F4004. **§4.5.176(supported·최종)**: iteration key는 항상 FRAME-LOCAL(`__foreach_i` body-local)이라 `&self` executor가 interior-mutable frame window로 advance 가능→`assoc_iter_step`의 read/compute half를 `SimState::assoc_iter_compute`(`&self`)로 추출(process path는 이걸 call하도록 리팩터→byte-identical)·`frame_assoc_iter`가 key를 `frame_write_lvalue`로·status를 `__st`로 write. 적대 differential 全 검증(dyn/queue/assoc·int/string key·first/next/last/prev·signed·nested·iverilog-oracle 케이스 MATCH·assoc-in-function은 iverilog compile-fail이라 hand-IEEE). **잔여 loud**: key가 non-local인 direct `st=aa.first(module_net)`(module-net write=`&mut` 필요→fatal). 신규 `frame_foreach_dynamic.rs`×10.

**🔴 DEEP-defer (전용 인프라 필요):**

- `%c`/`%s` high-byte(128-255) UTF-8 remangle — output-pipeline 전체 byte-clean 필요(diag `RtlText`→`Vec<u8>`·~8 test sink·CLI·OBS). §4.5.119/128 발굴.
- derived-localparam self-width (`localparam P=A+B`·`$bits(Q)`=32 등 expression-init 폭) — `param_meta` pollution+allow-list 인프라. §4.5.124 blocker.
- top-level(`$unit`) typedef ② — flat map이 §26.3 scope-precedence 미모델(wildcard-import가 same-name `$unit` typedef 미shadow). 2회 revert. §4.5.104.
- enclosing-const-over-inner-block-local resolution(§6.21 역행) · static-coalesce-onto-automatic. §4.5.108 발굴.
- packed-WIDTH sibling-block 멤버 coalesce(SW2) — read-gate 불가(SOMETIMES-correct)·per-block-scope width 필요. §4.5.98 재분류.

**중형 (오라클 확보 시 착수 후보):**

- **함수/태스크 body-내부 read → caller comb 감도 미기여**(§4.5.166 발굴·pre-existing·DEEP): `always_comb y=f();`서 `function f`가 모듈 net `a`를 **인자 없이 내부**로 read하면(`f=a^8'hFF`) 그 read가 caller 감도에 안 들어감→`a` 변경에 무감(vita `y=xx` vs iverilog `ef`/`df`·§9.2.2.2.1은 함수호출 접근 변수 포함). `collect_expr_reads`의 `Call` arm은 **인자 read만** 수집(callee body 미하강). fix=callee body의 transitive net read-set(재귀 cycle-guard·formal/local vs 모듈 net scope 분리·callee-내 hier/index read)—전용 슬라이스. task-call(`always_comb tsk(y)`)은 iverilog도 미재발화(no-clean-oracle·murky). ⚠️§4.5.166이 comb 자기-body의 LHS-index/계층 ref는 해소했으나 callee-body는 별개 클래스.
- dual-wildcard import type ambiguity(둘 다 silent·loud화 후보). §4.5.104.
- mixed-sign enum 산술 · `enum bit` 2-state-base X-leak(일반 enum-base-kind 갭). §4.5.109/110.
- `$signed`-in-wider-sum sign-loss · size-cast fn-call operand sign-loss `4'(f(15))`. §4.5.111/112.
- packed bit/part-select replication count · hierarchical `{s.CNT[0]{v}}` count silent-0. §4.5.109/121 잔여.
- inline-local/generate/task-local param sub-select(`param_range`=module-scope only) · net-vs-param VALUE precedence · gen-scope param value truncation. §4.5.118 잔여.
- 2D-packed iface member element/outer part-select · scalar iface member over-acceptance. §4.5.116 잔여.
- non-uniform `$dist_*` libm draw 발산 · chi_square/t seed LCG. §4.5.126.
- string-array-elem 전원 concat `{s[0],"-",s[1]}` truncate — native-eval static string-width. §4.5.134 발굴.
- `$monitor`에 직접 `$random`/`$urandom` 인자=매 스텝 spurious re-fire(no-oracle: iverilog는 non-simple `$monitor` 인자 자체 거부 "SORRY"·시간함수 3종만 예외). 값 렌더는 정상. §4.5.135 발굴.
- leading-NUL frame string · frame-body 内 SYS-READ(assignment form도). §C/§4.5.122/124. (runtime `$clog2(real)` f64 misread = §4.5.143서 해결)
- format 출력 잔여(§4.5.144 후·전부 pre-existing): real-const `%s`=vita packed f64 bytes vs iverilog warn+`<%s>` · string-LITERAL embedded-NUL(const_string NUL-리터럴+lexer octal-escape `\000`) · render_template malformed spec(missing-arg·`%`+non-spec char)=vita `x`/literal vs iverilog `<…>`+warn(literal+var 공통) · `%d`/`%0d`-of-non-finite real(inf/nan)=vita `i64::MAX` vs `inf`/`nan`. (숫자-const `%s` NUL→space·`%d`-of-real width=§4.5.144/142 해결)
- inline 함수 잔여 4종: global-reader widening 미수혜 · size-cast `16'(a*b)` context · signed `>>>` unsigned-context · inline-call return 미truncate. §4.5.80 잔여.
- hier `@(*)` sensitivity · hier md-packed-nested part-select. §4.5.115/103 잔여.
- ~~narrow-typed param init 32-bit width(§4.5.146)~~ **RESOLVED**(§4.5.159 재현 probe: `localparam logic [3:0] B=(2==2)`=`%b`→`0001` 4-bit MATCH·후속 param_meta 작업이 해소·non-reproducing 판정).
- param scalar bit/part-select const-fold **미지원**(silent-wrong·DEEP): `logic [P[5:0]-1:0] x`=range-bound 폭 1 vs iverilog 63(param 값 컨텍스트는 E3009 honest-loud). §4.5.148 naive fold `(v>>i)&mask` 시도→**적대 2렌즈 수렴 발굴**: `[N:0]` descending만 정답·**zero-LSB ascending `[0:N]`**(선언 범위 미정규화→wrong bit)+non-zero-LSB below-LSB index=loud→silent 회귀→revert. 근원=`param_range`가 non-zero-LSB만 추적("absent=descending zero-LSB" 불변식·zero-LSB ascending 미탐·`base_net_ascending` false). fix=全 param 범위(lo/msb/direction) 기록 or `[lo..hi]` membership(param_range 불변식 확장=broad).

- enum label **unfoldable-range signed** 잔여(§4.5.158 minor): `enum logic signed [X-1:0]`처럼 base range가 const-fold 안 되면 `enum_base_width`=None→`base_w` None→라벨 sign이 value-inferred(positive 라벨 unsigned)·§4.5.158은 fold되는 base·base-less만 커버. 극한 엣지(unfoldable enum base). fix=`param_range` 불변식 확장 or value-inferred에 enum sign 전파.
- **~~🔴 size-cast `N'(expr)` context width 미전파 (DEEP·broad)~~ → RESOLVED §4.5.212**: `8'(a*b)`=13 vs 45·`6'(a<<1)`=14 vs 30 등 — size cast가 inner 산술을 operand self-width로 계산 후 resize(carry 소실). "DEEP(새 IR 노드/26-caller 회귀)"로 defer됐으나, **size-cast 전용 재귀 헬퍼로 격리**하면 공유 ctx 머신 미변경. `lower_size_ctx`가 context-det 연산자 구조(arith/shift/unary±~/ternary)를 재귀하며 각 self-det leaf를 N으로 widening(shift amount/cmp는 self-det 유지)·extension sign=operand 전체 sign(`ast_ctx_signed`·signed iff 全 leaf signed·§11.8.1 mixed→unsigned=zero-ext all, iverilog 실측). sign 판정 불가(param/call leaf)면 기존 fill-only 경로 유지(회귀 0). iverilog **오라클** 있어 exhaustive 차분 검증(unsigned 全 op·signed mul/div/ashr·mixed-sign·ternary·nested-cast·power·랜덤 sweep). format 23 불변·+23 tests·상세 ARCHIVE §4.5.212. **잔여 residual**: param/call leaf(`8'(P*a)`)는 fallback→여전히 self-width(follow-on).
- **`$bits(md-array ROW)` partial-index sub-array** = next-pow2 오류(`byte m[2][3]; $bits(m[0])`=vita 32 vs iverilog 24·`int`=128 vs 96) — logic 배열도 동일(atom-independent)·element `m[i][j]`는 정상(§4.5.155). runtime 경로만(const-context는 §4.5.155가 부수 교정). from_prescan partial-index 미처리→from_table next-pow2. pre-existing·§4.5.155 발굴.
- **class value-param 타입 폐기(no-oracle·§4.5.159 발굴)**: `parse_class_param_list`이 `ClassParam{name,default}`만 보유→선언 타입(`#(parameter byte N=300)`의 byte/[3:0])을 파싱만 하고 폐기→narrow 타입 truncation 미적용(`N`=300 vs 44 예상). **oracle 없음**(iverilog가 typed class value-param 미지원=mutual loud on `[3:0]` form). 헤더 param(§4.5.159) 상속과 별개 경로·pre-existing. fix=`ClassParam`에 type 필드 추가(AST·schema re-pin)+elaborate coercion(전용 슬라이스·no-oracle→hand-IEEE).
- **untyped param 값-결정 타입 잔여(§4.5.161 LITERAL·§4.5.162 IDENT/EXPRESSION·§4.5.163 PKG-scoped 해소·이하 residual)**: (a) **`time` param value-inferred**: `localparam time C=<expr>`가 value-inferred(negative value→32-bit signed·`$bits(time-alias)`=32 vs net 64)—time은 value-determined에서 의도적 제외라 declared 64-unsigned 미적용. fix=time param에 (64,unsigned) meta 배선. (b) **expr self-determined WIDTH**(`8'hFF+8'hFF` 등 sub-expr truncation·§2 size-cast DEEP 동근·sign만 정확·width=`min_signed_bits(folded)` 근사). (c) **interface-member** positive param(`i.A<i.B`)=unsigned(module-hier/pkg 정상). oracle=iverilog ✓. 전부 pre-existing·미악화 확인.

- **~~scope-shadow blindness (string-array side-map)~~ → RESOLVED §4.5.218**: inner-scope 로컬이 outer `string_array_elems` 엔트리를 shadow하도록 **opt-in** `walk_scopes_key_shadowed` 도입(function/task/generate scope·read/write 양 경로·非-string 로컬 포함·generate-local의 bogus range-LOUD도 해소). block-local `string` 충돌은 loud. 상세=ARCHIVE §4.5.218.
- **🔴 inner NET이 outer PARAM/enum-label을 shadow 못 함(§4.5.218 잔여·오라클 有·DEEP)**: 같은 cross-map 근인이지만 `params`/`param_meta` consumer는 **순서 의존** 때문에 고칠 수 없었다 — shadow 검사가 `symbols`(elaboration 중 채워짐)에 의존하는데 `elaborate_gen_item`이 generate control expr를 phase마다 재-fold하고 fold 실패는 Nets phase에서만 진단→중첩 generate body가 **통째로 조용히 삭제**(exit 0). 재현: 모듈 `localparam W=4` + function-local `int W; W=9; return W;`=vita **4** vs iverilog 9(task-local·generate-local·enum-label·`str_param_raw` 동형). fix=order-INDEPENDENT **AST-gathered per-scope name set**(`gather_local_decl_names`/`compute_scoped_block_locals`의 "pure function of the AST" 패턴)로 shadow 판정 → 전용 슬라이스.
- **block-local이 imported package 변수를 clobber(§4.5.218 재감사 발굴·오라클 有)**: `package pk; int pvar=33;` + `import pk::*` + `initial begin int pvar; pvar=7; end` → `pk::pvar`이 vita **7** vs iverilog 33(IEEE §26.3: 로컬이 import를 shadow·패키지 변수 불변). alias net이 `symbols`에 있어 `existing=Some`이지만 `int` 패키지 변수는 dyn/string 3-clause guard를 전부 통과해 coalesce. function-local은 안전(`$func$` scoping).
- **block-local 잔여 shadow 2형(§4.5.218·PRE==POST)**: block-local **scalar vector**의 이름을 block이 index-select(`logic [7:0] sa; sa[0]`)=vita 0 vs iverilog 1 · named-block array 1형. **이름 충돌만으로 gate하면 byte-correct 설계 11건이 false-reject**되므로(§4.5.218 실측) per-shape hazard 모델이 필요.
- **scalar-string 같은-family 잔여(§4.5.217 differential 발굴)**: `expr_is_string_ast`에 **`Ternary` arm 부재**→`{(c?a:b),"!"}`가 바이트 drop(scalar·element 공통·**no-oracle**[iverilog assert]·hand-IEEE) · `{a, 8'h00}`/X-Z 바이트가 `0x20`으로 치환되고 길이에 계상. **정정**: 같이 기록됐던 "`always @(*)` string concat 조용히 drop"은 **오진**—vita가 `[abcd]`로 정확하고 iverilog가 빈 문자열이며, 명시 `@(a,b)`는 vita가 정직하게 loud.

**문서화된 divergence (수정 비대상·핀됨):**

- 크로스스코프 t0 decl-init race(양쪽 §6.8 합법·self-consistent) · 런타임 구성 `-0.0` 표시 · iverilog 자인 결함들(expression-force "evaluated once" 등).

## 3. Loud→supported 후보 (현재 전부 loud=안전 · additive)

**~~frame-body validator over-scan~~ RESOLVED §4.5.172** (pre-existing false-REJECT · §4.5.171 적대 agent 발굴 · V5 무관): `classify_frame_body`의 linear `block_base..func_blocks.len()` 스캔이 **POST-PASS**(`resolve_frame_task_rejects`·전 func lower 후 subset task 검증)서 `func_blocks.len()`=**전체** 끝이라 **뒤에 정의된 func 블록까지 over-scan**→그들의 (합법적으로) out-of-frame인 output-formal write를 자기 것으로 오판→subset task를 E3009("assignment to a net outside the function")로 false-reject(iverilog는 accept). **fix = reachable-block CFG walk**: entry(`self.funcs[fid].entry`)서 자기 CFG 엣지(`Goto`/`Branch`/`Call`→`ret_bb`[callee entry 아님]/`Delay`·`Wait`→`resume`)만 순회→다른 func·dead 블록 미방문. **correct-or-loud**: 미방문 블록은 타 func이거나 dead(실행 안 됨)이므로 verdict는 false-reject만 DROP(silent-wrong 불가). `frame_task_pending` 튜플서 dead `base` 필드 제거. 적대 differential 全 MATCH(nested call·if/case/for·suspendable-mixed·5-task interleave·still-loud=모듈 net write). 신규 `frame_subset_overscan.rs`×4. 상세=ARCHIVE §4.5.172.

**string/heap:**

- ~~frame string LOCAL 대입(E3018→heap slot)~~ **RESOLVED §4.5.167(round-14 V1)**: function/task/block/class-method-local `string`이 heap `NetKind::String` frame slot(`frame_local_net_kind`·4 사이트)·procedural 대입/return/`s[i]`/method(`.len`/`.substr`/`.getc`) 지원(엔진 str_bytes frame-aware). **잔여 loud**: static(non-`automatic`) task inline string local(`hoist_inline_task_locals`:18119)=Wire→E3018(inline 경로=frame-slot 아님·순진 String化시 str_bytes twin 미적용→silent 회귀 위험·별개 inline-string-storage 슬라이스). substr-actual `s[i]` · string part-select `s[i:j]`.
- dyn string 요소 method(`s[i].len()`) · whole-element read as value(`x=arr[i]`) · record array-of-record.
- queue/assoc의 string·real 요소 · string queue · block-local queue decl.

**함수/패키지/formal:**

- ~~control-flow pkg fn~~ · ~~pkg `function string`~~ = **둘 다 이미 동작**(2026-07-25 재그라운딩: `9` / `hi`). 잔여 = pkg TASK statement call뿐. §4.5.111.
- array formal 재전달(nested/recursion) · non-zero-LSB 원소 · 2-D/non-zero-base/signed/task array formal. §4.5.110 slice 밖.
- 음수-LSB 멤버 sub-select 정식 지원(§4.5.114). md-packed nested part-select WRITE `x[j][m:l]`/`arr[i][j][m:l]`=**§4.5.145 지원**(descending zero-lsb leaf·const packed idx); fail-closed 잔여(전부 loud·honest)=ascending/non-zero-lsb leaf·**genvar-index**(`x[g][m:l]`=const-fold 안 됨·over-reject)·const-OOB packed idx=silent no-op(read path 공유·값 무손상).
- method/ctor NAME-default class-scope 해석(§4.5.90) · G4 string-return frame call(§4.5.129).
- **round-14 리포트 잔여 (전부 loud·오라클=iverilog ✓·§4.5.167서 스코프)**:
  - ~~**V3/V4 (task body 内 timing/suspend·최우선)**~~ **RESOLVED §4.5.168**: scheduler activity **call-stack** + **window stash/restore**로 leaf task의 `@`/`#`/wait/NBA/$systask/재귀/nested 동작(KAT driver `drive(v);@(posedge);sig<=v`=22 등 iverilog MATCH). recompute(no sidecar)→format 22 불변. 적대 2-lens: core SOUND 실증 + 5 over-lift correct-or-loud화. **잔여 loud(§2 defer 후보·전부 correct-or-loud)**: ~~frame-local unpacked array~~ **RESOLVED §4.5.169**(array-FORMAL md-packed 표현 재사용·`reserve_frame_local_decl` 4-site 통일·엔진 frame_local=net-range라 자동 격리·적대 differential common 全 MATCH·automatic-array 격리/subroutine port는 vita>iverilog) · `wait(<frame-local>)`(level-wait re-eval window 미복원)·`repeat(<non-const>) @`(hidden counter SHARED net·동시활성화 오염)·NBA-to-frame-local(illegal)·`fork`/`disable fork`/`wait fork`-in-task(fork-family 미지원). 각 근본해결=per-activation repeat counter·in-frame fork machinery(별개 슬라이스). **§4.5.169 잔여 loud(safe gap)**: frame-local array의 multi-dim·non-zero-based·non-simple-element·whole-copy(`b=a`)·`foreach`·NBA-elem·`'{…}` init.
  - ~~**V2A (task input dyn-array formal)**~~ **RESOLVED §4.5.170** (static task 한정·pass-by-value): `task consume(input byte b[])`가 R11 함수 dyn-array 머신을 static(inline) task 경로로 확장해 동작(`.size()`/`b[i]`/re-forward). **pass-by-value SNAPSHOT**(entry deep-copy)—적대 soundness agent가 초판 direct-alias의 IEEE §13.5.1 위반(body/callee가 배열 변형시 누출) silent-wrong 발굴→`alloc_dyn_snapshot`+`handle_copy_stmts`로 수정. 공통 fix로 `dyn_array_actual_net`이 `dyn_subst` 우선 consult→re-forward가 함수·태스크 양쪽 동작. **잔여 loud(V5 의존)**: **automatic/recursive task**의 dyn-array formal은 frame formal이 scalar slot이라 handle-in-slot(V5) 필요→loud-defer. write-to-formal/sign-mismatch/non-bare/queue actual=correct-or-loud.
  - ~~**V5 (frame dyn-array local `new[]`)**~~ **RESOLVED §4.5.171** (TASK 한정·per-net heap + lifecycle): `task automatic; int loc[]; loc=new[n]; loc[i]; loc.size()`가 frame-local dyn-array를 `NetKind::DynArray` net으로 예약해 동작. **30-site dyn_heap 재키잉 회피**: `dyn_heap[net]`=현재 활성화 배열·`enter_task_frame` reentry guard(recursion/concurrent→**fatal F4004**)·`exit_task_frame` free-at-exit. **잔여 loud(follow-on)**: (a) **FUNCTION** dyn-array local(`&self` 동기 `run_frame_call`/`run_task`가 `new[]`=`&mut` heap 실행 불가·구조적) (b) **recursion/concurrent**(per-activation heap stash 필요=진짜 격리) (c) multi-dim/packed/non-bit-vector element.
  - ~~**V2A-frame (automatic task dyn-array input FORMAL)**~~ **RESOLVED §4.5.173**: `task automatic consume(input byte b[]); b.size(); b[i];`가 formal을 per-activation `NetKind::DynArray` heap slot으로 예약(`reserve_frame_task`)하고 caller 배열을 **frame ENTRY서 deep-copy**(pass-by-VALUE·IEEE §13.5.1)해 동작. 엔진 스냅샷=`enter_task_frame` 直後 `frame_dyn_snapshot_formals`(exec.rs 2 site·`split_frame_in_binds`가 dyn formal을 scalar copy-in서 분리·in-bind는 caller dyn-array net을 읽는 bare `Signal`). V5 net-range lifecycle이 formal 자동 커버(free-at-exit·reentry guard). **적대 differential 全 MATCH**: basic/signed/4-state X/twice-isolated/**snapshot-immune-across-suspend**(동시 resize+mutate 누출 0)/re-forward/mixed/empty. **LOUD(safe)**: recursion·concurrent fork(F4004·per-activation stash follow-on)·**non-suspendable subset task**(동기 `run_task_call`=`&self`가 heap 못 채움→E3009·function 쓰라 안내)·sign-mismatch/queue actual(E3009). format 22 불변(wire 무변화·TaskCallInfo 필드 추가 회피·기존 designs byte-identical). 신규 `frame_dyn_formal.rs`×13.
  - **잔여 loud(follow-on)**: (a) FUNCTION dyn-array local (b) recursion/concurrent(per-activation heap stash) (c) multi-dim/packed/non-bit-vector element.

**~~inline(static-task) 경로 `foreach`-on-dyn-formal~~ RESOLVED §4.5.174** (§4.5.170 gap·§4.5.173 발굴): static(non-`automatic`) task/function의 `input b[]` formal에 `foreach(b[i])`가 E3009("first/next/last/prev are only supported as the DIRECT rhs...")였음. **원인**: 파서가 `foreach(b[i])`를 `__st=b.first/next(__i)`로 uniform desugar→elaborator가 array KIND로 재작성하는데 dyn/queue dense-walk dispatch가 `dyn_handle`(=`lookup_net_scoped`)로 resolve→`dyn_subst` formal alias(read-only input dyn-array formal은 real net 아님) 못 봄→`b.first(__i)`가 generic method 경로로 falling through→E3009. **fix**: `foreach`는 배열 READ이므로 dispatch(dispatch gate + resolution 2 site)를 `dyn_handle_read`(alias 우선 consult)로 교체→formal이 module dyn-array와 **동일한 dense walk**로 라우팅. `dyn_subst`는 inline dyn-formal body 밖에서 empty→**모든 다른 array(fixed/module dyn/queue/assoc) byte-identical**. 적대 differential 全 MATCH(index+element·signed·two-foreach·module dyn/fixed/queue regression). **잔여 loud(별개 follow-on)**: FUNCTION+`foreach`(control flow라 R2-inline 불가→framed→framed function dyn formal 미지원)는 여전히 loud=function-frame dyn-formal 슬라이스 소관.
- **round-14/16/17 oracle-reject (hand-IEEE)**: ~~**V6**(unpacked-array/queue of unpacked struct)~~ **RESOLVED §4.5.191(FIXED array) + §4.5.195(QUEUE·round-17 A)**: §4.5.191이 FIXED array를 열었고 round-17 리뷰어가 실측한 `pkt_t q[$]`(QUEUE)는 여전히 loud였음(ROADMAP "queue-of-struct 이미 동작"은 PACKED struct queue 한정·UNPACKED은 미동작이었던 triage 불일치). §4.5.195=파서 `Dim::Queue(None)`→§4.5.191 packed-vector lowering 재사용 + packable struct 변수·tf-port를 unified whole-vector化(`q.push_back(p)`·`r=q[i]` end-to-end). · ~~**V8**(task-local PACKABLE unpacked struct decl)~~ **RESOLVED §4.5.192**(tf-body 인식+packed-vector local) · ~~**non-packable(string 멤버) struct tf-local**(round-17 B)~~ **RESOLVED §4.5.195**(파서-only·tf-body 최상위 decl 루프가 block-local 청사진 미러·per-member 저장 재사용·`frame_local_unpacked_struct.rs`). ~~**V2B**(function `output`/`inout` dyn-array formal — iverilog "must be input ports")~~ **RESOLVED §4.5.194 Phase 2b**(dyn_heap을 `RefCell`로 interior-mutable화→`&self` executor가 exit서 formal→caller deep-copy[`frame_dyn_copy_out`]·TASK 케이스=iverilog 오라클·FUNCTION output port=hand-IEEE). task OUTPUT **FIXED**-array formal은 §4.5.193서 supported(md-packed slot→packed temp→unpack).
- **round-16 리포트 잔여 ~~= 전부 executor-bound(하나의 아키텍처 blocker)~~ RESOLVED §4.5.194**(`dyn_heap: Vec<Option<DynObj>>`→`RefCell<Vec<..>>` interior-mutable화[`class_heap` 선례]→`&self` frame executors[`run_frame_call`/`run_task`]가 heap-op[`new[]`·dyn write] 수행·design doc=`docs/superpowers/plans/2026-07-21-dyn-heap-refcell-executor.md`): ~~① V2A-dyn on NON-suspendable task~~ **Phase 1a**(exec.rs subset 분기 + run_task nested서 dyn formal snapshot) · ~~② V5 function-body dyn-array LOCAL + `new[]`~~ **Phase 2a**(`alloc_dyn_array` 공유 코어·SysTask(DynNew/DynDelete) arm·frame-dyn lifecycle `first_slot=np`로 formal 제외) · ~~③ V2B function output/inout dyn-array formal~~ **Phase 2b**(`frame_dyn_copy_out`·4 apply site·`build_task_frame_set` framing) · ~~④ framed-function nested/recursive dyn-formal call~~ **동작**(nested 검증 R=60; recursion은 per-activation heap follow-on=loud). Phase 1b(write-path: `&self` executor dyn element write)가 공통 기반. **잔여 loud(follow-on·correct-or-loud)**: recursion with dyn local/formal(per-activation heap stash) · string/real/class-handle element dyn formal(input도 동일 제외) · unwritten output formal=IEEE §13.5.2 empty copy-out(iverilog는 by-ref로 caller 유지=非준수). 상세=ARCHIVE §4.5.194.

**~~frame-body dyn-formal FUNCTION call~~ RESOLVED §4.5.195 (round-17 C)**: `s=sum(b)` in a TASK body(리포트 케이스)가 E3009였음. §4.5.194 RefCell dyn_heap이 `&self` executor의 snapshot-marker heap-copy를 가능케 함→elaborate gate `in_frame_body` 제거·`classify_frame_body`가 전용 `dyn_formal_marker_stmts` set의 marker만 허용(§7.10 whole-copy는 frame body서 loud 유지)·`run_frame_call`/`run_task`에 marker arm(`frame_dyn_copy_out`). TASK-body direct/buried·function-local-arg·module-process 全 iverilog MATCH·recursion=F4004 loud. **잔여 loud(correct-or-loud follow-on)**: FUNCTION이 자기 dyn-formal을 다른 함수로 **재전달**(`fsum(c)` where c=이 함수의 formal)—framed function formal이 heap-resident라 `dyn_array_actual_net`이 re-forward source로 못 resolve(기존 "array formal 재전달" 갭). `frame_body_dyn_formal_call.rs`×7.

**round-17 D-가족 (리포트 §5 minor 잔여 — §4.5.196서 tractable 4항목 RESOLVED):**

- ~~**계층 함수호출 `u1.f(x)`** (FUNCTION)~~ **RESOLVED §4.5.196**(hier-net defer 미러: `hier_funcs` FQ `<inst>.<fname>`→per-instance fid[`reserve_frame_func`서 hier-callable만]·`DeferredHierCall`·`resolve_deferred_hier_call`[`hier_resolve` §23.6 walk 재사용·arity/POISON guard]·per-instance param/net baked·M-subset[input-only scalar·non-string]).
- ~~**계층 태스크호출 `u1.tk(x)`** (TASK·L)~~ **RESOLVED §4.5.197**(오너 지시="인프라를 지어서 hier-task까지"): "framed-task-write-to-instance-net 인프라"의 진짜 정체=**`compute_suspendable_tasks`가 frame-aware가 아니었던 것**. 근본 원인 진단—automatic task가 module net을 blocking write하면 `stmt_signal`이 blocking assign을 non-signal로 봐 NON-suspendable(subset)로 분류→`&self` subset executor가 module net write 불가라 loud("net outside function"). **인프라(Option A·format 22 불변)**: `compute_suspendable_tasks`에 `base_nets: &[u32]`(각 func frame base·engine `func_table`=elaborate `func_metas` verbatim thread라 양측 동일) 배선→blocking assign이 자기 frame window `[base, base+locals_len)` **밖** net을 `word.is_none()`로 write하면 suspend-signal로 취급→module-writer task가 suspendable path(`&mut`)로 lift·이게 P1(non-hier automatic module-write)도 해소. **hier-CALL defer(§4.5.196 미러)**: `hier_tasks` FQ→per-instance fid[`reserve_frame_task`서 input-only scalar 등록]·`DeferredHierTaskCall`(process key `(proc, call_block)`)·`resolve_deferred_hier_task_call`[`hier_resolve` walk 재사용·arity guard·`TaskCallInfo{callee,in_binds,out_binds:[]}` into `task_calls_proc`·`Terminator::Call.target` patch]. 엔진 process Call arm이 target 무시하고 `task_calls_proc[key].callee` 사용·`enter_task_frame`이 in-value를 per-instance formal width로 resize→defer 시 arg를 self-width로 lower. 적대 differential 全 MATCH[multi-instance 격리·deep path·signed/wide multi-arg·`#5` suspend·fib RMW]. **잔여 loud(correct-or-loud)**: output/inout/array/string formal[cross-boundary copy-out·별개]·STATIC(non-automatic) task hier-call[non-framed→force-frame이 local caller 회귀 위험=large·**정공법=frame⊂inline 동등화**·§4.5.198 step 1 착수]·~~module ARRAY-element write in framed task~~[**§4.5.198 RESOLVED**·`word.is_none()` 게이트 제거→suspendable `&mut` array write]·nested-in-frame-body hier enable[`task_calls_func` target transitivity]. 신규 `hier_task_call.rs`×14·`frame_task_module_write.rs`×6·기존 `frame_subset_overscan.rs` 1 flip.

- ~~**frame↔inline parity (정확도-우선)**~~ **완결(§4.5.198/199/200)**: `automatic`(framed) task가 inline task보다 지원 좁은 비대칭=정확도 갭이었음. **step 1 §4.5.198**(module array-element write)·**step 2 §4.5.199**(multi-dim frame-LOCAL array)·**step 3 §4.5.200**(STATIC task hier-call force-frame). differential-sweep(~25 구성) 결과 **frame ⊇ inline 완성**(string/sformatf local은 오히려 inline이 약함·local-queue는 양쪽 loud=parity 문제 아님). **hier-task 완성**(automatic+static 모두 커버). **잔여 loud follow-on(correct-or-loud·별개·드묾)**: ~~multi-dim FORMAL(1D binding)~~(**§4.5.202 RESOLVED**·ascending zero-based multi-dim array formal이 N-D md-packed slot 재사용·descending/output/inout multi-dim은 여전히 loud)·~~hier-task의 output/inout scalar formal~~(**§4.5.201 RESOLVED**·dual-lower+port-dir routing)·hier-task의 string/array formal(cross-boundary array copy)·nested-in-frame-body hier enable(`task_calls_func` target transitivity)·generate-block 内 hier task call(pre-scan은 top-level proc block만)·~~STATIC(non-framed) task array formal~~(**§4.5.203 RESOLVED**·`is_fixed_unpacked_array_formal`→`build_task_frame_set` force-frame·§4.5.200 골격 재사용)·~~output/inout multi-dim array formal~~(**§4.5.204 RESOLVED**·copy-out unpack LHS를 fully-indexed `caller[i0][i1]…`로 일반화)·~~descending/mixed multi-dim array formal~~(**§4.5.205 RESOLVED**·§4.5.202 과보수 gate 제거·per-dim direction MATCH가 진짜 guard·경험적으로 forward 확인). ~~descending 다차원~~·~~non-zero-based~~(**§4.5.206 non-zero-base ASCENDING RESOLVED**·tuple에 lo threading·non-zero-base descending은 correct-or-loud). **이로써 배열 formal shape 스토리 완성**(input/output/inout × 1-D/multi-dim × zero-based-any-direction + non-zero-base-ascending × automatic/static/function·잔여=non-zero-base descending[correct-or-loud]). **hier-task 계열**: ~~hier-task INPUT array formal~~(**§4.5.207 RESOLVED**·defer actual-net + resolve-time pack)·~~nested-in-frame-body hier enable~~(**§4.5.208 RESOLVED**·format 22→23·`FuncMeta.has_hier_call` force-suspend·iverilog oracle)·잔여=hier-task OUTPUT/INOUT array formal[deferred copy-out·hard]·frame-formal array를 nested hier로 forward[drop].
- ~~**`$systask($display)`-in-subset-function**~~ **RESOLVED §4.5.196**(genuine print sid→`frame_print_stmts` positive gate[validator-only]·`classify_frame_body` 허용·`run_frame_call`/`run_task` render arm[`format_args_str`+`sink.emit(RtlOutput)`·format 무변화]·severity/$timeformat은 set 밖→loud). $error/$fatal(severity)은 `&mut` had_error 필요→follow-on.
- ~~**task inout unpacked-array formal**~~ **RESOLVED §4.5.196**(§4.5.188 copy-in[`lower_array_actual_packed`]+§4.5.193 copy-out 조합·IEEE §13.5.2·hand-IEEE[iverilog가 unpacked subroutine port 거부]·round-trip 자기검증).
- ~~**imports-in-pkg**~~ **RESOLVED §4.5.196**(패키지 body Import arm이 `apply_import_consts`로 base 상수 fold·타입은 파서 resolve·패키지-내부 루틴 호출은 follow-on).
- **잔여 follow-on (전부 loud·correct-or-loud)**: **function이 자기 dyn-formal 재전달**(`return sum(c)`·framed callee·risky: mutual-recursion soundness hole→frame-route 시 신중 guard 필요·`nested_in_frame_body_loud` 유지) · **block-local automatic lifetime**(loop-body `automatic int j=k*10` 등·per-activation storage 필요=deep block-local-flatten 인프라·iverilog도 "Overriding default variable lifetime" 거부) · **chained-method** `q.min()[0]`(vague·iverilog syntax-error·no-oracle). **이미 동작(vita>iverilog)**: output-fn-in-`while`(D4)·task OUTPUT array(D5b·§4.5.193).

**소형 큐:**

- **string byte select on a PARENTHESIZED base가 조용히 0**(§4.5.220 재감사 발굴·pre-existing·scalar/fixed/dyn 全 형태): `(p)[0]`=0 vs `p[0]`=119. **paren-select 일반 갭이 아님** — `logic[7:0] v; (v)[0]`·`(v)[3:0]`은 bare 형태와 동일하게 정확. string 전용이며 근인은 `string_index_read`의 base gate가 `Ident|BitSelect`만 matches!하고 `Paren`을 unwrap 안 함(반면 `expr_is_string_ast`에는 `Paren` arm이 있음)→byte-select 경로에 도달 못 하고 width-0 handle의 packed bit-select로 낙하. **§4.5.220이 고친 것과 동일 실패 클래스**(gate가 자기 술어를 under-approximate)·한 gate 옆. iverilog는 구문 자체 거부(오라클 無).
- **scalar `real`의 part-select write가 조용한 no-op**(§4.5.220 재감사 발굴·pre-existing): `real x; x[3:0] = 4'hF`→값 불변·무진단. iverilog는 거부("can not select part of real"). §4.5.220이 dyn `real` ELEMENT(`r[0][3:0]`)를 loud화했으므로 **이제 scalar 쪽이 뒤처진 비대칭**(방향이 string 케이스와 반대·회귀 아님).
- **array reduction method가 var-init initializer에서 loud**(§4.5.219 재감사 발굴·pre-existing): `string s = $sformatf("%0d", arr.sum());` 및 배열 원소 형태 모두 E3009 "unsupported hierarchical function call arr.sum" — t0 pre-sweep 경로의 갭이며 scalar/array 동일(=게이트 문제 아님). `q.size()`·`.len()`·`.substr()`·`.name()`은 동작.
- **string-array 잔여 → §0 승격 큐 T1로 이관(2026-07-25)**: FIXED string array decl-init(`string s[2]='{"a","b"}`·module/block 양쪽 loud·iverilog ✓·§4.5.183 기록 항목) · fixed array **runtime index**/`foreach`(dyn 배열은 이미 동작→fixed만 element-net 표현 때문에 const-index 전용) · `string q[$]`(queue of string·iverilog ✓) · multi-dim `string s[2][2]`(iverilog ✓) · hierarchical `u.s[0]` · **frame-local**(task/function body) string array(static task=E3018·function/automatic=E3009). dyn element의 byte select `d[0][0]`(no-oracle)도 잔여.

- gen/iface **string** decl-init(→ §0 승격 큐 T2-9; queue/dyn은 **이미 동작**·2026-07-25 확인) · generate-case 스코프 이름 `gcase[0].x`.
- SYS-READ hier-element dest · hier-write sentinel panic→loud · generate-내 `import` · package 자기-func init(㉽). explicit `import p::t`(TYPE)=**§4.5.148 지원**.
- `$fmonitor`/`$fstrobe`(파일 strobe/monitor) — 현재 W3056 skip=**파일출력 silent drop**(non-silent·warned). 지원=**format bump 필요**(`SysTaskId` 변종 ① or 직렬화 사이드카 ②·staged 파리티): `FmtCapture`에 `fd:Option<u32>` 추가(engine-local)+strobe drain을 `file_write` 라우팅·전용 슬라이스. STDIN read(결정성 설계 필요).
- compound-const `==?` fold=**§4.5.146 지원**(sized 패턴)·잔여 fail-closed loud=unsized x/z 패턴(`'hx` self-width truncation)·negative-signed LHS·non-literal RHS. param override 비상수(W3056→error) · longint MIN fold(package) · loud-message 품질 2건(`[bit]` 캐스케이드·typedef-키 메시지).
- `case (x) inside {…}`(§12.5.4 wildcard case)=vita E2002 parse-reject(loud)·③ 후보(no-oracle: iverilog 13.0 `case inside`/`inside` op/array reduction method 全 거부→hand-IEEE `==?`+내부차분). `inside` operator는 지원(== 시맨틱·§11.4.13). based-literal 내 whitespace(`64'sh FFFF`)=vita lexer reject(loud) vs iverilog 허용(minor·§4.5.147 발굴).
- ~~**enum label 범위검증 부재**~~ **RESOLVED §4.5.165**: const-foldable label 값 vs const-foldable base 폭(부호 포함·폭 1..=64)을 range-check해 out-of-range를 loud(E2002)화(`{X=16}`→0·`{X=-1}`→15·auto-inc overflow·unsigned-64 음수 전부 iverilog와 일치). **잔여(fail-open·§2 invalid-program 한정)**: sized/based-literal label(`{A=8'hFF}`)·param-width base(`[N-1:0]`)의 out-of-range는 `const_lit` 미fold(decimal-only)라 skip=silent-truncate 잔존(iverilog reject하나 유효 프로그램 무영향·fail-open>over-reject·const_lit 확장 or elaborate-time 검사=별개 슬라이스).
- **sized-literal enum label → enum-method loud**(§4.5.164 발굴·pre-existing): `enum bit[3:0] {A=8'hFF}`처럼 label이 non-foldable(sized-literal/식)이면 `enum_defs` 미등록 → `.first`/`.next`/`.name` 全 enum-method가 E3010/E3009 loud(honest·유효값엔 무영향). `const_lit`이 unsized-decimal만 fold. diagnostic 품질 minor(`.name` 메시지="hierarchical function call deferred"·오도). fix=enum label const-fold 확장 or label-site 진단 개선. 부수: function-port receiver `.name`/`.first`=E3010(`var_enum`이 tf-port 미bind·enum-method-family 공통)·chained `x.name().len()`·`arr.min()[0]`=iverilog도 거부(no-oracle).
- ~~**bit63-set unsigned 64-bit param 리터럴**~~ **RESOLVED §4.5.167(round-14 V9)**: `const_eval_i64_lit`의 explicit-signed width-64 arm(`v as i64` bit-reinterpret)을 `cv.width==64`(부호 무관)로 확장 — 폭-64 리터럴=64-bit bit container. **§4.5.151 보류 사유**(naive `v as i64`가 downstream 부호 비교/산술 silent-wrong 위험)는 **`width==64` gate로 봉쇄**: 폭-64만 hit(narrow는 bit63 불가)·폭-64를 magnitude로 쓰는 건 pathological이며 downstream서 negative→loud(bound clamp→width 1·array index E4002·i64::MIN abs_diff 全 cap 초과). 적대 2-lens 全 magnitude sink 추적 CLEAN(differential서 오히려 iverilog가 magnitude-misuse silent-wrong).
- **partial-timescale 정책 진단**(`--timescale-policy`·`W-PARSE-TIMESCALE-PARTIAL`/`E-PP-TIMESCALE-PARTIAL`): 일부 모듈만 `` `timescale `` 선언 시 현재 무진단 1ns/1ns 디폴트(전무 케이스만 W1017). doc-08 §15 설계는 문서화됨·`rt.default_used` 신호 이미 존재 — 배선만 필요. §4.5.151 발굴.

**외부 리포트 잔여 (§6-2 → ARCHIVE · 전부 no-oracle 또는 docs):**

- EXT2-A2c: packed multi-dim param `localparam logic[1:0][7:0] PK=…`(외부 0회 사용·hand-IEEE+내부차분).
- EXT2-NAP: named assignment pattern `'{k:v}`(외부 0회).
- EXT2-DOC: 문서 stale(CLI-ref·lang-ref·system-tasks·explain — 외부 2회 보고).

**deep 잔여(저우선):**

- t0 race 그라운딩(계단식 CA 체인) · `@(*)` decl-init wake · runtime `==?` pattern.
- inline body NON-fill context-width · modport 방향 강제 · force part-select · assoc 배열-key/clocking 배열-output word0.
- **음수 range bound**(`logic [3:-2]`/`[-1:-8]` net·multi-packed inner `[1:0][3:-2]`·unpacked `[-1:2]`) — iverilog=`|msb-lsb|+1`(예: `[3:-2]`=6bit). 현재 net/multi-packed=W3056 warn+clamp-1(**whole-value 손상**)·unpacked=E4002. 전부 non-silent(§4.5.135 후 diag 정직화). 정식 지원=**packed-struct-member 선례 미러**(whole=flat offset 정확+sub-select loud; `struct_field_select.rs`): `range_to_dims` 정확 폭+정규화 base·neg-base 마커로 sub-select loud-guard(u32 dbase→signed 또는 사이드카). 단, `[W-1:0]`-with-W==0 param underflow(lsb≥0)는 graceful width-1 유지(test `v3_12`).
- **VCD 잔여 fidelity**(§4.5.138 range fix 후·전부 **cosmetic·decode 동일**·§4.5.139서 VALUE 검증 완료: x/z·real·wide·readmem·format 全 decoded waveform iverilog 일치). 남은 encoding 차이: ① value 미압축=vita full-width(`bxxxxxxxx`) vs iverilog leading-redundant strip(`bx`·`b0`)—decode 동일·큰 golden churn ② t=0 초기덤프 구조=vita `$dumpvars`에 pre-assign X + `#0` change vs iverilog settled값—final 동일 ③ var-type=logic 절차구동시 vita `wire` vs iverilog `reg`(연속구동=both wire·usage 의존이라 non-trivial)·`int`=`reg` vs `integer` ④ real size `64` vs `1` ⑤ `parameter` 미덤프. + 근본: elaborate packed-md NetVar.lsb stale(lib.rs 8435/7862·VCD helper서 flat fallback 우회).
- **real const-fold 전면 미지원**(§4.5.141 발굴): `localparam/parameter real` = `2.0+3.0`·`*`·`/`·`-`·`**` 全 E3009 "not foldable"(iverilog=folds). `localparam=$clog2(real-lit)`도 동근(const_eval_in_scope=i64-only·real arg→None loud·§4.5.143 런타임은 해결). 런타임 real 산술은 정상(§4.5.141서 `**`도 지원)·const 경로만 uniformly loud. const_eval_in_scope에 real f64 arithmetic 추가 필요(broad·non-silent).
- **X-bearing integral→real 변환 divergence**(§4.5.141 발굴): vita=whole X값→`0.0`(`real_arg`=`to_i128_signed().unwrap_or(0)`) vs iverilog=per-bit X→0(예 `4'bxx01`→1). `$itor`/`$sqrt`/`$pow`/real-`**` 공통·pre-existing. non-silent 아니지만 divergent(impl-defined X→real).
- **width>128 정수→real 변환**=여전히 `0.0`(§4.5.151서 `to_i128_signed`를 128-bit lane까지 확장 — 65..=128 signed/unsigned는 수정 완료·>128만 잔여). 초희귀(129-bit+ 값의 real 대입)·word-grid f64 근사 필요.
- **x/z-fill const param LHS→0**(§4.5.146 발굴): `localparam logic [W] P = 'x`=const_eval가 `fill_to_i64`/`fill_literal_const`로 0 bind(x 소실)→**全 const 연산자 상속**(`P==0`·`P+1`·`P ==? pat` 4-state 결과 divergent). upstream param binding 근원·contrived(all-x const 선언)·broad. §4.5.146 `==?` fold는 sized 패턴만이라 무영향(a=int).

## 4. SVA / 검증 honest-loud 잔여

- empty-match `##0`/unbounded `##[m:$]` 융합(§16.9.2.1 불연속·오라클 부재).
- N2c full sequence local var(중첩 attempt 각자 데이터=L급; 단일-capture ✅).
- later-antecedent read · outer-`|=>` prop-ref skew 고급형(2-cycle·중첩·cross-clock).
- SVA-QUAD collapse default-flip(`VITA_SVA_COLLAPSE` opt-in 상태 — full-VCD 골든 audit 선행).
- N4 clocking 잔여: non-`#1step` skew·INOUT·multi-event-list clock·non-net bind·hier input drive·cross-hier `@(inst.cb)`.
- class: down-cast `Derived'(base)`($cast 런타임 타입가드 선행) · real→longint cast · base-shadow 명시 접근 `Base'(d).v` · cast-as-receiver `(B'(d)).foo()`.

## 5. perf / 하드닝 잔여 (전부 보류 판정 — 트리거 시만)

- SVA-QUAD default-flip = §4 항목과 동일(full-VCD audit 선행).
- FMT-CACHE part b(render_template pre-segment) · GEN-3X-STR part a(unroll plan 캐시=byte-identity 위험>이득) — 저ROI 보류.
- QUEUE-MID-ON: 스펙 내재 O(n)(iverilog 동일) — 영구 비권장·monitor-only.
- 백로그 원문·완료 32건 = ARCHIVE §5.

## 6. G2 — AI-Agent 친화 OBS 트랙 (SPEC=[preview/19](preview/19-ai-agent-observability.md))

> 완료: OBS-0 스펙 · OBS-1a run.json+results.jsonl(§4.5.73) · OBS-1b coverage.json(§4.5.99) · OBS-2 v1 trace.jsonl(§4.5.100) · OBS-3 stage.jsonl(§4.5.101) · OBS-S0 설계구조 export `--hier-tree`/`--inst-paths`(2026-07-13). teeth=3-way 내부 차분(JSONL≡VCD≡`$display`)+결정성 골든. 틀린 로그=silent-wrong과 동급. 값 인코딩 실태(§4.5.151 문서 정합)=trace `old`/`new`=full-width 4-state binary·stage `vals[]`=`%0d` decimal — doc-19 §3 pin 4에 기록.

| 단계 | 산출물 | 공수 |
|---|---|---|
| OBS-2 잔여 | sva.jsonl(R-L6·SVA property명+support-cone v0) · per-element array probe · class/event probe(no-oracle) | M |
| OBS-1 잔여 | staged/vrun obs(.velab source-identity) · compile-fail manifest · `--seed` | S-M |
| R-L4 | 로그 채널 분리 | M |
| OBS-4 | `vrun --control stdio` JSON-RPC(peek/poke/step/run_until)+poke 저널 replay | L |
| OBS-5 | snapshot/restore/rewind(엔진 상태 postcard 직렬화) | L-XL |
| OBS-6 | X-origin·region-annotated events·정적 backward cone | L+ |

- 비목표: FSDB/UCDB·SQLite 내장·waveform GUI·UVM 연동. VCD는 사람용 유지.

## 7. 조건부 / 장기 (재진입 트리거 충족 시에만 승격 · 정확성과 직교)

| id | 항목 | 트리거 |
|---|---|---|
| BACKEND | ① cycle-based 컴파일드(Verilator급) ② PDES BSP 병렬(Amdahl 상한 T4≈2.5x) ③ native-eval 잔여 lane(signed>64·>128bit·sysfunc·real) | ① 대형 RTL 실수요 ② 지속 W≥64+grain≥200ns ③ 저ROI 상시 defer |
| VHDL | VHDL 프론트엔드(9-value std_logic 매핑·별도 파서·GHDL 오라클·E7xxx) | SV plateau + 값도메인 결정 + GHDL 셋업 |
| VCD-EXT | `$dumpports*`(포트 strength) | 파형 툴 수요 (FST=**§4.5.149·150 지원** — `$dumpfile("x.fst")`/`-o x.fst`; known-edge=소형 타임테이블 fst-writer [issue #4] loud 거부, preview/07 참조) |
| MVP-CUT | string concat-nonassign · wildcard assoc `[*]` · package internal-import/scoped-call 잔여 · cross-frame disable | 개별 수요 시 |

## 8. 비계획 (영구 비목표 · gap 아님)

- **DEFPARAM**(IEEE deprecated·`#(.param())`로 충분) · **IMPLICIT-NET**(정책=E3010 명시 에러) · **OOS**(synthesis·waveform GUI·UPF/SDF/DPI-C·shortreal·trireg·UVM 생태계·unique/priority 다중-match 검사).

## 9. 완료 이력 포인터

- 완료 슬라이스 상세 로그(§4.5.3~§4.5.134)·구 §0~§7 원문 = [ROADMAP_ARCHIVE.md](ROADMAP_ARCHIVE.md)(§번호 보존).
- 탄 단위 내러티브·방법론 교훈 = [DEVLOG.md](DEVLOG.md)·ARCHIVE §3.
- 외부 호환성 리포트 1·2차 전말(A1~C1·EXT2 체인) = ARCHIVE §6·§6-2 — **잔여는 위 §3 "외부 리포트 잔여" 3건뿐**.

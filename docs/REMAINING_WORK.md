# vitamin — 잔여 작업 트래커 (Remaining Work)

> **"goal까지 남은 것"의 상위 스냅샷.** 재계획 시점마다 통째로 갱신한다(과거 판본은 git 이력이 보존).
>
> - **기준(2026-07-22)**: format_version **22** · **3994 tests green** · 3-OS CI green · MsgCode **59**(W3057) · **MSRV 1.85** · 최신 완료 = **§4.5.195(round-17 리포트 3-가족 A/B/C + CLI 이원-top 전부 RESOLVED** — A=queue-of-record[`Dim::Queue(None)`→packed-vector lowering·packable struct/tf-port unified whole-vector로 `q.push_back(p)`/`r=q[i]` end-to-end]·B=non-packable struct tf-local[파서-only·block-local 청사진 미러]·C=frame-body dyn-formal call[§4.5.194 RefCell로 `&self` executor marker 실행·gate 제거·전용 `dyn_formal_marker_stmts` set]·CLI=one-shot `--top` 허용+auto-top ambiguity W3057 경고화[iverilog-parity multi-root 유지]·format 22 불변·+25 tests·잔여 loud=function dyn-formal 재전달·D-가족 minor[hierarchical fn call=large]·상세 ARCHIVE §4.5.195). 직전 = **§4.5.194(round-16 executor-bound gaps 전부 RESOLVED** — `dyn_heap`를 `RefCell`로 interior-mutable화[class_heap 선례]→`&self` frame executors가 heap-op 수행·6-phase[0 plumbing·1a V2A-dyn subset task·1b write-path·2a V5 function/task dyn-local+new[]·2b V2B output/inout dyn formal]·framed-nested 동작·format 22 불변·+25 tests·상세 ARCHIVE §4.5.194) · 직전 = **round-16 리포트 대응 7-슬라이스**(§4.5.187~193, +56 tests, format 22 불변): §4.5.187(**$fopen 런타임 파일명**[변수/concat·CAVP walker·file-I/O 층]) · §4.5.188(**input unpacked-fixed array formal on TASK**[리포트 최대 task 클래스·함수 md-packed 경로 미러]) · §4.5.189(**SILENT-WRONG 수정: loop-body block-local init 매 진입 아닌 1회만→per-entry**[frame body init을 frame entry가 아닌 block entry에·IEEE §6.21]) · §4.5.190(**`arr[i].field` on packed-struct 1-D array**[register-file idiom·`struct_member_expr`/`parse_struct_field_lval` base 일반화]) · §4.5.191(**FIXED unpacked-struct 배열·V6**[TB=top axi_mem_model·packed-vector lowering+§4.5.190 재사용]) · §4.5.192(**task/function-local unpacked struct·V8**[TB=partial·tf-body 인식+packed-vector local]) · §4.5.193(**output unpacked-fixed array formal on TASK**[md-packed slot→packed temp out-bind→ret unpack·heap/format 무변화]). **★남은 리포트 항목(executor-bound)=§4.5.194서 전부 RESOLVED**: `dyn_heap`를 `RefCell`로 interior-mutable화하니 `&self` frame executor도 heap-op 수행 가능(executor `&self` 자체가 아니라 dyn_heap plain-Vec가 원인이었음)→V2A-dyn-nonsuspendable[Phase 1a]·V5 function/task-dyn-local+new[][Phase 2a]·V2B function/task-output/inout-dyn[Phase 2b]·framed-nested[동작] 전부 supported. 잔여 loud follow-on=recursion-with-dyn(per-activation heap stash)·string/real/class-element dyn formal. · **그 이전** §4.5.186(**constant-function evaluation in const contexts loud→supported** — fresh-area probe서 발견. `localparam W = clog(256)`[while-loop 함수]가 vita E3009인데 iverilog는 평가[흔한 파라미터 계산 idiom]. `const_eval_in_scope`가 사용자 `Call` 미처리→elaborate-time 정수 함수-body 인터프리터[`const_func_table`을 param fold 전 populate·`eval_const_call`/`eval_const_env`/`exec_const_stmt`·`const_binop` 추출로 폴딩 공유]·correct-or-loud 극엄[param silent-wrong=P0-5·정수 도메인 밖·런타임 참조·비종료 loop step cap·과재귀 depth cap 全 loud]·적대 differential 全 MATCH·`const_function_eval.rs`×15·+15) · 그 이전 §4.5.185(**`$bits(TYPE)` loud→supported** — fresh-area probe서 발견. `$bits(logic[15:0])`·`$bits(s_t)`가 vita 거부인데 iverilog는 fold[`$bits(변수)`는 동작·TYPE 인자만 gap]. `$bits`는 compile-time 상수·파서가 타입 폭[struct_layouts/typedefs/atom] 보유→파서서 `$bits(TYPE)`를 IntLit로 fold[keyword+dims·bare type-name·struct Σ/union MAX]·decl-range/param 위치 동작·correct-or-loud[INTEGRAL만 fold·초판 `$bits(real)`=1 silent-wrong을 적대 probe서 발견해 real/string/event loud화]·적대 differential 全 MATCH·`bits_of_type.rs`×11·+11) · 그 이전 §4.5.184(**multi-dimensional packed array struct/union member loud→supported** — fresh-area probe서 발견. `union packed { logic [1:0][3:0] nib; }`가 vita parse error인데 iverilog ✓·독립 multi-dim packed net은 지원[멤버 파서만 단일 range]. struct 멤버가 파서서 part-select로 desugar→파서-only·format 불변[StructMember에 `packed_dims` 확장·sim-ir 미도달]·`member_flat_dims`[flat=base×∏inner·elem_stride=∏inner]·`parse_struct_field_sel`가 `s.m[i]`→`Indexed{i*stride +: stride}`·correct-or-loud[element write/range/ascending/nested/record-array=loud follow-on·runtime index는 iverilog 초과 지원]·적대 differential 全 MATCH·`multidim_packed_struct_member.rs`×13·+13) · 그 이전 §4.5.183(**SILENT-WRONG 수정: block-local `string s[] = '{…}` dyn-array init 조용히 drop → supported** — §4.5.182 적대 검증 중 발견. `string s[]='{...}`를 initial/always BLOCK 내부에 두면 vita SZ=0[빈 배열] vs iverilog SZ=3[module-scope 정상]. block-local decl-init 수집기의 `scalar_string` 판정이 per-name unpacked dim 무시→dimensioned string도 scalar 취급→`name.unpacked.is_empty()` gate서 drop. module-scope 수집기는 이미 `is_dyn_str_init` 특례 有→두 scope 비대칭. block-local gate에 동일 조건[`Dim::Dyn`+`AssignPattern`] 미러→기존 flush 라우팅으로 supported화[module parity·fixed/multi-dim/concat string은 loud 유지]·적대 differential 全 MATCH·`block_local_string_dyn_init.rs`×9·format 22 불변·+9) · 그 이전 §4.5.182(**queue/dyn-array `{…}` unpacked-array concat decl-init loud→supported** — fresh-area probe서 queue 메서드 차분 중 발견. `int q[$]={1,2,3,4}`·`int d[]={a,b,c}`가 vita는 `'{…}` assignment-pattern만 수용해 E3009[SV §10.10 unpacked concat=합법·iverilog ✓]. 신규 헬퍼 `dyn_pattern_elems`가 `AssignPattern|Concat`서 균일 element 추출→Concat init을 기존 `'{…}` var-init flush에 라우팅[handle-gate+flush 2곳·collect는 무수정]. correct-or-loud by construction: `{…}`=`'{…}` byte-identical→loud 상속[array-elem `{a,3}`·replication `{n{x}}`[별도 Replicate node]·STRING 원소[gate 제외→silent-empty 방지] 全 loud]·적대 differential 全 MATCH·`queue_dyn_brace_init.rs`×14·format 22 불변·+14) · 그 이전 §4.5.181(**enum `.next(N)`/`.prev(N)` CONSTANT-step loud→supported** — fresh-area probe서 발견한 oracle-backed loud gap. `e.next(2)`처럼 step 인자가 있으면 파서가 arg-less enum-method desugar(1652)를 건너뛰어 generic Call→E3009 "hierarchical function call (deferred)"였음. 파서에 constant-step 분기 추가: `var_enum` 멤버의 `next|prev`에 single literal arg(`const_lit`)면 `enum_step_n_expr`가 N-step ternary chain 생성[각 label→N 위치 앞/뒤·`rem_euclid`로 wrap·`prev(N)`=`next(len-N)`]. non-constant step은 loud 유지(correct-or-loud)·arg-less `.next()` byte-identical·AST/IR 무변화(기존 Ternary/`==`/IntLit)·적대 differential 全 MATCH[next(2/3/4/0)·prev·wrap·identity]·`enum_methods.rs`+5·format 22 불변·+5) · 그 이전 §4.5.180(**SILENT-WRONG 수정: same-named STATIC block-locals in DISJOINT blocks → loud** — fresh-area iverilog 차분 probe서 발굴. 두 sibling/named/cross-process 블록이 같은 이름 static 블록-로컬을 선언하면 v1이 한 module net으로 flatten→둘째 블록이 첫째의 TYPE[sign/width]과 STORAGE를 조용히 물려받음[`%d` sign·`>>>` arith·`%h` width·uninit read 全 오염·vita -3/253·5 vs iverilog 253/x]. `hoist_block_local_nets` collision 경로에 type-mismatch + definite-assignment guard 추가[E3009]·SAME-type+assigned-first coalesce[흔한 `for`/`tmp` 재사용]는 무영향·module-var SHADOW[struct/enum scoping·scope-leak guard]는 `local_decl_names` 제외로 무영향·적대 differential 검증·`block_local_name_collision.rs`×9·format 22 불변·+9) · 그 이전 §4.5.179(**FRAMED function dyn-formal call BURIED in an expression loud→supported** — `$display(f(a))`·`r=f(a)+1`·`if(f(a)>0)`를 R5-B inout-hoist 骨格 재사용해 `__t=f(a)` temp(direct-rhs)으로 hoist→§4.5.177 snapshot marker 재발화. framed function=pure(output formal 無)라 eval-order guard 불요·hoist는 무조건-평가 위치만(short-circuit `&&`/`||` RHS·`?:` arm·`while`/`case` scrutinee·frame-body 내부 call=loud 유지)·각 hoist가 자기 snapshot marker emit→mutation 사이 pass-by-value freshness 유지·적대 differential 全 MATCH·format 22 불변·+14) · §4.5.178(**SILENT-WRONG 수정: dyn-array `input` formal WRITE in synchronous frame body → loud** — §4.5.177이 formal을 heap snapshot으로 READ 지원했으나 body가 formal을 WRITE(`b[0]=9`)하면 `&self` frame executor가 heap store(`&mut`) 불가→write가 scalar frame slot로 조용히 유실·`r=f(a)`가 pre-write 값 반환(1 vs IEEE 9)하던 pre-existing 버그. `frame_write_lvalue` 상단 runtime guard가 heap dyn-array write 도달 시 F4004(element/whole/`new[]` 균일 포착·string slab-store 제외·read-only body 무영향)·format 22 불변·+4) · 그 이전 §4.5.177(**FRAMED function `input` dynamic-array formal loud→supported** — "순차적으로 전부" 원래 목표. control-flow-body automatic function의 `input c[]`을 `NetKind::DynArray` net으로 reserve하고 direct-rhs `r=f(arr)`서 caller 배열을 `handle_copy` snapshot marker로 deep-copy[module-process `&mut` executor가 실행]·§4.5.176이 body foreach 동작시킴·framed function은 `&self`라 dyn-array 변형 불가→snapshot sound·**correct-or-loud by construction**[marker 없는 call=`emit_frame_call` loud→non-direct-rhs/nested/recursion/sign-mismatch 全 loud]·적대 differential 全 MATCH·format 22 불변·+9) · §4.5.176(**`foreach` over dynamic array/queue/assoc inside FUNCTION/SUBSET-task body loud→supported** — §4.5.175 loud를 supported로: iteration key가 항상 frame-local이라 `&self` 동기 executor가 interior-mutable frame window로 advance 가능·`assoc_iter_step` read/compute를 `SimState::assoc_iter_compute`로 추출[process path byte-identical]·`frame_assoc_iter`가 key write·적대 differential 全 검증·잔여 loud=non-local key direct iter·format 22 불변·+2) · §4.5.175(**SILENT-WRONG 수정: `foreach` … → loud** — desugar된 walk step `a.first/next(__i)`이 iteration key를 side-effect write하는데 동기 `&self` frame executor[`run_frame_call`/`run_task`]가 못 advance→조용히 0 반환하던 것을 runtime fatal F4004로 loud화·적대 de-risk probe서 vita 0 vs iverilog 15 발굴·suspendable task/`for`-loop workaround/fixed-array 미영향·format 22 불변·+8) · §4.5.174(**inline static-task `foreach`-on-dyn-formal loud→supported** — 파서가 `foreach(b[i])`를 `b.first/next(__i)`로 desugar하는데 dyn/queue dense-walk dispatch가 `dyn_handle`로 array를 resolve해 `dyn_subst` formal alias를 못 봐 E3009였음→`dyn_handle_read`[alias 우선]로 2 site 교체·formal이 module dyn-array와 동일 dense walk·`dyn_subst` empty 밖에선 모든 array byte-identical·적대 differential 全 MATCH·`inline_foreach_dyn_formal.rs`×8·format 22 불변·+8) · §4.5.173(**V2A-frame: AUTOMATIC task `input` dynamic-array formal loud→supported** — formal을 per-activation `NetKind::DynArray` heap slot으로 예약[`reserve_frame_task`]하고 caller 배열을 frame ENTRY서 deep-copy[pass-by-VALUE·IEEE §13.5.1·`frame_dyn_snapshot_formals`·exec.rs 2 suspendable site]·V5 net-range lifecycle이 formal 자동 커버[free/reentry]·**wire 무변화**[snapshot src를 새 IR 필드 대신 in-bind Signal서 엔진-side 회수→format 22 불변·golden churn 0]·적대 differential 全 MATCH[특히 **snapshot-immune-across-suspend**=동시 resize 누출 0]·LOUD=recursion/concurrent/non-suspendable-subset/sign-mismatch·`frame_dyn_formal.rs`×13·+13) · §4.5.172(**frame-body validator over-scan false-REJECT 수정** — subset task가 not-defined-last일 때 `classify_frame_body`의 linear `block_base..func_blocks.len()` 스캔이 post-pass서 뒤 func 블록까지 over-read→그들의 out-of-frame write를 오판해 E3009 false-reject하던 것을 **reachable-block CFG walk**[entry서 자기 CFG 엣지만·`Call`→`ret_bb`라 함수 밖 안 나감]로 교체·correct-or-loud[미방문=타 func/dead→false-reject만 DROP·silent-wrong 불가]·적대 differential 全 MATCH[nested call·control flow·suspendable-mixed·still-loud 유지]·`frame_subset_overscan.rs`×4·format 22 불변·+4) · §4.5.171(**V5: frame-local(task-body) DYNAMIC array loud→supported** — TASK 한정·per-net heap + lifecycle[reentry guard=recursion/concurrent fatal F4004·free-at-exit]·30-site 재키잉 회피·function은 `&self` executor 한계로 loud·회귀 발굴[frame-local string `dyn_is_handle` 공유→`kind!=String` 가드]·적대 differential ~18 probe MATCH·format 22 불변·+13) · §4.5.170(**V2A: TASK `input` dynamic-array formal loud→supported** — static task 한정·**pass-by-value SNAPSHOT**[적대 soundness agent가 초판 direct-alias의 IEEE §13.5.1 위반 silent-wrong 발굴→`alloc_dyn_snapshot`+`handle_copy_stmts` entry deep-copy로 수정]·automatic/frame은 V5[handle-in-slot] 필요→loud-defer·공통 `dyn_array_actual_net` fix로 re-forward가 함수·태스크 양쪽 동작·format 22 불변·+10) · §4.5.169(**frame-local unpacked ARRAY loud→supported** — §4.5.168 잔여 loud 첫 항목·"순차적으로 전부" 1순위·배열 FORMAL의 md-packed 표현 재사용[`classify_unpacked_array` 공유·`reserve_frame_local_decl` 4-site 통일·`frame_arr_formal_meta`]·엔진 frame_local=net-range라 md-packed net 자동 per-activation/재귀 격리·적대 differential ~20 probe common 全 MATCH+automatic-array 격리/subroutine array port는 vita>iverilog[hand-IEEE]·잔여 loud safe gap=multi-dim/non-zero-based/non-simple-element/whole-copy/foreach/NBA-elem/`'{}`init·format 22 불변·+1) · 직전 §4.5.168(**V3/V4 suspendable tasks** — task 内 `@`/`#`/wait/NBA/$systask/재귀/nested가 scheduler call-stack + window stash/restore로 동작·round-14 gating ①·format 22 불변[recompute]·적대 2-lens core SOUND+5 over-lift correct-or-loud화) · §4.5.167(round-14 loud→supported: **V9** 64-bit MSB-set scalar-localparam 리터럴 fold[`const_eval_i64_lit`의 signed-only width-64 bit-reinterpret arm을 `cv.width==64`로 확장·§4.5.151 defer 해소·magnitude sink 全 loud 확인] + **V1** function/task/block/class-method-local `string` 변수[`frame_local_net_kind`·4 사이트 heap NetKind::String slot] + **V1-twin** 엔진 str_bytes frame-aware[심층검증서 loud→silent 회귀 발굴: NetKind만 고치면 `.len()` 등 method가 frame slot 못 읽어 조용히 0→`read_net` 미러로 수정]; round-14=§4.5.166 ★V10의 confirmation 리포트; 적대 2-lens differential CLEAN[~40 probe·zero silent-wrong·오히려 iverilog가 magnitude-misuse silent] + soundness SOUND[4 change 全]; elaborate value-only+engine eval-only·format 불변). 미착수 후보("순차적으로 전부" 잔여)=**function-frame dyn-formal follow-on(§4.5.177 direct-rhs `r=f(arr)`·§4.5.179 buried-call[`$display(f(a))`·`r=f(a)+1`·`if(f(a)>0)`] 완료·잔여=nested caller/recursion[frame-body 내부 call·`&mut`-capable executor + per-activation 필요]·formal WRITE[§4.5.178서 loud화—`&self` executor heap 불가·구조적])·V5 follow-on(function dyn-array LOCAL[`new[]`=`&mut` heap·`&self` 구조적 불가]·automatic task recursion/concurrent[per-activation heap stash])·V3/V4 잔여 loud(in-frame fork·per-activation repeat counter)·V6/V8 unpacked struct(oracle-reject→hand-IEEE)** · 함수/태스크 body-내부 read→caller comb 감도(§2 DEEP·§4.5.166) · size-cast `N'(expr)` context width(§2 DEEP).
> - 잔여 상세 목록(정본) = [ROADMAP.md](ROADMAP.md) · 완료 상세 = [ROADMAP_ARCHIVE.md](ROADMAP_ARCHIVE.md)(§번호 보존) · 이력 = [DEVLOG.md](DEVLOG.md) · 실행 큐 = `LOOPROMPT.md` NEXT.
> - **최종 목표**: **G1** = icarus·verilator·xcelium·vcs급 *정확한* RTL 시뮬레이터(correct-or-loud) · **G2** = AI-Agent 친화 simulator(SPEC=[preview/19](preview/19-ai-agent-observability.md)).

## A. 현재 상태 한 줄 요약

- **oracle-backed ① silent-wrong = 소진**(발굴 즉시 수정 체제). known silent-wrong 잔여는 전부 **pre-existing·baseline 동일·deep-defer 기록됨**(ROADMAP §2).
- 외부 리포트 1·2차(EXT2)·round 3~11 = **사실상 완결**(잔여 3건=A2c·NAP·DOC, 전부 no-oracle/docs).
- 나머지 잔여는 **honest-loud=안전**(ROADMAP §3~§5) + **G2 OBS 트랙**(ROADMAP §6).

## B. 다음 착수 후보 (우선순위순)

| # | 항목 | 근거/오라클 |
|---|---|---|
| 1 | 신규 CRITICAL silent-wrong 발굴분(fresh-area probe) | iverilog 라이브 차분 |
| 2 | 소형 loud→supported 큐(ROADMAP §3) | iverilog ✓ 대부분 |
| 3 | OBS-2 sva.jsonl(R-L6) 또는 OBS-1 잔여(staged obs·`--seed`) | 3-way 내부 차분 |
| 4 | DEEP-defer 재개(%c/%s UTF-8 pipeline·derived-localparam self-width·top-level typedef ②) | 전용 인프라 슬라이스 |

## C. 잔여 분류 (요약 — 상세=ROADMAP 해당 §)

| 분류 | 내용 | 정본 |
|---|---|---|
| 🔴 silent-wrong 잔여 | DEEP 5(UTF-8 pipeline·derived-param width·$unit typedef·enclosing-const·packed-WIDTH sibling) + 중형 ~15(enum·sign-loss·repl-count·residual sub-select·param-scope·iface-2D·$dist_*·string-concat-width 등) — 전부 pre-existing·기록됨 | ROADMAP §2 |
| honest-loud 잔여 | string/heap·함수/formal·소형 큐·EXT2 3건·deep 저우선 | ROADMAP §3 |
| SVA/검증 잔여 | empty-match 융합·N2c full·prop-ref skew 고급형·QUAD default-flip·N4 clocking 잔여·class down-cast | ROADMAP §4 |
| perf/하드닝 | 전부 보류 판정(SVA-QUAD flip·FMT-CACHE b·GEN-3X-STR a·QUEUE-MID) | ROADMAP §5 |
| G2 OBS | sva.jsonl·staged obs·R-L4·control API·snapshot·X-origin | ROADMAP §6 |

## D. 별도 관리 — 트리거 충족 시에만 승격 (정확성과 직교)

| id | 항목 | 트리거 |
|---|---|---|
| BACKEND | cycle-based 컴파일드 · PDES BSP(T4≈2.5x 상한) · native-eval 잔여 lane | 대형 RTL 실수요 · W≥64+grain≥200ns · 저ROI defer |
| VHDL | VHDL 프론트엔드(GHDL 오라클·E7xxx) | SV plateau + 값도메인 결정 + GHDL 셋업 |
| VCD-EXT | `$dumpports*`(포트 strength) | 파형 툴 수요 (FST=**§4.5.149·150 지원**; known-edge=소형 fst-writer issue #4) |
| MVP-CUT | string concat-nonassign·wildcard assoc `[*]`·cross-frame disable 등 | 개별 수요 시 |

## E. 비계획 — 영구 비목표 (gap 아님)

- **DEFPARAM**(deprecated) · **IMPLICIT-NET**(정책=E3010) · **OOS**(synthesis·waveform GUI·UPF/SDF/DPI-C·shortreal·trireg·UVM·unique/priority 다중-match).
